---
phase: 06-marketing-components-navigation
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - artifacts/web/src/components/marketing/HowItWorksSection.tsx
  - artifacts/web/src/components/marketing/MultiChannelSection.tsx
  - artifacts/web/src/components/marketing/CtaSection.tsx
autonomous: true
requirements: [MKT-04, MKT-05, MKT-08]
must_haves:
  truths:
    - "How-it-works renders a 3-step non-technical managed onboarding sequence"
    - "Multi-channel section presents lala.la, Telegram, and social channels"
    - "A mid-page CtaSection repeats the primary CTA with identical wording from the same i18n keys"
  artifacts:
    - path: "artifacts/web/src/components/marketing/HowItWorksSection.tsx"
      provides: "MKT-04 3-step onboarding"
      exports: ["HowItWorksSection"]
      min_lines: 30
    - path: "artifacts/web/src/components/marketing/MultiChannelSection.tsx"
      provides: "MKT-05 multi-channel deployment"
      exports: ["MultiChannelSection"]
    - path: "artifacts/web/src/components/marketing/CtaSection.tsx"
      provides: "MKT-08 mid-page CTA repeat"
      exports: ["CtaSection"]
  key_links:
    - from: "artifacts/web/src/components/marketing/CtaSection.tsx"
      to: "CtaButton with t.cta.button"
      via: "import from ./CtaButton, same label keys as hero/footer"
      pattern: "t\\.cta\\.button"
---

<objective>
Build the remaining three content sections: `HowItWorksSection` (MKT-04), `MultiChannelSection` (MKT-05), and the mid-page `CtaSection` (MKT-08).

Purpose: Completes the content-section set begun in 06-02. This plan owns a disjoint file set from 06-02 (no shared files) so the two run in parallel. The `CtaSection` here is one of the three required CTA repeat positions (hero in 06-02, footer in 06-04) — all three reuse the same `CtaButton` primitive and the same i18n label keys to guarantee identical wording (MKT-08).
Output: HowItWorksSection.tsx, MultiChannelSection.tsx, CtaSection.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-marketing-components-navigation/06-RESEARCH.md
@.planning/phases/06-marketing-components-navigation/06-PATTERNS.md
@.planning/phases/06-marketing-components-navigation/06-UI-SPEC.md
@.planning/phases/06-marketing-components-navigation/06-01-SUMMARY.md

<interfaces>
From artifacts/web/src/components/marketing/CtaButton.tsx (built in 06-01):
```typescript
export function CtaButton(props: { label: string; fallbackLabel: string; size?: "sm" | "md" }): JSX.Element;
```
From artifacts/web/src/lib/i18n.ts — marketing copy slice (`t = getMessages(locale).marketing`):
- t.onboarding.{title, subtitle, step1_label, step1_desc, step2_label, step2_desc, step3_label, step3_desc}
- t.channels.{title, subtitle, lala_label, telegram_label, social_label}
- t.cta.{headline, subheadline, button, no_telegram}

Type the `t` prop as `ReturnType<typeof getMessages>["marketing"]` (no named MarketingMessages export exists).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HowItWorksSection (MKT-04)</name>
  <files>artifacts/web/src/components/marketing/HowItWorksSection.tsx</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (HowItWorksSection lines 435-479 — StepCard + grid; shared Section Wrapper/Heading/Subtitle lines 793-846)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Contract 8 HowItWorks lines 362-389 — numeral weight 800 display font vs label weight 600 sans)
    - artifacts/web/src/index.css lines 403-451 (@layer marketing-tokens)
  </read_first>
  <action>
    Create `HowItWorksSection.tsx` exporting `HowItWorksSection({ t })`. Universal section wrapper (`<section className="w-full py-20"><div className="w-full max-w-[1120px] mx-auto px-6">`). `<h2>` = `t.onboarding.title`, `<p>` subtitle = `t.onboarding.subtitle` Body `text-[--mkt-muted-fg]` mt-4. Then a `grid grid-cols-1 md:grid-cols-3 gap-8`. Define an inline `StepCard` sub-component per 06-PATTERNS: `flex flex-col gap-4 min-w-0` (no card border — open layout). The numeral ("01"/"02"/"03") is Subheading size `1.5rem` weight **800** `var(--mkt-font-display)` color `text-[--mkt-accent]` aria-hidden; the `<h3>` label is Subheading `1.5rem` weight **600** `var(--mkt-font-sans)` `text-[--mkt-fg]`; the `<p>` desc is Body `text-[--mkt-muted-fg]`. Render 3 steps from `t.onboarding.step1_label/step1_desc`, `step2_*`, `step3_*`. Copy must stay non-technical (it already is in the scaffold). Fonts via inline style only.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -q "onboarding.step1_label" artifacts/web/src/components/marketing/HowItWorksSection.tsx && grep -q "onboarding.step3_label" artifacts/web/src/components/marketing/HowItWorksSection.tsx && grep -q "md:grid-cols-3" artifacts/web/src/components/marketing/HowItWorksSection.tsx && ! grep -E "bg-primary|text-foreground|font-sans\b|bg-card|border-border" artifacts/web/src/components/marketing/HowItWorksSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - HowItWorksSection renders 3 StepCards reading step1/step2/step3 label+desc from `t.onboarding.*`.
    - Step numerals use weight 800 + `var(--mkt-font-display)` + `--mkt-accent`; labels use weight 600 + `var(--mkt-font-sans)` (differentiated by weight/font, not a new size).
    - Grid uses `grid-cols-1 md:grid-cols-3`; each step has `min-w-0`.
    - Zero fan-page utilities; fonts via inline style (grep gate passes).
  </acceptance_criteria>
  <done>Three-step non-technical onboarding renders; numerals/labels correctly differentiated; grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: MultiChannelSection (MKT-05)</name>
  <files>artifacts/web/src/components/marketing/MultiChannelSection.tsx</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (MultiChannelSection lines 483-517 — ChannelCard + grid)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Contract 9 MultiChannel lines 392-413)
    - artifacts/web/src/index.css lines 403-451 (@layer marketing-tokens)
  </read_first>
  <action>
    Create `MultiChannelSection.tsx` exporting `MultiChannelSection({ t })`. Section wrapper + `<h2>` `t.channels.title` + `<p>` `t.channels.subtitle` Body `text-[--mkt-muted-fg]` mt-4. Then a `grid grid-cols-1 sm:grid-cols-3 gap-6`. Define an inline `ChannelCard` sub-component per 06-PATTERNS: `flex flex-col items-center gap-3 p-6 min-w-0 rounded-[--mkt-radius-lg] border border-[--mkt-border] bg-[--mkt-surface-1] text-center`, hover `border-[--mkt-accent]/40`, lucide icon 32px `text-[--mkt-accent]`, `<h3>` Subheading weight 600 `var(--mkt-font-sans)` `text-[--mkt-fg]`. Render 3 channel cards using lucide-react icons: lala.la (`Globe`, label `t.channels.lala_label`), Telegram (`Send`, `t.channels.telegram_label`), Social (`Share2`, `t.channels.social_label`). Every grid child carries `min-w-0`. Fonts via inline style only.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -q "channels.lala_label" artifacts/web/src/components/marketing/MultiChannelSection.tsx && grep -q "channels.telegram_label" artifacts/web/src/components/marketing/MultiChannelSection.tsx && grep -q "channels.social_label" artifacts/web/src/components/marketing/MultiChannelSection.tsx && grep -q "sm:grid-cols-3" artifacts/web/src/components/marketing/MultiChannelSection.tsx && ! grep -E "bg-primary|text-foreground|font-sans\b|bg-card|border-border" artifacts/web/src/components/marketing/MultiChannelSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MultiChannelSection renders 3 ChannelCards for lala.la, Telegram, and Social from `t.channels.*`.
    - Each card uses a lucide-react icon at 32px in `--mkt-accent`.
    - Grid uses `grid-cols-1 sm:grid-cols-3`; each card has `min-w-0`.
    - Zero fan-page utilities; fonts via inline style (grep gate passes).
  </acceptance_criteria>
  <done>Three-channel deployment section renders (lala.la + Telegram + social); grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: CtaSection mid-page repeat (MKT-08)</name>
  <files>artifacts/web/src/components/marketing/CtaSection.tsx</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (CtaSection lines 578-611)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Contract 11 CtaSection lines 447-471; Copywriting Contract — identical-wording rule lines 503-507)
    - artifacts/web/src/components/marketing/CtaButton.tsx (06-01 — the shared primitive)
    - artifacts/web/src/index.css lines 403-451 (@layer marketing-tokens)
  </read_first>
  <action>
    Create `CtaSection.tsx` exporting `CtaSection({ t })`. Section wrapper with `text-center py-20`. `<h2>` = `t.cta.headline` Display size, `<p>` = `t.cta.subheadline` Body `text-[--mkt-muted-fg]` mt-4. Then a `mt-8 flex justify-center` containing `<CtaButton label={t.cta.button} fallbackLabel={t.cta.no_telegram} />`. CRITICAL (MKT-08): use the exact same i18n keys (`t.cta.button` / `t.cta.no_telegram`) and the same `CtaButton` primitive as the hero (06-02) and footer (06-04) so all three CTA instances share identical wording and the same deep-link URL. Do not inline a different label. Optional reduced-scale bloom is the executor's call (UI-SPEC says optional). Fonts via inline style only.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -q "CtaButton" artifacts/web/src/components/marketing/CtaSection.tsx && grep -q "t.cta.button" artifacts/web/src/components/marketing/CtaSection.tsx && grep -q "t.cta.no_telegram" artifacts/web/src/components/marketing/CtaSection.tsx && ! grep -E "bg-primary|text-foreground|font-sans\b|bg-card|border-border" artifacts/web/src/components/marketing/CtaSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CtaSection renders `t.cta.headline` + `t.cta.subheadline` + a single `CtaButton` using `t.cta.button` and `t.cta.no_telegram`.
    - The CTA reuses the shared `CtaButton` primitive (not an inline `<a>`), guaranteeing identical wording/URL with hero and footer (MKT-08).
    - Zero fan-page utilities; fonts via inline style (grep gate passes).
  </acceptance_criteria>
  <done>Mid-page CTA repeat renders with identical wording via the shared primitive; grep gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n namespace → DOM | Localized copy rendered as auto-escaped React text |
| client → external (t.me) | CtaSection CTA navigates to the build-time Telegram deep-link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering (XSS) | section copy rendering | mitigate | All copy rendered as React JSX text children; no `dangerouslySetInnerHTML` (enforced — sections read `t.*` only) |
| T-06-01 | Tampering | CtaSection external link | mitigate | Reuses CtaButton primitive which already applies `rel="noopener noreferrer"` (06-01 T-06-01); no new external link constructed here |
| T-06-SC | Tampering | npm/pnpm installs | accept | No new packages; lucide-react already present (RESEARCH) |

No HIGH threats: static rendering, no user input, no inner-HTML, no direct network calls.
</threat_model>

<verification>
- `grep -rn "bg-primary\|bg-background\|bg-card\|text-foreground\|text-primary\|border-border\|font-sans" artifacts/web/src/components/marketing/` returns zero new matches in this plan's files.
- Each of the 3 components exports its named function.
- CtaSection reuses CtaButton with `t.cta.*` keys (identical-wording guarantee).
</verification>

<success_criteria>
- MKT-04 3-step non-technical onboarding renders.
- MKT-05 multi-channel section (lala.la + Telegram + social).
- MKT-08 mid-page CTA repeat with identical wording via the shared primitive.
</success_criteria>

<output>
Create `.planning/phases/06-marketing-components-navigation/06-03-SUMMARY.md` when done.
</output>
