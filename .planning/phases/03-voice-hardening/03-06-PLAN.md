---
phase: 03-voice-hardening
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - lib/providers/src/providers/gmi-tts-client.ts
  - artifacts/api-server/src/providers/gmi/GmiVoiceProvider.ts
  - artifacts/worker/src/workers/voice-generation.ts
  - artifacts/worker/package.json
  - artifacts/api-server/package.json
  - lib/twin-runtime/src/voice.ts
  - lib/twin-runtime/src/index.ts
  - lib/db/src/schema/index.ts
  - lib/db/src/migrations/014_twins_voice_id.sql
autonomous: true
requirements: [VOICE-01, VOICE-02]
tags: [voice, gmi-tts, opossum, circuit-breaker, worker, voice-generation, async-queue]

user_setup:
  - service: gmi-cloud-tts
    why: "Voice worker submits + polls the GMI TTS async request queue (contract confirmed in 03-01-GMI-TTS-CONTRACT.md)"
    env_vars:
      - name: GMI_TTS_BASE_URL
        source: "https://console.gmicloud.ai (per 03-01-GMI-TTS-CONTRACT.md — distinct from GMI_API_BASE_URL which is the LLM chat host)"
      - name: GMI_TTS_MODEL_ID
        source: "minimax-tts-speech-2.6-hd (synth model; per 03-01-GMI-TTS-CONTRACT.md)"
      - name: GMI_TTS_FALLBACK_VOICE_ID
        source: "Preset voice_id used when twins.voice_id is null, e.g. English_expressive_narrator (per 03-01-GMI-TTS-CONTRACT.md)"
      - name: GMI_TTS_POLL_INTERVAL_MS
        source: "Optional. Poll interval for the submit→poll loop. Default 1500."
      - name: GMI_TTS_TIMEOUT_MS
        source: "Optional. Overall submit+poll timeout. Default 30000."

must_haves:
  truths:
    - "When GMI TTS responds normally, the voice-generation worker SUBMITS a job to the async request queue, POLLS until status='success', FETCHES the audio bytes from outcome.media_urls[0].url, uploads to Object Storage at `creators/{creatorId}/generations/{jobDbId}.mp3`, sets generation_jobs.result_url to the storage key, and (for telegram delivery) sends an audio file via bot.telegram.sendAudio with the COMPLY-01 disclosure caption"
    - "Synthesis uses twins.voice_id when present (the creator's cloned voice); when twins.voice_id is null, it falls back to the preset GMI_TTS_FALLBACK_VOICE_ID so the synth path is testable now. The clone-registration step (reference clip → voice_id) is STUBBED behind registerVoiceClone() with a TODO referencing the contract's one open item — its request shape is NOT invented"
    - "When GMI TTS submit+poll fails repeatedly (≥50% errors over 60s), opossum trips the breaker; subsequent voice jobs fall back to null (no audio) without calling GMI; downstream worker writes generation_jobs.status='failed' with errorMessage='circuit-open' and does NOT deliver any voice — fan sees the text reply only"
    - "The submit+poll unit is bounded: the worker polls every GMI_TTS_POLL_INTERVAL_MS (default 1500ms) up to GMI_TTS_TIMEOUT_MS (default 30000ms); on timeout it throws so opossum records a failure"
    - "Voice worker re-checks creator's voice consent (`creator_kyc.voice_synthesis_consent_granted` AND `consent_grants(modality=voice, granted=true, revokedAt IS NULL)`) IMMEDIATELY before Object Storage write — if revoked between enqueue and finish, discard audio bytes and mark job 'cancelled' (Pitfall 7)"
    - "Telegram voice delivery always uses bot.telegram.sendAudio (mp3 is not a valid Telegram voice note — voice notes require OGG/Opus). The disclosure caption is attached. OGG/Opus voice-note delivery via sendVoice is deferred unless transcoding is added (Pitfall 5 revised)"
    - "If L3 moderation flagged self-harm on the transcript before this job ran, voice job is short-circuited at worker entry: no GMI call, no storage write, no delivery (Security Domain — SB 243 defense-in-depth)"
  artifacts:
    - path: "lib/providers/src/providers/gmi-tts-client.ts"
      provides: "GmiTtsClient/callGmiTts implementing submit→poll→fetch against console.gmicloud.ai, wrapped by opossum CircuitBreaker; plus a STUBBED registerVoiceClone()"
      contains: "CircuitBreaker|opossum|gmiTtsBreaker"
      min_lines: 80
    - path: "artifacts/api-server/src/providers/gmi/GmiVoiceProvider.ts"
      provides: "Replaces the stub at line 13–28 with a real call to gmi-tts-client"
      contains: "GmiVoiceProvider"
    - path: "artifacts/worker/src/workers/voice-generation.ts"
      provides: "Worker body replaces console.log stub at lines 39–41 with the full submit→poll→fetch→upload→deliver pipeline"
      min_lines: 100
    - path: "lib/twin-runtime/src/voice.ts"
      provides: "shouldGenerateVoice + enqueueVoiceJob helpers used by web route and text worker (wiring in 03-07)"
      contains: "shouldGenerateVoice|enqueueVoiceJob"
    - path: "lib/db/src/migrations/014_twins_voice_id.sql"
      provides: "Adds nullable twins.voice_id column (the creator's cloned MiniMax voice_id from clone Step A)"
      contains: "voice_id"
  key_links:
    - from: "lib/providers/src/providers/gmi-tts-client.ts"
      to: "GMI Cloud TTS async request queue"
      via: "fetch(GMI_TTS_BASE_URL + /api/v1/ie/requestqueue/apikey/requests) submit + poll {id}"
      pattern: "requestqueue/apikey/requests"
    - from: "artifacts/worker/src/workers/voice-generation.ts"
      to: "opossum breaker"
      via: "breaker.fire(input)"
      pattern: "breaker\\.fire\\(|gmiTtsBreaker\\.fire\\("
    - from: "voice worker"
      to: "creator_kyc + consent_grants"
      via: "consent recheck before Object Storage write"
      pattern: "voice_synthesis_consent_granted|findActiveVoiceConsentGrant"
    - from: "artifacts/worker/src/workers/voice-generation.ts"
      to: "twins.voice_id (cloned voice) with GMI_TTS_FALLBACK_VOICE_ID fallback"
      via: "payload.voice_id resolution before submit"
      pattern: "voice_?[iI]d|GMI_TTS_FALLBACK_VOICE_ID"
---

<objective>
VOICE-01 + VOICE-02: implement the GMI TTS async-queue client (submit → poll → fetch result URL) wrapped in an opossum circuit breaker, add the `twins.voice_id` column for the creator's cloned voice, fill the voice-generation worker body (fetch bytes from the returned GCS URL → upload mp3 to Object Storage → deliver via sendAudio), and ship a small shared lib (`voice.ts`) used by 03-07 to wire enqueue points into the web route and text worker.

Purpose: SC1 (voice reply with fallback). The worker is the most context-heavy single piece in Phase 3 — async submit+poll TTS call + circuit breaker + GCS fetch + Object Storage upload + Telegram delivery + consent recheck + caption-with-disclosure. Isolating it in its own plan keeps quality high.

Output: A wrapped async TTS client (submit/poll/fetch), a `twins.voice_id` column, a real (non-stub) voice provider, a complete worker body, and a small twin-runtime helper for downstream consumers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-voice-hardening/03-CONTEXT.md
@.planning/phases/03-voice-hardening/03-RESEARCH.md
@.planning/phases/03-voice-hardening/03-PATTERNS.md
@.planning/phases/03-voice-hardening/03-01-GMI-TTS-CONTRACT.md
@CLAUDE.md
@lib/providers/src/providers/gmi-client.ts
@artifacts/api-server/src/providers/gmi/GmiTextProvider.ts
@artifacts/api-server/src/providers/gmi/GmiVoiceProvider.ts
@artifacts/worker/src/workers/voice-generation.ts
@artifacts/worker/src/workers/text-generation.ts
@artifacts/hermes/src/lib/object-storage.ts
@lib/db/src/schema/index.ts

<interfaces>
Target shape for `lib/providers/src/providers/gmi-tts-client.ts` (async submit→poll→fetch — per 03-01-GMI-TTS-CONTRACT.md):
```typescript
import CircuitBreaker from "opossum";

export interface GmiTtsInput {
  text: string;
  voiceId: string;        // twins.voice_id when set, else GMI_TTS_FALLBACK_VOICE_ID
  language: "en" | "ja" | "zh-TW";
  creatorId: string;      // for Helicone hashing
}
export interface GmiTtsOutput {
  mediaUrl: string;       // outcome.media_urls[0].url — a storage.googleapis.com URL
  audioBytes: Buffer;     // fetched from mediaUrl by the client (mp3)
  mimeType: "audio/mpeg"; // MiniMax outputs mp3/flac; we request format:"mp3"
}

// callGmiTts: POST submit → poll GET {id} until success/failed/cancelled (bounded
// by GMI_TTS_POLL_INTERVAL_MS / GMI_TTS_TIMEOUT_MS) → fetch outcome.media_urls[0].url.
export const gmiTtsBreaker: CircuitBreaker<[GmiTtsInput], GmiTtsOutput | null>;
//   .fire(input) → Promise<GmiTtsOutput | null>   (the WHOLE submit+poll+fetch unit)
//   .fallback(() => null)  // null = caller falls back to text-only
//   timeout: GMI_TTS_TIMEOUT_MS (default 30_000)  errorThresholdPercentage: 50  resetTimeout: 60_000

// STUB — clone Step A. Request shape is the ONE open item in the contract; do NOT invent it.
export async function registerVoiceClone(referenceUrl: string): Promise<string>; // → voice_id
```

Target shape for `lib/twin-runtime/src/voice.ts`:
```typescript
export async function shouldGenerateVoice(creatorId: string, twin: { voiceReferenceUrl: string | null }): Promise<boolean>;
export async function enqueueVoiceJob(args: {
  jobDbId: string;
  creatorId: string;
  fanIdHash: string;
  transcript: string;
  locale: "en" | "ja" | "zh-TW";
  conversationId: string;
  deliveryChannel: "web" | "telegram";
  telegramChatId?: number;
  handle?: string;
  twinId?: string;
}): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the confirmed contract + assert env wiring (no longer a blocking gate)</name>
  <files>artifacts/worker/.env.example, artifacts/api-server/.env.example</files>
  <read_first>
    - .planning/phases/03-voice-hardening/03-01-GMI-TTS-CONTRACT.md  (CONFIRMED — async queue, console.gmicloud.ai host, two-step clone with one open item)
  </read_first>
  <action>
    The GMI TTS contract is CONFIRMED (founder-pasted 2026-05-30). The only open item — the clone-registration (Step A) request shape — is STUBBED in Task 2, not blocking. This task is no longer a human-verify gate.

    Read `03-01-GMI-TTS-CONTRACT.md` and assert the env contract is wired into `.env.example` (or the canonical env doc) so the worker/client read from env, never hard-coded paths:
    - `GMI_TTS_BASE_URL=https://console.gmicloud.ai`
    - `GMI_TTS_MODEL_ID=minimax-tts-speech-2.6-hd`
    - `GMI_TTS_FALLBACK_VOICE_ID=English_expressive_narrator`
    - `GMI_TTS_POLL_INTERVAL_MS` (optional, default 1500)
    - `GMI_TTS_TIMEOUT_MS` (optional, default 30000)

    Add these keys to the worker and api-server `.env.example` files if absent. Do NOT embed the submit/poll paths anywhere as literals except inside the client where they derive from `GMI_TTS_BASE_URL` — the paths `/api/v1/ie/requestqueue/apikey/requests[/{id}]` are part of the GMI request-queue API surface and live in the client only.
  </action>
  <verify>
    <automated>grep -E "GMI_TTS_BASE_URL|GMI_TTS_MODEL_ID|GMI_TTS_FALLBACK_VOICE_ID" artifacts/worker/.env.example artifacts/api-server/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GMI_TTS_BASE_URL" artifacts/worker/.env.example` >= 1 and the value is `https://console.gmicloud.ai`.
    - `grep -c "GMI_TTS_MODEL_ID" artifacts/worker/.env.example` >= 1 and the value is `minimax-tts-speech-2.6-hd`.
    - `grep -c "GMI_TTS_FALLBACK_VOICE_ID" artifacts/worker/.env.example` >= 1.
    - No hard-coded `api.gmi-serving.com` appears in any voice file (`grep -rn "api.gmi-serving.com" lib/providers/src/providers/gmi-tts-client.ts` returns 0 — voice host is console.gmicloud.ai).
  </acceptance_criteria>
  <done>Confirmed contract read; the async-queue env vars (base URL, model id, fallback voice id) are wired into env and reachable without hard-coded paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add twins.voice_id + install opossum + write the async submit/poll/fetch GMI TTS client (with stubbed clone)</name>
  <files>lib/db/src/schema/index.ts, lib/db/src/migrations/014_twins_voice_id.sql, artifacts/worker/package.json, artifacts/api-server/package.json, lib/providers/src/providers/gmi-tts-client.ts, lib/providers/src/index.ts</files>
  <read_first>
    - lib/providers/src/providers/gmi-client.ts  (the exact analog — fromEnv, Helicone routing, retry-with-backoff)
    - lib/providers/src/providers/GmiTextProvider.ts (file imports / ProviderError taxonomy)
    - lib/db/src/schema/index.ts  (twinsTable at line 113 — add voiceId column next to voiceReferenceUrl)
    - lib/db/src/migrations/011_phase3_voice_dsar_ocr.sql  (migration style — IF NOT EXISTS, comment header)
    - .planning/phases/03-voice-hardening/03-01-GMI-TTS-CONTRACT.md  (CONFIRMED async submit/poll/fetch + two-step clone)
  </read_first>
  <action>
    **A. Add `twins.voice_id` column (clone Step A output):**
    - In `lib/db/src/schema/index.ts`, add `voiceId: text("voice_id"),` to `twinsTable` (nullable; sits next to `voiceReferenceUrl`). Add a comment: cloned MiniMax voice_id from clone Step A; null until clone registration runs.
    - Write `lib/db/src/migrations/014_twins_voice_id.sql` following the `011_phase3_voice_dsar_ocr.sql` style: `ALTER TABLE twins ADD COLUMN IF NOT EXISTS voice_id TEXT;` with a comment header.
    - **[BLOCKING] schema push:** this introduces a schema change. Follow the phase's existing migration pattern — the post-merge hook runs `pnpm --filter db push`. Surface that `pnpm --filter @workspace/db run push` must be run for local dev before the worker resolves `twins.voice_id`.

    **B. Install packages:**
    ```
    pnpm --filter @workspace/api-server add opossum
    pnpm --filter @workspace/api-server add -D @types/opossum
    pnpm --filter @workspace/worker add opossum
    pnpm --filter @workspace/worker add -D @types/opossum
    pnpm --filter @workspace/providers add opossum
    ```
    Per RESEARCH §Installation Reference. opossum @9.x. Verified [OK] by slopcheck in 03-01. Client lives in `lib/providers/` (matches existing `gmi-client.ts` and the §Architectural Responsibility Map row "GMI TTS HTTP call: Primary Tier: API/Backend (lib/providers)").

    **C. Write `lib/providers/src/providers/gmi-tts-client.ts` — ASYNC submit/poll/fetch:**

    Per `03-01-GMI-TTS-CONTRACT.md` (this REPLACES the old synchronous-bytes assumption):

    1. `callGmiTts(input: GmiTtsInput): Promise<GmiTtsOutput>`:
       - **Submit:** `POST ${GMI_TTS_BASE_URL}/api/v1/ie/requestqueue/apikey/requests` with `Authorization: Bearer ${GMI_API_KEY}`, body `{ model: GMI_TTS_MODEL_ID, payload: { text: input.text, voice_id: input.voiceId, language_boost: "auto", format: "mp3", speed: "1", vol: "1", pitch: "0", emotion: "auto", audio_sample_rate: "32000", bitrate: "128000", channel: "2" } }`. Parse `{ request_id, status }`.
       - **Poll:** `GET ${GMI_TTS_BASE_URL}/api/v1/ie/requestqueue/apikey/requests/{request_id}` every `GMI_TTS_POLL_INTERVAL_MS` (default 1500) until `status` ∈ {`success`, `failed`, `cancelled`} or until `GMI_TTS_TIMEOUT_MS` (default 30000) elapses. On timeout: throw (lets opossum record a failure). On `failed`/`cancelled`: throw.
       - **Fetch:** on `success`, read the result URL at `outcome.media_urls[0].url` (a `storage.googleapis.com` URL). `fetch()` that URL, read the bytes into a Buffer. Return `{ mediaUrl, audioBytes, mimeType: "audio/mpeg" }`.
       - Reuse the Helicone hashing pattern from `gmi-client.ts` for `creatorId` if routing through Helicone; otherwise raw fetch with the Authorization header. Host is `console.gmicloud.ai` via `GMI_TTS_BASE_URL` — NEVER `api.gmi-serving.com`.
    2. Wrap the WHOLE submit+poll+fetch unit in `CircuitBreaker`: `timeout: Number(process.env.GMI_TTS_TIMEOUT_MS ?? 30_000)`, `errorThresholdPercentage: 50`, `resetTimeout: 60_000`, `rollingCountTimeout: 60_000`, `rollingCountBuckets: 6`, `name: "gmi-tts"`.
    3. `.fallback(() => null)` — returns null on trip; worker uses this to skip voice delivery.
    4. Log events on `open` / `halfOpen` / `close` via console.warn/info.
    5. Export the breaker as `gmiTtsBreaker` (named export) so consumers call `breaker.fire(input)`.
    6. **STUB clone Step A:** add `export async function registerVoiceClone(referenceUrl: string): Promise<string>` with a body that throws `new Error("registerVoiceClone: clone-registration request shape is the one open item in 03-01-GMI-TTS-CONTRACT.md; not yet implemented")` and a `// TODO(03-01 OPEN ITEM): submit reference clip to minimax-audio-voice-clone-speech-2.6-hd → outcome.voice_id` comment. Do NOT invent the request shape. Tests use the preset `GMI_TTS_FALLBACK_VOICE_ID`.

    Add to `lib/providers/src/index.ts`: `export { gmiTtsBreaker, registerVoiceClone } from "./providers/gmi-tts-client.js";`.
  </action>
  <verify>
    <automated>pnpm --filter @workspace/providers run typecheck && pnpm --filter @workspace/db run typecheck && grep -c "CircuitBreaker\|opossum\|gmiTtsBreaker" lib/providers/src/providers/gmi-tts-client.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "voiceId\|voice_id" lib/db/src/schema/index.ts` returns at least 1 match (twins column added).
    - `grep -c "voice_id" lib/db/src/migrations/014_twins_voice_id.sql` >= 1.
    - `grep -n 'from "opossum"' lib/providers/src/providers/gmi-tts-client.ts` returns exactly 1 match.
    - `grep -n "new CircuitBreaker" lib/providers/src/providers/gmi-tts-client.ts` returns exactly 1 match.
    - `grep -c "requestqueue/apikey/requests" lib/providers/src/providers/gmi-tts-client.ts` >= 2 (submit + poll paths).
    - `grep -c "media_urls" lib/providers/src/providers/gmi-tts-client.ts` >= 1 (result fetch).
    - `grep -n "\\.fallback(" lib/providers/src/providers/gmi-tts-client.ts` returns exactly 1 match.
    - `grep -n "export.*gmiTtsBreaker" lib/providers/src/providers/gmi-tts-client.ts` returns exactly 1 match.
    - `grep -n "registerVoiceClone" lib/providers/src/providers/gmi-tts-client.ts` returns at least 1 match AND `grep -c "TODO" lib/providers/src/providers/gmi-tts-client.ts` >= 1 (clone stub marked).
    - `grep -rn "api.gmi-serving.com" lib/providers/src/providers/gmi-tts-client.ts` returns 0 matches (voice host is console.gmicloud.ai via env).
    - `grep -c "GMI_TTS_BASE_URL" lib/providers/src/providers/gmi-tts-client.ts` >= 1 (no hard-coded host).
    - `pnpm --filter @workspace/providers run typecheck` exits 0.
    - `cat artifacts/worker/package.json | grep -c opossum` >= 2 (dep + dev-dep).
  </acceptance_criteria>
  <done>twins.voice_id column + migration shipped; async submit/poll/fetch TTS client compiles against the confirmed contract; clone Step A is a clearly-marked stub.</done>
</task>

<task type="auto">
  <name>Task 3a: GmiVoiceProvider stub + Voice worker entry & guards</name>
  <files>artifacts/api-server/src/providers/gmi/GmiVoiceProvider.ts, artifacts/worker/src/workers/voice-generation.ts</files>
  <read_first>
    - artifacts/api-server/src/providers/gmi/GmiVoiceProvider.ts  (current stub at lines 13–28)
    - artifacts/api-server/src/providers/gmi/GmiTextProvider.ts  (env-driven constructor + ProviderError taxonomy)
    - artifacts/worker/src/workers/voice-generation.ts  (current stub + worker shell)
    - artifacts/worker/src/workers/text-generation.ts  (analog — getFanTwinOut singleton, hashFanId, generation_jobs status updates)
    - artifacts/worker/src/workers/consent-revocation.ts  (consent-revocation cancel-data shape)
    - .planning/phases/03-voice-hardening/03-RESEARCH.md  (§Security Domain — SB 243 voice short-circuit)
    - lib/db/src/schema/index.ts  (safetyAuditLogTable, generationJobsTable, creator_kyc shape, twins.voiceId)
  </read_first>
  <action>
    **A. `GmiVoiceProvider.ts` (api-server side):**
    Replace the stub body. The provider now calls `gmiTtsBreaker.fire(...)` and returns a `VoiceGenerationResult` shape compatible with the existing `IVoiceProvider` interface. Resolves `voice_id` from input (twin's cloned `voiceId` or `GMI_TTS_FALLBACK_VOICE_ID`). Throws `ProviderTransientError` on opossum timeouts/5xx, `ProviderError` on 4xx (matches GmiTextProvider taxonomy at line 101–119).

    Note: The worker calls the breaker DIRECTLY in Task 3b; the api-server provider is only invoked if a synchronous voice path is added in the future. For Phase 3 the worker is the sole caller — provider stays minimal but real.

    **B. Voice worker entry & guards (worker side) — first half of the worker body, ending BEFORE the breaker call:**

    Replace the stub at lines 39–41 with these steps in order. Task 3b picks up from step 6.

    1. **Mark processing** (Pattern from text-generation.ts:124–131):
       `db.update(generationJobsTable).set({ status: "processing", bullmqJobId: job.id, attemptCount: job.attemptsMade + 1 }).where(eq(generationJobsTable.id, payload.jobDbId))`.

    2. **Cancellation check (consent-revocation interlock):** Read `job.data.cancelled` flag. If true (set by consent-revocation worker per Phase 2 pattern), short-circuit: mark `status="cancelled"`, return. Already supported by the existing cancel-data shape.

    3. **SB 243 self-harm short-circuit (RESEARCH Security Domain):**
       Query the most recent `safety_audit_log` row for `(creatorId, fanIdHash)` from the past 60 seconds. If it has `crisis_type="self-harm"` OR `categoryScores['self-harm'] > 0.5`, abort voice: mark job `status="failed", errorMessage="self-harm-defense-in-depth"`. Do NOT deliver any voice. Text reply (if any) already went out via text-generation worker.

    4. **Load creator + twin:**
       Fetch `creators` (need handle for caption disclosure in Task 3c) and `twins.voiceReferenceUrl` + `twins.voiceId`. If `voiceReferenceUrl` is NULL, mark job `failed: "no-reference"` and return. Resolve the synth `voice_id`: use `twin.voiceId` when present, else `process.env.GMI_TTS_FALLBACK_VOICE_ID` (so the synth path is testable now even before clone Step A is implemented).

    5. **Pre-call consent check:**
       Reuse the `findActiveVoiceConsentGrant`-equivalent from `@workspace/db` (or write a thin wrapper if Hermes is the only existing caller). Also verify `creator_kyc.voice_synthesis_consent_granted = true`. If either fails: mark `cancelled: "consent-revoked"`, return.

    Leave a clearly marked `// HANDOFF: Task 3b — breaker call begins below` comment as the boundary. Do NOT call the breaker in this task.
  </action>
  <verify>
    <automated>pnpm --filter @workspace/api-server run typecheck && pnpm --filter @workspace/worker run typecheck && grep -cE "self-harm|self_harm|findActiveVoiceConsentGrant" artifacts/worker/src/workers/voice-generation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "self-harm-defense-in-depth\|self_harm.*defense" artifacts/worker/src/workers/voice-generation.ts` returns at least 1 match (SB 243 short-circuit error message).
    - `grep -n "findActiveVoiceConsentGrant\|voice_synthesis_consent" artifacts/worker/src/workers/voice-generation.ts` returns at least 1 match (pre-call consent check).
    - `grep -nE "voice_?[iI]d|GMI_TTS_FALLBACK_VOICE_ID" artifacts/worker/src/workers/voice-generation.ts` returns at least 1 match (voice_id resolution with fallback).
    - `grep -n "HANDOFF: Task 3b" artifacts/worker/src/workers/voice-generation.ts` returns exactly 1 match.
    - `grep -n "gmiTtsBreaker.fire" artifacts/worker/src/workers/voice-generation.ts` returns 0 matches (NOT yet wired — that is Task 3b).
    - api-server + worker typechecks pass.
  </acceptance_criteria>
  <done>Provider stub replaced; worker entry has all guards (cancellation, SB 243, creator/twin load, voice_id resolution, pre-call consent) up to the breaker handoff.</done>
</task>

<task type="auto">
  <name>Task 3b: Voice worker — breaker submit/poll/fetch, mid-flight consent recheck, Object Storage upload (.mp3)</name>
  <files>artifacts/worker/src/workers/voice-generation.ts</files>
  <read_first>
    - artifacts/worker/src/workers/voice-generation.ts  (post-Task-3a state)
    - lib/providers/src/providers/gmi-tts-client.ts  (built in Task 2 — `gmiTtsBreaker` export; .fire() returns GmiTtsOutput|null with fetched audioBytes)
    - artifacts/hermes/src/lib/object-storage.ts  (env var names + error shapes; the worker uses the @replit/object-storage SDK directly, not raw fetch)
    - .planning/phases/03-voice-hardening/03-RESEARCH.md  (§Pitfall 7 — IMMEDIATELY-before-storage consent recheck)
    - .planning/phases/03-voice-hardening/03-01-GMI-TTS-CONTRACT.md  (result is fetched from outcome.media_urls[0].url; stored as .mp3)
  </read_first>
  <action>
    Hard precondition: `@replit/object-storage` is already installed into `@workspace/worker` by 03-01 Task 2 (founder-gated). This task imports and uses the SDK directly with NO raw-fetch fallback branch.

    Pick up at the `// HANDOFF: Task 3b — breaker call begins below` marker left by Task 3a. Append these steps:

    6. **Call the breaker (submit→poll→fetch happens inside the client):**
       ```
       const ttsOut = await gmiTtsBreaker.fire({
         text: payload.transcript,
         voiceId: resolvedVoiceId,   // twin.voiceId ?? GMI_TTS_FALLBACK_VOICE_ID (from Task 3a step 4)
         language: payload.language,
         creatorId: payload.creatorId,
       });
       if (ttsOut === null) {
         // Breaker open or fallback fired — no audio. Text reply already went out.
         await db.update(generationJobsTable).set({ status: "failed", errorMessage: "circuit-open" }).where(...);
         return;
       }
       // ttsOut.audioBytes are the mp3 bytes the client already fetched from outcome.media_urls[0].url.
       ```

    7. **Mid-flight consent recheck (Pitfall 7 — IMMEDIATELY before storage write):**
       Re-query the same consent predicates as the pre-call check in Task 3a. If revoked between Task 3a step 5 and now, discard `ttsOut.audioBytes` and mark `cancelled: "consent-revoked-mid-flight"`. Return — do NOT write storage, do NOT deliver.

    8. **Object Storage upload (SDK-only, no fallback) — .mp3 (D-02-13 pattern):**
       ```
       import { Client } from "@replit/object-storage";
       const client = new Client();  // reads REPLIT_OBJECT_STORAGE_BUCKET internally
       const storageKey = `creators/${payload.creatorId}/generations/${payload.jobDbId}.mp3`;
       await client.uploadFromBytes(storageKey, ttsOut.audioBytes);
       ```
       Extension is `.mp3` (MiniMax outputs mp3/flac; we request format:"mp3"). Do NOT include a raw-fetch fallback branch (W4/W5 — the SDK is a hard precondition from 03-01).

       If `REPLIT_OBJECT_STORAGE_BUCKET` is missing: log a warn and mark job `failed: "storage-unavailable"`. This is a configuration error, not a runtime fallback.

    9. **Set result_url** (storage key, NOT HTTP URL — 03-07 wraps it with HMAC; never expose the raw GCS URL):
       `db.update(generationJobsTable).set({ resultUrl: storageKey }).where(eq(generationJobsTable.id, payload.jobDbId))`. Status stays `processing` until Task 3c marks complete after delivery.

    Leave a clearly marked `// HANDOFF: Task 3c — delivery & completion below` comment as the boundary.
  </action>
  <verify>
    <automated>pnpm --filter @workspace/worker run typecheck && grep -cE "gmiTtsBreaker\.fire|@replit/object-storage|consent-revoked-mid-flight" artifacts/worker/src/workers/voice-generation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "gmiTtsBreaker.fire" artifacts/worker/src/workers/voice-generation.ts` returns exactly 1 match.
    - `grep -n "@replit/object-storage" artifacts/worker/src/workers/voice-generation.ts` returns exactly 1 match (import).
    - `grep -c "generations/\${payload.jobDbId}.mp3\|.mp3" artifacts/worker/src/workers/voice-generation.ts` >= 1 (storage key is .mp3, NOT .ogg).
    - `grep -c ".ogg" artifacts/worker/src/workers/voice-generation.ts` returns 0 (no .ogg storage key).
    - `grep -n "consent-revoked-mid-flight" artifacts/worker/src/workers/voice-generation.ts` returns exactly 1 match.
    - In the file: line of mid-flight consent recheck > line of `gmiTtsBreaker.fire` > line of pre-call consent recheck (from Task 3a). Verify ordering with `grep -n` line numbers.
    - In the file: line of `uploadFromBytes` > line of mid-flight consent recheck (Pitfall 7 invariant).
    - **No raw-fetch fallback to storage:** `grep -nE "fetch\(.*objectstorage|fetch\(.*storage\." artifacts/worker/src/workers/voice-generation.ts` returns 0 matches.
    - `grep -n "HANDOFF: Task 3c" artifacts/worker/src/workers/voice-generation.ts` returns exactly 1 match.
    - Worker typecheck passes.
  </acceptance_criteria>
  <done>Breaker submit/poll/fetch wired; mid-flight consent recheck guards storage write; mp3 audio is in Object Storage via SDK; result_url stored as the bucket key.</done>
</task>

<task type="auto">
  <name>Task 3c: Voice worker — delivery (sendAudio mp3) + mark-complete + failure handler</name>
  <files>artifacts/worker/src/workers/voice-generation.ts</files>
  <read_first>
    - artifacts/worker/src/workers/voice-generation.ts  (post-Task-3b state)
    - artifacts/worker/src/workers/text-generation.ts  (getFanTwinOut singleton, getDisclosureFooter)
    - .planning/phases/03-voice-hardening/03-01-GMI-TTS-CONTRACT.md  (mp3 is not a Telegram voice note)
  </read_first>
  <action>
    Pick up at the `// HANDOFF: Task 3c — delivery & completion below` marker left by Task 3b. Append:

    10. **Telegram delivery (if `payload.deliveryChannel === "telegram"`):**
        - Always use `bot.telegram.sendAudio` with the mp3 bytes. mp3 is NOT a valid Telegram voice note (`sendVoice` requires OGG/Opus), so we deliver as an audio file. OGG/Opus voice-note delivery via `sendVoice` is DEFERRED unless we add a transcoding step (note this with a `// DEFERRED: sendVoice (OGG/Opus) requires transcoding mp3→opus` comment).
        - Caption: `${payload.transcript}\n\n— ${getDisclosureFooter(locale, handle)}` (COMPLY-01 disclosure footer on every outbound).
        - Use `getFanTwinOut()` singleton pattern from text-generation.ts (no `.launch()`).
        - On send-failure: catch + log; do NOT throw (the audio is in storage; the failure is just delivery).

    11. **Mark complete:**
        `db.update(generationJobsTable).set({ status: "complete", completedAt: new Date() }).where(eq(generationJobsTable.id, payload.jobDbId))`. (Terminal status is `"complete"` per the generationJobStatus enum.) resultUrl was set in Task 3b step 9.

    12. **Failure handler** (worker.on("failed", ...)): mirror the existing pattern in voice-generation.ts:56–75 — increment attemptCount on transient, mark `status=failed` on final attempt. Breaker bookkeeping (open/halfOpen/close events) is logged by the breaker itself; this handler only tracks per-job failure state.

    For the web path (`payload.deliveryChannel === "web"`): skip step 10. The fan-page polls `/api/voice/:jobId` (03-07) and gets the audio. Worker just marks complete after Task 3b's storage write.
  </action>
  <verify>
    <automated>pnpm --filter @workspace/worker run typecheck && grep -cE "sendAudio|getDisclosureFooter" artifacts/worker/src/workers/voice-generation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sendAudio" artifacts/worker/src/workers/voice-generation.ts` returns at least 1 match (mp3 delivered as audio file).
    - `grep -n "getDisclosureFooter" artifacts/worker/src/workers/voice-generation.ts` returns at least 1 match (COMPLY-01 caption).
    - `grep -nE "status:.*\"complete\"|status:.*'complete'" artifacts/worker/src/workers/voice-generation.ts` returns at least 1 match (mark-complete; terminal enum value).
    - `grep -n "worker.on.*failed\|worker\.on(\"failed" artifacts/worker/src/workers/voice-generation.ts` returns at least 1 match (failure handler).
    - Worker typecheck passes.
  </acceptance_criteria>
  <done>Voice worker delivers mp3 via Telegram sendAudio with disclosure caption, marks job complete, and handles failures.</done>
</task>

<task type="auto">
  <name>Task 4: lib/twin-runtime/src/voice.ts — shouldGenerateVoice + enqueueVoiceJob helpers</name>
  <files>lib/twin-runtime/src/voice.ts, lib/twin-runtime/src/index.ts</files>
  <read_first>
    - lib/twin-runtime/src/conversation.ts  (analog: pure-fn-over-DB shape; persistTurn pattern)
    - artifacts/hermes/src/db.ts  (findActiveVoiceConsentGrant — copy the query shape into a workspace-db version)
    - .planning/phases/03-voice-hardening/03-PATTERNS.md  (§lib/twin-runtime/src/voice.ts — copy notes)
  </read_first>
  <action>
    Implement `lib/twin-runtime/src/voice.ts`:

    1. `shouldGenerateVoice(creatorId, twin)`: Returns true if ALL of:
       - `twin.voiceReferenceUrl != null`
       - `creator_kyc.voice_synthesis_consent_granted === true`
       - An active `consent_grants` row exists with `modality='voice', granted=true, revokedAt IS NULL`
       - `creators.kill_switch_active === false`
       Uses `@workspace/db` for queries (this lib already imports it for other modules). Returns `false` on any DB error (defensive — better to skip voice than crash text path).

    2. `enqueueVoiceJob(args)`: Creates a `generation_jobs` row (status='queued', kind='voice') and adds a BullMQ job to the `voiceGeneration` queue. Returns void (job id is in args.jobDbId, generated by caller via `crypto.randomUUID()`).

    3. Re-export from `lib/twin-runtime/src/index.ts` and add a subpath export `./voice` to package.json exports (match existing pattern).

    Wiring of these helpers into `artifacts/api-server/src/routes/twin.ts` and `artifacts/worker/src/workers/text-generation.ts` happens in 03-07 (so the proxy URL helper from 03-07 can be combined into one focused commit).
  </action>
  <verify>
    <automated>pnpm --filter @workspace/twin-runtime run typecheck && grep -c "shouldGenerateVoice\|enqueueVoiceJob" lib/twin-runtime/src/voice.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function shouldGenerateVoice" lib/twin-runtime/src/voice.ts` returns exactly 1 match.
    - `grep -n "export async function enqueueVoiceJob" lib/twin-runtime/src/voice.ts` returns exactly 1 match.
    - `grep -n "kill_switch_active\\|killSwitchActive" lib/twin-runtime/src/voice.ts` returns at least 1 match.
    - `grep -n "shouldGenerateVoice\\|enqueueVoiceJob" lib/twin-runtime/src/index.ts` returns at least 2 matches.
    - Typecheck exits 0.
  </acceptance_criteria>
  <done>Helpers exist; web route + text worker can import them in 03-07 to enqueue voice without duplicating consent logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GMI TTS HTTP ↔ Worker | Untrusted upstream async queue — opossum breaker isolates failure across the submit+poll+fetch unit |
| Object Storage write ↔ Consent state | Audio bytes must NEVER reach storage if consent was revoked between job enqueue and audio generation completion |
| GCS result URL ↔ fan | The `storage.googleapis.com` URL from `outcome.media_urls[0].url` is fetched server-side ONLY; never handed to the fan (TTL/visibility unconfirmed) |
| Voice reference URL ↔ GMI clone | A signed reference URL gives GMI temporary access to the creator's voice sample during clone Step A (stubbed) |
| Worker → Telegram delivery | Voice file bytes leave our infrastructure to Telegram CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Spoofing | GMI MITM returns attacker-controlled audio | accept | HTTPS to console.gmicloud.ai is the trust boundary; same model as text generation (Phase 2 accepted) |
| T-03-06-02 | Tampering | Consent revoked between job enqueue and worker run; voice delivers anyway | mitigate | Task 3a step 5 (pre-call check) AND Task 3b step 7 (Pitfall 7 — immediate-before-storage recheck) |
| T-03-06-03 | Repudiation | Worker writes audio to storage but creator denies generation occurred | accept | generation_jobs row carries creatorId + auditable status transitions; sufficient for N=1 |
| T-03-06-04 | Information disclosure | GCS result URL leaks → public-ish googleapis URL exposes the generated clip | mitigate | The `storage.googleapis.com` URL is fetched server-side only and never returned to the fan; fan access is via the HMAC proxy (03-07). Worker stores the Object Storage key, not the GCS URL |
| T-03-06-05 | Information disclosure | Helicone proxy leaks creator data via voice path | accept | Helicone receives only hashed fanId + creatorId (Pattern: Hashed fan_id at provider boundary); same model as Phase 2 text |
| T-03-06-06 | Denial of service | GMI 5xx storm or stuck-queued jobs exhaust the poll budget and stall the worker | mitigate | Bounded poll loop (GMI_TTS_TIMEOUT_MS, default 30s); opossum breaker trips at 50% error rate over 60s window; worker returns null and fan gets text-only |
| T-03-06-07 | Elevation of privilege | Self-harm transcript delivers as voice (SB 243 violation) | mitigate | Task 3a step 3 — pre-flight scan of recent safety_audit_log; abort voice on self-harm detection. Defense in depth beyond L3's deflection swap |
| T-03-06-08 | Tampering | Worker writes voice file outside `creators/{id}/` prefix → survives DSAR sweep | mitigate | Task 3b step 8 — storage key is constructed `creators/${payload.creatorId}/generations/${jobDbId}.mp3`; no other code path constructs storage keys for voice files |
| T-03-06-09 | Information disclosure | Worker logs raw transcript or audio metadata to console | mitigate | Logs reference `jobDbId` only; do NOT console.log `transcript`; `audioBytes.length` is acceptable (size, not content) |
| T-03-06-10 | Elevation of privilege | Voice URL guessing brute-forces `/api/voice/:jobId` | mitigate | 03-07 wraps the URL with HMAC token + 24h TTL; this plan's worker only stores the storage key, not the URL |
| T-03-06-11 | Tampering | Clone Step A request shape is invented and submits malformed/over-broad payloads | mitigate | registerVoiceClone() is a STUB that throws; the clone request shape is the one open contract item and is NOT invented. Synthesis uses preset/cloned voice_id only |
| T-03-06-SC | Tampering | Compromised opossum or @replit/object-storage package executes during install | mitigate | Package legitimacy gates cleared in 03-01 (opossum) and 03-01 (@replit/object-storage); both packages installed before any voice code runs |
</threat_model>

<verification>
- opossum/types/opossum added to api-server + worker (+ providers) package.json files.
- `twins.voice_id` column added in schema + migration 014; `pnpm --filter @workspace/db run push` runs clean.
- `pnpm run typecheck` exits 0.
- Manual: confirm Task 3 line-order invariants — pre-call consent check < gmiTtsBreaker.fire < mid-flight consent recheck < storage upload.
- Manual: confirm SB 243 short-circuit appears before any GMI call.
- Manual: confirm the client uses GMI_TTS_BASE_URL (console.gmicloud.ai) and the two requestqueue paths; no api.gmi-serving.com literal.
- Manual: confirm storage key ends in `.mp3` (not `.ogg`) and delivery uses sendAudio (not sendVoice).
- Integration test (deferred to 03-08): force breaker open via test env var; assert worker writes status='failed' errorMessage='circuit-open' and no delivery occurs.
</verification>

<success_criteria>
- GMI TTS happy path: text → submit → poll to success → fetch mp3 from media_urls[0].url → mp3 in Object Storage → Telegram sendAudio with disclosure caption.
- Cloned voice: synthesis uses twins.voice_id when set; falls back to GMI_TTS_FALLBACK_VOICE_ID when null; clone Step A is a stub (one open contract item).
- Circuit-breaker fallback: GMI submit/poll fails repeatedly → subsequent jobs return null → no voice delivery, text reply remains the only outbound.
- Consent racing: revoke voice between enqueue and finish → audio bytes discarded, no storage write.
- SB 243: self-harm transcript → voice short-circuited before GMI call.
- Format: result stored as .mp3, delivered via sendAudio (mp3 ≠ Telegram voice note).
</success_criteria>

<output>
Create `.planning/phases/03-voice-hardening/03-06-SUMMARY.md` with: breaker config, submit/poll/fetch flow, worker step order, consent-recheck count, twins.voice_id migration, clone-stub note, opossum verified version.
</output>
