---
phase: 02-twin-runtime-core
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - artifacts/api-server/src/config/env.ts
  - artifacts/hermes/src/index.ts
  - artifacts/hermes/package.json
  - artifacts/fan-twin/package.json
  - .env.example
  - artifact.toml
  - .replit
  - artifacts/fan-twin/vitest.config.ts
  - artifacts/api-server/src/__tests__/twin-chat.e2e.test.ts
  - artifacts/api-server/src/__tests__/hmac-conversation.test.ts
  - artifacts/api-server/src/__tests__/conversation-history.test.ts
  - artifacts/api-server/src/__tests__/moderation-l1.test.ts
  - artifacts/api-server/src/__tests__/moderation-l3.test.ts
  - artifacts/api-server/src/__tests__/disclosure-footer.test.ts
  - artifacts/api-server/src/__tests__/helpline-injection.test.ts
  - artifacts/api-server/src/__tests__/locale-detection.test.ts
  - artifacts/fan-twin/src/__tests__/webhook-ack.test.ts
  - artifacts/hermes/src/__tests__/persona-wizard.test.ts
autonomous: false
requirements: []
tags: [phase-2, infrastructure, env, vitest, package-audit]
user_setup:
  - service: replit
    why: "Replit Secrets panel must hold the new Phase 2 env vars before Wave 1 can ship"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN_LALA
        source: "Rename existing TELEGRAM_BOT_TOKEN in Replit Secrets panel"
      - name: TELEGRAM_BOT_TOKEN_FAN_TWIN
        source: "Create new bot via @BotFather, copy token to Replit Secrets"
      - name: OPENAI_API_KEY
        source: "platform.openai.com → API keys"
      - name: HMAC_CONVERSATION_SECRET
        source: "Generate via `openssl rand -hex 32` (32+ char random string)"
      - name: FOUNDER_TELEGRAM_CHAT_ID
        source: "DM @userinfobot on Telegram, copy your numeric chat ID"
    dashboard_config:
      - task: "Register new fan-twin bot via @BotFather (creator-specific name, e.g. @sakura_ai_bot)"
        location: "Telegram → @BotFather → /newbot"
      - task: "Delete SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY from Replit Secrets (Phase 1 carry-over)"
        location: "Replit project → Secrets panel"

must_haves:
  truths:
    - "api-server cold-starts without Supabase env vars set"
    - "Hermes reads TELEGRAM_BOT_TOKEN_LALA (not the old TELEGRAM_BOT_TOKEN)"
    - "Fan-twin port (3002 or alternative) is reserved in both artifact.toml AND .replit"
    - "Vitest runs in artifacts/fan-twin/ without error (empty suite is OK)"
    - "Every Wave 0 RED test file exists and fails for the documented reason (feature not yet built)"
    - "@telegraf/session npm legitimacy verified before any install (i18next-http-middleware removed per D-02-14)"
  artifacts:
    - path: "artifacts/api-server/src/config/env.ts"
      provides: "Zod env schema with OPENAI_API_KEY, HMAC_CONVERSATION_SECRET, TELEGRAM_BOT_TOKEN_LALA, TELEGRAM_BOT_TOKEN_FAN_TWIN required; SUPABASE_* removed"
    - path: "artifacts/fan-twin/package.json"
      provides: "Workspace package manifest (deps not yet installed — gated behind legitimacy checkpoint)"
    - path: ".env.example"
      provides: "Documents all Phase 2 env vars with example values"
    - path: "artifact.toml"
      contains: "fan-twin artifact entry on port 3002"
    - path: ".replit"
      contains: "fan-twin port mapping matching artifact.toml"
    - path: "artifacts/api-server/src/__tests__/twin-chat.e2e.test.ts"
      provides: "RED integration test for CHAT-01 full pipeline (currently fails)"
  key_links:
    - from: "artifacts/api-server/src/config/env.ts"
      to: "process.env"
      via: "z.object({...}).parse(process.env)"
      pattern: "envSchema\\.parse"
    - from: "artifacts/hermes/src/index.ts"
      to: "process.env.TELEGRAM_BOT_TOKEN_LALA"
      via: "renamed env var read"
      pattern: "TELEGRAM_BOT_TOKEN_LALA"
---

<objective>
Unblock Phase 2 by closing three Phase 1 carry-over bugs (Supabase env requirement, Hermes token env name, missing port allocation for fan-twin) and standing up the RED test scaffolds plus package legitimacy gates that every subsequent wave depends on.

Purpose: Wave 1+ cannot ship until api-server cold-starts without Supabase env vars, Hermes reads the renamed token, the fan-twin port is reserved, and the test files exist (so each subsequent task can run `vitest run <test-file>` and see RED→GREEN transitions). Per RESEARCH cross-cutting concern #1.

Output: Rewritten `config/env.ts`, renamed token reads in Hermes, `artifact.toml`+`.replit` updated with fan-twin port, `artifacts/fan-twin/` skeleton (package.json + vitest config — NO deps installed yet), 10 RED test files documented as failing-by-design, founder checkpoint for Replit Secrets rotation and package legitimacy verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-twin-runtime-core/02-CONTEXT.md
@.planning/phases/02-twin-runtime-core/02-RESEARCH.md
@.planning/phases/02-twin-runtime-core/02-PATTERNS.md
@.planning/phases/01-baseline-repair/01-VERIFICATION.md
@CLAUDE.md
</context>

<interfaces>
<!-- Existing env schema imports + Drizzle zod pattern. Executor uses these as the template. -->
From artifacts/api-server/src/config/env.ts (current buggy state):
- Imports `z` from "zod" and exports a parsed `env` object
- Required vars: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY (REMOVE these), DATABASE_URL, GMI_API_KEY, SESSION_SECRET

From lib/db/src/schema/index.ts:
- Uses `z` from "zod/v4" for schema validation — same pattern applies here

From artifacts/api-server/src/__tests__/safety-audit.test.ts (vitest analog):
- `vi.mock("@workspace/db", () => { ... })` pattern for unit tests without DB
- `vi.stubGlobal("fetch", vi.fn().mockImplementation(...))` for fetch-stubbing

From artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts (e2e analog):
- HTTP harness with `http.request` + `beforeAll` dynamic DB import gated on `process.env.DATABASE_URL`

From artifacts/hermes/package.json:
- Existing workspace package shape — fan-twin copies verbatim with renamed `name` field
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy verification — @telegraf/session (i18next-http-middleware REMOVED per D-02-14)</name>
  <read_first>
    - .planning/phases/02-twin-runtime-core/02-RESEARCH.md (## Package Legitimacy Audit section, lines ~188-205)
    - .planning/phases/02-twin-runtime-core/02-CONTEXT.md (D-02-14 — skip i18next-http-middleware install)
    - CLAUDE.md (pnpm preinstall guard)
  </read_first>
  <what-built>RESEARCH.md flagged @telegraf/session as [ASSUMED] because slopcheck was not run. Before any `pnpm add` in Wave 1+, the founder must visit each package page on npmjs.com and confirm legitimacy. Per D-02-14, i18next-http-middleware is NO LONGER on the install list — verification removed from this gate.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@telegraf/session — confirm maintainer is `telegraf` org, source repo `github.com/telegraf/session` exists and has recent commits, no postinstall scripts that hit network. Note version published (expect ^2.0.0-beta.x as of 2026-05).
    2. Open https://www.npmjs.com/package/telegraf — confirm ^4.16.3 is still current (no major bump that would break artifacts/hermes).
    3. Run `npm view @telegraf/session version` to record canonical version in plan 02-06b install task.
    4. (Conditional, only if 02-08 founder approves @replit/object-storage install path over raw fetch) Open https://www.npmjs.com/package/@replit/object-storage — confirm maintainer is Replit, recent updates, no suspicious postinstall. Plan 02-08 has its own checkpoint for the bucket creation that gates this install.
    5. If ANY package fails (no recent commits, suspicious maintainer, postinstall hits network), do NOT approve — escalate for an alternative.
  </how-to-verify>
  <resume-signal>Type "approved — @telegraf/session: vX.Y.Z" with confirmed version (and optionally @replit/object-storage: vX.Y.Z), or describe the issue.</resume-signal>
  <acceptance_criteria>
    - Founder explicit approval message with confirmed version string(s)
    - Per D-02 threat model: T-02-SC mitigated for @telegraf/session
    - Per D-02-14: i18next-http-middleware NOT installed (inline detectLocale used instead)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite api-server env schema + rename Hermes token + Vitest scaffold for fan-twin</name>
  <files>artifacts/api-server/src/config/env.ts, artifacts/hermes/src/index.ts, .env.example, artifacts/fan-twin/package.json, artifacts/fan-twin/tsconfig.json, artifacts/fan-twin/build.mjs, artifacts/fan-twin/vitest.config.ts, artifacts/fan-twin/src/index.ts</files>
  <read_first>
    - artifacts/api-server/src/config/env.ts (current required-Supabase shape)
    - artifacts/hermes/src/index.ts (line 31-32 — `TELEGRAM_BOT_TOKEN` read site)
    - artifacts/hermes/package.json (template for fan-twin/package.json)
    - artifacts/hermes/build.mjs (template — copy verbatim)
    - artifacts/hermes/tsconfig.json (template — copy verbatim)
    - .planning/phases/02-twin-runtime-core/02-PATTERNS.md (A10 — env.ts rewrite; C1/C2 — fan-twin scaffold)
    - .planning/phases/02-twin-runtime-core/02-RESEARCH.md (## Pitfall (new for Phase 2): Pre-existing env schema requires Supabase)
  </read_first>
  <behavior>
    - Test 1 (env schema): With OPENAI_API_KEY, HMAC_CONVERSATION_SECRET (≥32 chars), TELEGRAM_BOT_TOKEN_LALA, TELEGRAM_BOT_TOKEN_FAN_TWIN, DATABASE_URL, GMI_API_KEY, SESSION_SECRET all set and SUPABASE_* unset, `envSchema.parse(process.env)` returns an env object without throwing.
    - Test 2 (env schema negative): When HMAC_CONVERSATION_SECRET is <32 chars, parse throws a ZodError mentioning `HMAC_CONVERSATION_SECRET`.
    - Test 3 (env schema negative): When OPENAI_API_KEY missing, parse throws ZodError naming `OPENAI_API_KEY`.
    - Test 4 (Hermes): `artifacts/hermes/src/index.ts` reads `process.env.TELEGRAM_BOT_TOKEN_LALA` (grep returns ≥1 match, returns 0 for `TELEGRAM_BOT_TOKEN[^_]`).
    - Test 5 (fan-twin scaffold): `pnpm --filter @workspace/fan-twin exec vitest run` exits 0 with "no test files found" (acceptable empty suite — keeps CI green).
  </behavior>
  <action>
    Rewrite `artifacts/api-server/src/config/env.ts` per PATTERNS A10: build a `z.object` schema with DATABASE_URL `.url()`, REDIS_URL `.url().optional()`, GMI_API_KEY `.min(1)`, OPENAI_API_KEY `.min(1)` (new — required), HMAC_CONVERSATION_SECRET `.min(32)` (new — required), TELEGRAM_BOT_TOKEN_LALA `.min(1)` (new — required), TELEGRAM_BOT_TOKEN_FAN_TWIN `.min(1)` (new — required), FOUNDER_TELEGRAM_CHAT_ID `.optional()` (new — recommended), SESSION_SECRET `.min(1)`, SENTRY_DSN `.url().optional()`, HELICONE_API_KEY `.optional()`, MODERATOR_PROVIDER `.optional()` (default "openai" in registry, not here). REMOVE all three SUPABASE_* fields. Export `env = envSchema.parse(process.env)`. Per D-02-07.

    In `artifacts/hermes/src/index.ts` rename `process.env.TELEGRAM_BOT_TOKEN` reads to `process.env.TELEGRAM_BOT_TOKEN_LALA` (line 32 area and any others — use grep first to find all sites). Update the throw message to name the new var. Per D-02-07.

    Update `.env.example` — remove SUPABASE_*, add OPENAI_API_KEY, HMAC_CONVERSATION_SECRET, TELEGRAM_BOT_TOKEN_LALA, TELEGRAM_BOT_TOKEN_FAN_TWIN, FOUNDER_TELEGRAM_CHAT_ID with example values and a comment explaining each. Note: TELEGRAM_BOT_TOKEN (old name) replaced.

    Create `artifacts/fan-twin/` directory with `package.json` (copy `artifacts/hermes/package.json`, rename `name` to `@workspace/fan-twin`; add deps placeholders but DO NOT run `pnpm add` yet — installs happen in plan 02-06 after Wave 0 legitimacy gate; for now use the same deps section minus what we don't have legitimacy for). Copy `tsconfig.json` and `build.mjs` verbatim from hermes. Create `vitest.config.ts` mirroring api-server vitest config (or hermes if one exists). Create stub `src/index.ts` that exports `export const FAN_TWIN_PLACEHOLDER = true` so vitest finds *something* to compile (real bot lives in 02-06).

    Add `"@workspace/fan-twin": "workspace:*"` to root `pnpm-workspace.yaml` packages list if pnpm-workspace.yaml uses a packages array (verify shape first).
  </action>
  <verify>
    <automated>node -e "process.env.OPENAI_API_KEY='x';process.env.HMAC_CONVERSATION_SECRET='x'.repeat(32);process.env.TELEGRAM_BOT_TOKEN_LALA='x';process.env.TELEGRAM_BOT_TOKEN_FAN_TWIN='x';process.env.DATABASE_URL='postgres://u:p@h:5/d';process.env.GMI_API_KEY='x';process.env.SESSION_SECRET='x';delete process.env.SUPABASE_URL;delete process.env.SUPABASE_ANON_KEY;delete process.env.SUPABASE_SERVICE_ROLE_KEY;require('./artifacts/api-server/dist/config/env.js')" && grep -c "TELEGRAM_BOT_TOKEN_LALA" artifacts/hermes/src/index.ts | awk '{exit ($1>=1)?0:1}' && ! grep -E "TELEGRAM_BOT_TOKEN[^_]" artifacts/hermes/src/index.ts && pnpm --filter @workspace/fan-twin exec tsc --noEmit</automated>
  </verify>
  <done>
    - api-server cold-starts when SUPABASE_* vars unset (verified via node -e env import smoke OR by running api-server with cleared env)
    - `grep -c TELEGRAM_BOT_TOKEN_LALA artifacts/hermes/src/index.ts` ≥ 1
    - `grep -E "TELEGRAM_BOT_TOKEN[^_]" artifacts/hermes/src/index.ts` returns nothing
    - `artifacts/fan-twin/package.json`, `tsconfig.json`, `build.mjs`, `vitest.config.ts`, `src/index.ts` exist
    - `pnpm --filter @workspace/fan-twin exec tsc --noEmit` exits 0
    - `.env.example` has zero SUPABASE_* lines and ≥5 new lines (OPENAI_API_KEY, HMAC_CONVERSATION_SECRET, TELEGRAM_BOT_TOKEN_LALA, TELEGRAM_BOT_TOKEN_FAN_TWIN, FOUNDER_TELEGRAM_CHAT_ID)
  </done>
  <acceptance_criteria>
    - Per D-02-07 token rename: source greps as specified above
    - Per Wave 0 unblock: api-server can boot in an environment with no Supabase secrets
    - Per PATTERNS S4: all new TS files use `.js` extension on relative imports
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reserve fan-twin port in artifact.toml + .replit + create Wave 0 RED test files</name>
  <files>artifact.toml, .replit, artifacts/api-server/src/__tests__/twin-chat.e2e.test.ts, artifacts/api-server/src/__tests__/hmac-conversation.test.ts, artifacts/api-server/src/__tests__/conversation-history.test.ts, artifacts/api-server/src/__tests__/moderation-l1.test.ts, artifacts/api-server/src/__tests__/moderation-l3.test.ts, artifacts/api-server/src/__tests__/disclosure-footer.test.ts, artifacts/api-server/src/__tests__/helpline-injection.test.ts, artifacts/api-server/src/__tests__/locale-detection.test.ts, artifacts/fan-twin/src/__tests__/webhook-ack.test.ts, artifacts/hermes/src/__tests__/persona-wizard.test.ts</files>
  <read_first>
    - artifact.toml (current shape — read first to understand port-allocation syntax)
    - .replit (current shape)
    - .planning/phases/02-twin-runtime-core/02-RESEARCH.md (## Validation Architecture → Wave 0 Gaps; Pitfall #9 atomic update)
    - .planning/phases/02-twin-runtime-core/02-PATTERNS.md (G1, G2 — test file analogs)
    - artifacts/api-server/src/__tests__/safety-audit.test.ts (vi.mock pattern — copy structure for new unit tests)
    - artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts (HTTP harness — copy structure for twin-chat.e2e.test.ts)
  </read_first>
  <action>
    Step 1 (port allocation, per D-02-06): Read `artifact.toml` and `.replit`. Confirm ports 8080 (api-server), 22333 (web), 3001 (admin) are taken. Hermes uses dynamic PORT — confirm. Reserve port 3002 for fan-twin by adding a fan-twin entry in `artifact.toml` mirroring the api-server/hermes entry shape (start command, build command, port=3002) AND adding the matching port mapping to `.replit`. Both files updated in the same commit (Pitfall #9). If 3002 is already taken, escalate via inline comment in CONTEXT.md and use the next free port.

    Step 2 (RED test files): Create ALL 10 test files listed in `<files>`. Each file starts with a `describe.skip` block OR `it.todo` placeholders that document the feature under test, the expected behavior, and the Phase 2 plan that will turn them GREEN. Use PATTERNS G1/G2 mocking shape: `vi.mock("@workspace/db", () => ({ db: { ... }, conversationMessagesTable: {} }))` for unit tests; HTTP harness from kyc-gate.e2e.test.ts for e2e. Do NOT implement actual assertions yet — those land in the respective feature plans (02-02 through 02-07). The point is that the file exists and CI can run `vitest run <file>` immediately when each feature task starts.

    Each test file body MUST include a top-of-file comment block: `// RED test for {REQ-ID} — will GREEN when plan 02-NN ships {feature}`. Example for `hmac-conversation.test.ts`: `// RED test for CHAT-03 — will GREEN when plan 02-02 ships api-server/src/lib/hmac-conversation.ts`.

    Test file mapping:
    - twin-chat.e2e.test.ts → CHAT-01, plan 02-03
    - hmac-conversation.test.ts → CHAT-03, plan 02-02
    - conversation-history.test.ts → CHAT-04, plan 02-02
    - moderation-l1.test.ts → MOD-01, plan 02-05
    - moderation-l3.test.ts → MOD-03, plan 02-05
    - disclosure-footer.test.ts → COMPLY-01, plan 02-03
    - helpline-injection.test.ts → COMPLY-02, plan 02-05
    - locale-detection.test.ts → I18N-02, plan 02-02
    - fan-twin webhook-ack.test.ts → CHAT-06, plan 02-06
    - hermes persona-wizard.test.ts → ONBOARD-01, plan 02-07

    Each file must use `it.todo("description")` so the suite RUNS but reports todos (not failures). This keeps `pnpm run test` green for Wave 0 → Wave 1 transition.
  </action>
  <verify>
    <automated>test -f artifact.toml && test -f .replit && grep -c "3002" artifact.toml | awk '{exit ($1>=1)?0:1}' && grep -c "3002" .replit | awk '{exit ($1>=1)?0:1}' && for f in artifacts/api-server/src/__tests__/twin-chat.e2e.test.ts artifacts/api-server/src/__tests__/hmac-conversation.test.ts artifacts/api-server/src/__tests__/conversation-history.test.ts artifacts/api-server/src/__tests__/moderation-l1.test.ts artifacts/api-server/src/__tests__/moderation-l3.test.ts artifacts/api-server/src/__tests__/disclosure-footer.test.ts artifacts/api-server/src/__tests__/helpline-injection.test.ts artifacts/api-server/src/__tests__/locale-detection.test.ts artifacts/fan-twin/src/__tests__/webhook-ack.test.ts artifacts/hermes/src/__tests__/persona-wizard.test.ts; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done && pnpm --filter @workspace/api-server exec vitest run --reporter=verbose 2>&1 | grep -E "todo|RED test for"</automated>
  </verify>
  <done>
    - Port 3002 (or alternative) appears in BOTH artifact.toml and .replit
    - All 10 test files exist
    - `pnpm --filter @workspace/api-server run test` exits 0 with `it.todo` entries reported
    - Each test file has the documented RED-test header comment with the matching REQ-ID and target plan
  </done>
  <acceptance_criteria>
    - Per Pitfall #9: atomic update of port mapping (verified by both files being modified in the same commit)
    - Per RESEARCH Wave 0 Gaps section: all 10 test files exist
    - Per PATTERNS S7: unit test files use the documented `vi.mock("@workspace/db", ...)` shape
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder → Replit Secrets | Founder rotates secrets manually; checkpoint enforces this |
| node process → env | `envSchema.parse(process.env)` is the trust gate at startup |
| npm registry → local install | Package legitimacy gate before any `pnpm add` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | npm packages @telegraf/session, i18next-http-middleware | mitigate | Task 0 blocking-human checkpoint — founder verifies maintainer + downloads on npmjs.com before install |
| T-02-01-02 | Info Disclosure | env vars in logs | mitigate | Zod schema rejects missing required secrets at startup; no env value ever interpolated into log strings (existing pino redact covers `authorization` header) |
| T-02-01-03 | Spoofing | TELEGRAM_BOT_TOKEN_LALA / TELEGRAM_BOT_TOKEN_FAN_TWIN reuse | mitigate | Two separate tokens enforced via env schema `.min(1)` on both — neither is optional |
| T-02-01-04 | Repudiation | port collision between artifacts | mitigate | atomic update of artifact.toml + .replit (Pitfall #9); founder rotation of port in both files |
| T-02-01-SC | Tampering | npm/pnpm installs (supply chain) | mitigate | Task 0 blocking-human legitimacy checkpoint per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `pnpm --filter @workspace/api-server exec tsc --noEmit` exits 0
- `pnpm --filter @workspace/hermes exec tsc --noEmit` exits 0
- `pnpm --filter @workspace/fan-twin exec tsc --noEmit` exits 0
- `pnpm --filter @workspace/api-server run test` exits 0 (todos reported, no failures)
- `grep -E "SUPABASE_(URL|ANON_KEY|SERVICE_ROLE_KEY)" artifacts/api-server/src/config/env.ts` returns nothing
- `grep "TELEGRAM_BOT_TOKEN_LALA" artifacts/hermes/src/index.ts` returns ≥1 line
</verification>

<success_criteria>
- api-server cold-starts in Replit without SUPABASE_* env vars set
- Hermes starts with TELEGRAM_BOT_TOKEN_LALA (old name no longer read)
- Fan-twin port reserved in artifact.toml + .replit
- 10 RED test files exist and run as `it.todo` (no failures, no false GREENs)
- @telegraf/session approved by founder with confirmed version (i18next-http-middleware install dropped per D-02-14)
- Founder confirms Replit Secrets panel rotation (delete SUPABASE_*, add 5 new vars)
</success_criteria>

<output>
Create `.planning/phases/02-twin-runtime-core/02-01-SUMMARY.md` when done documenting:
- Confirmed package versions (from Task 0 founder approval)
- Final fan-twin port (3002 or alternative)
- Files created/modified
- Any deviations from CONTEXT.md decisions
</output>
