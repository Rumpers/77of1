---
phase: 01-baseline-repair
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts
  - artifacts/api-server/src/lib/kyc.ts
  - artifacts/api-server/src/routes/twin.ts
  - artifacts/api-server/src/routes/health.ts
  - artifacts/api-server/src/routes/kyc.ts
  - artifacts/api-server/src/middlewares/require-creator-auth.ts
  - artifacts/api-server/src/middlewares/require-fan-access.ts
autonomous: false
requirements: [KYC-01, KYC-02, INFRA-01, INFRA-02]
must_haves:
  truths:
    - "A failing end-to-end test exists at the start of this plan (Task 0) that pins POST /api/twin/chat returning 423 for unsigned creators — and turns GREEN by Task 2"
    - "POST /api/twin/chat returns HTTP 423 with code 'KYC_UNSIGNED' for any creator whose creator_kyc.status is not 'signed' (null/pending/rejected/missing-row all blocked)"
    - "POST /api/twin/chat returns 200 (canned stub response) only when creator_kyc.status === 'signed'"
    - "GET /api/health/db reports {status:'ok'} after a real Drizzle Pool ping (no Supabase client)"
    - "isKycSigned() exists in lib/kyc.ts and replaces the legacy isKycComplete()"
    - "The SignWell document template, when signed by a creator via /api/kyc/signwell-webhook, writes creator_kyc.status='signed' (not the legacy 'rights_signed' or 'complete')"
    - "The SignWell template body includes a 'VOICE SYNTHESIS AUTHORIZATION' section naming scope, duration, and revocability as a signed line item (KYC-02 — verified via founder checkpoint)"
    - "The full api-server vitest suite passes end-to-end after the migration (no regressions from Supabase removal)"
  artifacts:
    - path: "artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts"
      provides: "End-to-end test: POST /api/twin/chat with handle pointing at a creator whose kyc row has status='pending' returns 423; with status='signed' it does NOT return 423; nonexistent handle returns 404"
    - path: "artifacts/api-server/src/lib/kyc.ts"
      provides: "isKycSigned, getKycRow, initiateSignwellSigning — all Drizzle-backed; KycStatus = 'pending'|'signed'|'rejected'"
      exports: ["isKycSigned", "getKycRow", "initiateSignwellSigning", "KycStatus", "hashIpForKyc", "extractIp"]
    - path: "artifacts/api-server/src/routes/twin.ts"
      provides: "POST /api/twin/chat with KYC gate at handler entry"
      contains: "isKycSigned"
    - path: "artifacts/api-server/src/routes/health.ts"
      provides: "GET /api/health/db pings Drizzle Pool via pool.query('SELECT 1')"
    - path: "artifacts/api-server/src/middlewares/require-creator-auth.ts"
      provides: "Replit-identity-based creator auth (replaces Supabase JWT)"
  key_links:
    - from: "artifacts/api-server/src/routes/twin.ts"
      to: "artifacts/api-server/src/lib/kyc.ts"
      via: "import { isKycSigned } from '../lib/kyc.js'"
      pattern: "isKycSigned\\("
    - from: "artifacts/api-server/src/lib/kyc.ts"
      to: "@workspace/db"
      via: "import { db, creatorKycTable } from '@workspace/db'"
      pattern: "creatorKycTable"
    - from: "artifacts/api-server/src/routes/health.ts"
      to: "lib/db pool"
      via: "import { pool } from '@workspace/db'; pool.query('SELECT 1')"
      pattern: "pool\\.query"
---

<objective>
Deliver the user-visible vertical slice that the Phase 1 user story turns on: a creator's twin route refuses to engage until that creator's KYC is signed, the SignWell document template explicitly names voice synthesis scope/duration/revocability as a signed line item, the health endpoint reports DB liveness through Drizzle (not Supabase), and the api-server's creator-auth middleware no longer depends on Supabase JWT. This plan owns its own RED→GREEN cycle: Task 0 writes the failing E2E test against the now-live Drizzle schema (from Plan 01-01); Task 2 makes it pass.

Purpose: KYC-01 and KYC-02 are legal hard gates — the twin must never serve fan traffic for an unsigned creator, and the signed agreement must explicitly authorize voice synthesis. INFRA-01 requires the api-server's port-8080 health probe to report status via the live data layer. INFRA-02 requires removing Supabase from this api-server's KYC, twin, health, and creator-auth code paths. Locating the failing test inside this plan (rather than Plan 01-01) keeps the red/green cycle inside a single executor context.

Output: api-server's KYC + twin + health + creator-auth surfaces fully Drizzle-backed; SignWell template updated and webhook writes the new `'signed'` status; kyc-gate E2E test green; full api-server vitest suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-baseline-repair/01-CONTEXT.md
@.planning/phases/01-baseline-repair/01-RESEARCH.md
@.planning/phases/01-baseline-repair/01-PATTERNS.md
@.planning/phases/01-baseline-repair/01-01-SUMMARY.md
@artifacts/api-server/src/lib/kyc.ts
@artifacts/api-server/src/lib/auth.ts
@artifacts/api-server/src/lib/supabase.ts
@artifacts/api-server/src/routes/twin.ts
@artifacts/api-server/src/routes/health.ts
@artifacts/api-server/src/routes/kyc.ts
@artifacts/api-server/src/middlewares/require-creator-auth.ts
@artifacts/api-server/src/middlewares/require-fan-access.ts

<interfaces>
<!-- After Plan 01, these are live in @workspace/db: -->
- db: NodePgDatabase<typeof schema>  (singleton)
- pool: pg.Pool                       (singleton — use for raw SELECT 1 in health)
- creatorsTable, creatorKycTable, creatorConfigTable
- KycStatus union ('pending'|'signed'|'rejected')   [DEFINED IN THIS PLAN — exported from lib/kyc.ts, NOT the schema]
- twinsTable, conversationMessagesTable, safetyAuditLogTable
- eq, and, inArray (drizzle-orm operators)

<!-- Replit auth helper (already in api-server, do NOT modify): -->
From artifacts/api-server/src/lib/auth.ts:
- export function getReplitUser(req: Request): { id: string; name: string; roles: string } | null

<!-- Express locals (preserve declaration in require-creator-auth.ts): -->
declare global { namespace Express { interface Locals {
  authUserId?: string;
  creatorId?: string;
} } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Write the failing E2E test for the KYC gate (moved from Plan 01-01 — Drizzle-seeded against the live schema)</name>
  <files>artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts</files>
  <read_first>
    - artifacts/api-server/src/routes/twin.ts (confirm current handler has NO KYC gate; test MUST fail today)
    - artifacts/api-server/src/index.ts or app.ts (confirm Express app factory export pattern for supertest)
    - artifacts/api-server/package.json (confirm vitest + supertest availability)
    - .planning/phases/01-baseline-repair/01-CONTEXT.md decisions D-05, D-06
    - .planning/phases/01-baseline-repair/01-01-SUMMARY.md (confirms schema is live; creatorsTable + creatorKycTable exported from @workspace/db)
    - lib/db/src/schema/index.ts (use Drizzle table types for seed data)
  </read_first>
  <behavior>
    - Test 1: POST /api/twin/chat with body `{ handle: "test-pending", message: "hi" }` returns HTTP 423 with JSON body `{ error: ..., code: "KYC_UNSIGNED" }` when the seeded creator row has `creator_kyc.status = 'pending'`. (FAILS today — handler has no gate, returns 200.)
    - Test 2: POST /api/twin/chat with body `{ handle: "test-signed", message: "hi" }` does NOT return 423 when the seeded creator row has `creator_kyc.status = 'signed'`. (Will PASS after Task 2; today the handler returns 200 unconditionally, which is considered not-423 so this assertion is already true.)
    - Test 3: POST /api/twin/chat with `{ handle: "nonexistent", message: "hi" }` returns HTTP 404 with `{ error: "Creator not found" }`. (FAILS today — handler returns 200 for any handle.)
    - Seed/teardown go through Drizzle (`db.insert(creatorsTable).values([...]).onConflictDoNothing()` and `db.insert(creatorKycTable).values([...])`), not raw `pool.query`, because Plan 01-01 already pushed the schema.
    - Cleanup uses `db.delete(creatorKycTable).where(inArray(creatorKycTable.creatorId, [pendingId, signedId]))` followed by `db.delete(creatorsTable).where(inArray(creatorsTable.handle, ['test-pending','test-signed']))`.
  </behavior>
  <action>Create the vitest test file `artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts` using supertest against the Express app factory. Import `db`, `creatorsTable`, `creatorKycTable` from `@workspace/db` and `inArray` from `drizzle-orm`. In `beforeAll`, generate two UUIDs (`crypto.randomUUID()`), insert two `creators` rows with handles `test-pending` and `test-signed` via `db.insert(creatorsTable).values([{ id: pendingId, handle: 'test-pending', displayName: 'Test Pending' }, { id: signedId, handle: 'test-signed', displayName: 'Test Signed' }]).onConflictDoNothing()`, then insert matching `creator_kyc` rows with `status='pending'` and `status='signed'`. In `afterAll`, delete the kyc rows then the creator rows via Drizzle. Wrap the three assertions in a `describe("Phase 1 KYC gate E2E", ...)` block. Tests 1 and 3 MUST fail on `vitest run` today against the current handler (which always returns the canned 200 stub) — the failure output names the missing 423/404 status as the assertion gap. Do NOT modify `twin.ts` in this task — the test is the contract Task 2 must satisfy.</action>
  <verify>
    <automated>pnpm --filter @workspace/api-server exec vitest run src/__tests__/kyc-gate.e2e.test.ts 2>&1 | tee /tmp/p2t0.txt; grep -E "(FAIL|fail|failed)" /tmp/p2t0.txt && grep -q 'from "@workspace/db"' artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts && grep -q 'db.insert(creatorsTable)' artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts</automated>
  </verify>
  <done>Test file exists, executes under vitest, Test 1 and Test 3 FAIL with current handler (proving the gate is missing); seed/teardown uses Drizzle against the live Plan-01 schema; the failure output names the missing 423/404 status as the assertion gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite lib/kyc.ts on Drizzle — rename isKycComplete → isKycSigned, collapse status enum, keep SignWell flow</name>
  <files>artifacts/api-server/src/lib/kyc.ts</files>
  <read_first>
    - artifacts/api-server/src/lib/kyc.ts (current — full file, including SignWell fetch logic lines 71–133, hashIpForKyc and extractIp at 136–145)
    - artifacts/api-server/src/lib/supabase.ts (do NOT delete here; understand which surface is used)
    - .planning/phases/01-baseline-repair/01-PATTERNS.md section "artifacts/api-server/src/lib/kyc.ts"
    - .planning/phases/01-baseline-repair/01-RESEARCH.md Pattern 4 (isKycSigned implementation)
    - lib/db/src/schema/index.ts (now contains creatorKycTable, kycStatusEnum from Plan 01)
  </read_first>
  <behavior>
    - `isKycSigned("uuid-of-pending-creator")` resolves to `false` (per D-05/D-06 strict positive)
    - `isKycSigned("uuid-of-signed-creator")` resolves to `true`
    - `isKycSigned("uuid-with-no-kyc-row")` resolves to `false` (null bypass blocked — Pitfall #4)
    - `isKycSigned("uuid-of-rejected-creator")` resolves to `false`
    - `getKycRow(creatorId)` returns the full row via Drizzle `db.select().from(creatorKycTable).where(eq(...)).limit(1)`
    - `initiateSignwellSigning(creatorId, email)` keeps the existing SignWell fetch logic (template-id env var, request body, error handling) but writes the final row update via Drizzle: status stays `'pending'` after initiation; the webhook task changes status to `'signed'`
    - The exported type `KycStatus` is the union `'pending' | 'signed' | 'rejected'` (collapsed from 8 states)
    - `hashIpForKyc` and `extractIp` are preserved verbatim (no Supabase dependency)
    - No `getSupabase()` import remains; only `@workspace/db` and `drizzle-orm` imports for DB access
  </behavior>
  <action>Replace the existing `import { getSupabase } from "./supabase.js"` with `import { db } from "@workspace/db"`, `import { creatorKycTable } from "@workspace/db"`, and `import { eq } from "drizzle-orm"`. Collapse the legacy 9-value `KycStatus` union to `"pending" | "signed" | "rejected"` per D-05. Rename `isKycComplete` to `isKycSigned` per D-06 and change the predicate to strict `row?.status === "signed"`. Rewrite `getKycRow` to use the Drizzle pattern shown in PATTERNS.md "artifacts/api-server/src/lib/kyc.ts". In `initiateSignwellSigning`, keep all SignWell fetch logic intact and only swap the final Supabase `.update()` call for the Drizzle equivalent: `db.update(creatorKycTable).set({ signwellDocId, signwellSigningUrl, status: "pending", updatedAt: new Date() }).where(eq(creatorKycTable.creatorId, creatorId))`. Do not change `hashIpForKyc`/`extractIp`. Update every caller within `kyc.ts` itself to use the new function name. Cross-file caller updates are handled in subsequent tasks of this plan.</action>
  <verify>
    <automated>pnpm --filter @workspace/api-server exec tsc --noEmit 2>&1 | tee /tmp/p2t1-tsc.txt; ! grep -E 'isKycComplete|getSupabase' artifacts/api-server/src/lib/kyc.ts && grep -q 'export async function isKycSigned' artifacts/api-server/src/lib/kyc.ts && grep -q 'from "@workspace/db"' artifacts/api-server/src/lib/kyc.ts && grep -q '"pending" | "signed" | "rejected"' artifacts/api-server/src/lib/kyc.ts</automated>
  </verify>
  <done>lib/kyc.ts is Supabase-free, exports isKycSigned with strict-positive predicate, types collapsed to 3-value union, SignWell flow preserved, api-server typecheck passes (modulo callers that get updated in subsequent tasks of this plan).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add KYC gate to POST /api/twin/chat; rewrite /api/health/db on Drizzle Pool; signwell-webhook writes status='signed'; /api/kyc/upload-url stubbed 503; run full vitest suite</name>
  <files>artifacts/api-server/src/routes/twin.ts, artifacts/api-server/src/routes/health.ts, artifacts/api-server/src/routes/kyc.ts</files>
  <read_first>
    - artifacts/api-server/src/routes/twin.ts (current handler — confirm no gate exists; preserve canned-stub response logic per D-05 / PATTERNS.md)
    - artifacts/api-server/src/routes/health.ts (lines 17–142 — keep Redis and GMI checks unchanged; replace only `GET /api/health/db`)
    - artifacts/api-server/src/routes/kyc.ts (signwell-webhook handler — find the `document.completed` branch and the status it writes; find the upload-url handler that uses Supabase Storage)
    - artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts (the contract written in Task 0 — must turn GREEN by the end of this task)
    - .planning/phases/01-baseline-repair/01-PATTERNS.md sections "artifacts/api-server/src/routes/twin.ts" and "artifacts/api-server/src/routes/health.ts"
    - .planning/phases/01-baseline-repair/01-RESEARCH.md Pitfall 5 (Supabase Storage signed URL removal blocks KYC upload flow)
  </read_first>
  <behavior>
    - twin.ts: When `req.body.handle` is provided, the handler resolves the creator via `db.select({ id: creatorsTable.id }).from(creatorsTable).where(eq(creatorsTable.handle, String(handle))).limit(1)`. If no creator row → respond `404 { error: "Creator not found" }`. If creator exists, call `isKycSigned(creator.id)`. If false → respond `423 { error: "Creator onboarding not complete", code: "KYC_UNSIGNED" }`. If true → continue to the existing canned stub response logic unchanged.
    - health.ts: `GET /api/health/db` performs `await pool.query("SELECT 1")` from `@workspace/db`'s exported pool. Records `latencyMs`. On success returns `200 { status: "ok", latencyMs }`. On failure returns `503 { status: "error", latencyMs, error }`. No Supabase dynamic import remains.
    - kyc.ts: The signwell-webhook handler, when it receives the `document.completed` event from SignWell, writes `status: "signed"` to `creator_kyc` via Drizzle — NOT the legacy `'rights_signed'` or `'complete'`.
    - kyc.ts: The `/api/kyc/upload-url` route is stubbed to return `503 { error: "Upload temporarily unavailable", code: "OBJECT_STORAGE_PENDING" }` with a comment marker referencing "Phase 3: Replit Object Storage migration".
    - The Task-0 E2E test (`src/__tests__/kyc-gate.e2e.test.ts`) now PASSES (Test 1 returns 423, Test 2 does not return 423, Test 3 returns 404).
    - The FULL api-server vitest suite runs green — no regressions from the Supabase removal in kyc.ts/twin.ts/health.ts/kyc.ts.
  </behavior>
  <action>In `routes/twin.ts`: add imports `import { db } from "@workspace/db"; import { creatorsTable } from "@workspace/db"; import { eq } from "drizzle-orm"; import { isKycSigned } from "../lib/kyc.js";`. Insert the resolution-then-gate block AFTER existing body validation and BEFORE the canned stub response logic, exactly per PATTERNS.md "KYC gate insertion pattern". Do not delete or restructure the canned response. In `routes/health.ts`: delete the dynamic `await import("@supabase/supabase-js")` block in the `/health/db` handler; add `import { pool } from "@workspace/db"` at the top; replace the handler body with the Drizzle Pool ping pattern from PATTERNS.md "artifacts/api-server/src/routes/health.ts". In `routes/kyc.ts`: find the SignWell webhook handler's status-write branch and change the literal status value from whatever legacy enum value is currently used to the literal string `"signed"`; switch the underlying DB write from `supabase.from("creator_kyc").update(...)` to `db.update(creatorKycTable).set({ status: "signed", personalityRightsSignedAt: new Date(), updatedAt: new Date() }).where(eq(creatorKycTable.creatorId, creatorId))`. Find the `/api/kyc/upload-url` route handler that calls `supabase.storage.from("kyc-docs").createSignedUploadUrl(...)`; replace the entire handler body with the 503 stub above, keeping the route definition and any auth middleware unchanged. Do NOT touch other routes in kyc.ts beyond signwell-webhook + upload-url in this plan. After the code changes land, run the full api-server vitest suite (`pnpm --filter @workspace/api-server run test`) and fix any regressions caused by the Supabase removal (e.g., other tests that imported from `./supabase.js`).</action>
  <verify>
    <automated>pnpm --filter @workspace/api-server exec tsc --noEmit && pnpm --filter @workspace/api-server exec vitest run src/__tests__/kyc-gate.e2e.test.ts 2>&1 | tee /tmp/p2t2-e2e.txt && grep -E '(✓|passed|PASS).*kyc-gate' /tmp/p2t2-e2e.txt && ! grep -E 'FAIL|failed' /tmp/p2t2-e2e.txt && grep -q 'pool.query' artifacts/api-server/src/routes/health.ts && grep -q 'isKycSigned' artifacts/api-server/src/routes/twin.ts && grep -q '"signed"' artifacts/api-server/src/routes/kyc.ts && grep -q 'OBJECT_STORAGE_PENDING' artifacts/api-server/src/routes/kyc.ts && ! grep -E '@supabase/supabase-js|getSupabase' artifacts/api-server/src/routes/twin.ts artifacts/api-server/src/routes/health.ts && pnpm --filter @workspace/api-server run test 2>&1 | tee /tmp/p2t2-suite.txt && ! grep -E '^FAIL\b' /tmp/p2t2-suite.txt</automated>
  </verify>
  <done>Task-0 E2E test passes; twin.ts has the KYC gate; health.ts pings via Drizzle Pool; signwell-webhook writes `'signed'`; upload-url is 503-stubbed; api-server typecheck passes; full api-server vitest suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace Supabase JWT in require-creator-auth.ts with Replit identity; stub require-fan-access.ts to next()</name>
  <files>artifacts/api-server/src/middlewares/require-creator-auth.ts, artifacts/api-server/src/middlewares/require-fan-access.ts</files>
  <read_first>
    - artifacts/api-server/src/middlewares/require-creator-auth.ts (current — full file; preserve `declare global { namespace Express { interface Locals { ... } } }` block)
    - artifacts/api-server/src/middlewares/require-fan-access.ts (current — uses Supabase JWT + fan_accounts + fan_subscriptions tables that are NOT in Phase 1 schema)
    - artifacts/api-server/src/lib/auth.ts (lines 27–39: getReplitUser signature)
    - artifacts/api-server/src/routes/kyc.ts (lines 36–56: resolveCreatorId — the pattern to mirror)
    - .planning/phases/01-baseline-repair/01-PATTERNS.md section "artifacts/api-server/src/middlewares/require-creator-auth.ts"
    - .planning/phases/01-baseline-repair/01-RESEARCH.md Pitfall #2 (Supabase auth JWT removed with no replacement)
  </read_first>
  <behavior>
    - require-creator-auth.ts: middleware calls `getReplitUser(req)`. If null → respond `401 { error: "Creator auth required" }`. Otherwise it looks up `creators` via Drizzle `eq(creatorsTable.replitUserId, user.id)`. If no creator → respond `403 { error: "No creator account linked to this user" }`. On success sets `res.locals.authUserId = user.id; res.locals.creatorId = creator.id; next()`.
    - require-fan-access.ts: middleware calls `next()` immediately with a `// PHASE-1 STUB: fan_accounts + fan_subscriptions tables not in Phase 1 schema; restore real check in Phase 2` comment. Existing exported function signature MUST be preserved so all callers continue to compile.
    - Both files have zero `@supabase/supabase-js` imports.
    - The `declare global { namespace Express { interface Locals { ... } } }` block in require-creator-auth.ts is preserved verbatim.
  </behavior>
  <action>For require-creator-auth.ts: replace the Supabase JWT logic with the Replit-identity pattern documented verbatim in PATTERNS.md (`getReplitUser` → Drizzle `creators` lookup by `replitUserId` → set `res.locals`). Keep the global Express.Locals augmentation block. Remove all Supabase imports. For require-fan-access.ts: keep the function signature exactly the same; replace the body with `next();` and a one-line comment block explaining the stub and pointing to Phase 2. Remove all Supabase imports from both files. Do not introduce new packages.</action>
  <verify>
    <automated>pnpm --filter @workspace/api-server exec tsc --noEmit && ! grep -E '@supabase/supabase-js' artifacts/api-server/src/middlewares/require-creator-auth.ts artifacts/api-server/src/middlewares/require-fan-access.ts && grep -q 'getReplitUser' artifacts/api-server/src/middlewares/require-creator-auth.ts && grep -q 'replitUserId' artifacts/api-server/src/middlewares/require-creator-auth.ts && grep -q 'PHASE-1 STUB' artifacts/api-server/src/middlewares/require-fan-access.ts && grep -q 'declare global' artifacts/api-server/src/middlewares/require-creator-auth.ts</automated>
  </verify>
  <done>Creator auth runs through Replit identity headers + Drizzle; fan-access middleware is a documented stub; both files Supabase-free; typecheck passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Founder updates SignWell template body with Voice Synthesis Authorization section (KYC-02 / D-07)</name>
  <what-built>The signwell-webhook handler now writes `status='signed'` via Drizzle (Task 2). What remains is the SignWell document template itself — its body lives in the SignWell dashboard, not in the repo. KYC-02 requires the signed agreement to explicitly name voice synthesis scope, duration, and revocability as a signed line item. This is a manual founder action with no programmatic API.</what-built>
  <how-to-verify>
    1. Open the SignWell dashboard → navigate to the template referenced by `SIGNWELL_TEMPLATE_ID` (the env var consumed by `initiateSignwellSigning` in lib/kyc.ts).
    2. Add (or replace) a section titled exactly `VOICE SYNTHESIS AUTHORIZATION` with the body documented in `.planning/phases/01-baseline-repair/01-RESEARCH.md` "KYC Agreement Template (KYC-02)" — covers scope (non-exclusive, revocable license), duration (term of Service Agreement, terminable on written withdrawal), revocability (via Lala bot `/revoke`, 48h deletion SLA), and scope limitation (no explicit/adult/intimate content).
    3. Save the template. Note the new template version/ID.
    4. Open `https://app.signwell.com/api/v1/templates/{SIGNWELL_TEMPLATE_ID}` via curl with the SignWell API key to confirm the response JSON includes a field referencing "VOICE SYNTHESIS AUTHORIZATION" (or open the dashboard preview and confirm visually).
    5. Reply with: `approved — template body verified, version: <version-id>`.
  </how-to-verify>
  <files>(no repo files modified — SignWell dashboard configuration; optionally `.planning/phases/01-baseline-repair/notes/signwell-template-status.md` for the deferred case)</files>
  <action>This is a manual founder action: open the SignWell dashboard, edit the document template referenced by SIGNWELL_TEMPLATE_ID, add the VOICE SYNTHESIS AUTHORIZATION section per the body documented in 01-RESEARCH.md "KYC Agreement Template (KYC-02)", save the new template version, and reply with the template version ID. If no SignWell account exists yet, record the deferral in `.planning/phases/01-baseline-repair/notes/signwell-template-status.md` and reply `not-applicable: no SignWell account yet`.</action>
  <verify>
    <automated>echo "Awaiting founder approval: SignWell template must include 'VOICE SYNTHESIS AUTHORIZATION' section (KYC-02 / D-07) before Phase 1 closes"</automated>
  </verify>
  <done>Founder has confirmed the SignWell template body includes the Voice Synthesis Authorization clause naming scope, duration, and revocability, OR explicitly deferred the action with a tracked note.</done>
  <resume-signal>Reply `approved — template body verified, version: <id>` to continue. Reply `not-applicable: no SignWell account yet` if Phase 1 is running pre-account-provisioning; in that case the planner accepts a deferred-with-evidence note recorded in `.planning/phases/01-baseline-repair/notes/signwell-template-status.md`. Any other reply aborts the slice.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → POST /api/twin/chat | Untrusted fan input crosses here; must be gated on creator KYC before any LLM/DB work |
| SignWell webhook → POST /api/kyc/signwell-webhook | External webhook can trigger DB writes; signature verification must remain in place |
| Replit identity headers → require-creator-auth | Headers are set by Replit's reverse proxy; trusted only inside Replit-served traffic |
| anonymous fan → require-fan-access (stub) | Phase-1 stub passes everyone through — acceptable because no fan-facing route ships until Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | POST /api/twin/chat with creator whose KYC is unsigned/missing | mitigate | Task 2 inserts strict positive `isKycSigned(creator.id) === true` check; null/pending/rejected all return 423 (KYC-01, Pitfall #4); Task 0 pins this behavior with an E2E test that MUST be RED before Task 2 and GREEN after |
| T-02-02 | Spoofing | Bypass require-creator-auth by spoofing Replit headers | accept | Replit's reverse proxy is the trust boundary for these headers; behind it spoofing requires Replit infra compromise — acceptable for Phase 1 (RESEARCH Open Q #1 resolution; Phase 2+ adds real JWT) |
| T-02-03 | Tampering | SignWell webhook forges `document.completed` event to mark a creator signed | mitigate | The existing SignWell webhook signature verification in kyc.ts (HMAC) is preserved; Task 2 only changes the status value written, not the verification step. Acceptance must confirm the verification call remains in the handler. |
| T-02-04 | Repudiation | Creator denies signing the voice-synthesis clause | mitigate | Task 4 founder checkpoint ensures the SignWell template body explicitly includes "VOICE SYNTHESIS AUTHORIZATION" — the signed PDF is the audit record |
| T-02-05 | Information Disclosure | /api/kyc/upload-url leaks an Object-Storage URL in the 503 stub | mitigate | Task 2 stubs the handler to return only `{ error, code }` — no URL, no internal path |
| T-02-06 | Denial of Service | /api/health/db is hit by an unauthenticated scanner | mitigate | health.ts already requires `requireHealthSecret(req, res)` per the existing pattern — Task 2 preserves this check; Pool ping is a single `SELECT 1` with negligible cost |
| T-02-SC | Tampering | npm/pnpm installs | accept | No new packages added in Plan 02; legitimacy gate not applicable per RESEARCH.md "Package Legitimacy Audit" |
</threat_model>

<verification>
- `pnpm --filter @workspace/api-server exec tsc --noEmit` exits 0
- `pnpm --filter @workspace/api-server run test` exits 0 with kyc-gate.e2e tests in the passing set (Task 2 already gates on this)
- `grep -RE '@supabase/supabase-js' artifacts/api-server/src/lib/kyc.ts artifacts/api-server/src/routes/twin.ts artifacts/api-server/src/routes/health.ts artifacts/api-server/src/middlewares/` returns nothing
- SignWell template body is updated (founder checkpoint Task 4)
- `/api/kyc/upload-url` returns 503 with `OBJECT_STORAGE_PENDING`
</verification>

<success_criteria>
The Phase-1 user-visible behavior holds end-to-end: a fan request to a creator's twin URL gets refused with HTTP 423 unless that creator has actively signed (status='signed'); the signed document explicitly authorizes voice synthesis with named scope/duration/revocability; the api-server health endpoint reports DB liveness via Drizzle; api-server's creator-auth middleware no longer touches Supabase. The failing E2E test written in Task 0 turns GREEN in Task 2, and the full api-server vitest suite is green.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-repair/01-02-SUMMARY.md` when done.
</output>
</content>
</invoke>