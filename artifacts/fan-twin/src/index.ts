// fan-twin — public-facing AI digital twin Telegram bot.
// Scaffold only — real bot entry lands in plan 02-06 after Wave 0 legitimacy gate.
// Keeping a placeholder export so tsc/vitest find at least one symbol to compile.
export const FAN_TWIN_PLACEHOLDER = true;
