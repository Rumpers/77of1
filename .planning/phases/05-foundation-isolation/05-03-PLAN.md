---
phase: 05-foundation-isolation
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - artifacts/web/package.json
  - artifacts/web/src/main.tsx
  - artifacts/web/vite.config.ts
  - artifacts/web/index.html
autonomous: true
user_setup: []
requirements: [MKT-15, MKT-16, MKT-17]
must_haves:
  truths:
    - "Inter and Noto Sans JP are self-hosted via Fontsource; the Google Fonts CDN <link> tags are removed from index.html"
    - "Noto Sans JP loads with font-display: swap; on slow 3G text shows a system fallback immediately with no invisible-text period"
    - "index.html carries a <link rel=preload> for the Noto Sans JP 400-weight latin woff2 at a path that resolves in the production build (stable, unhashed filename)"
    - "index.html carries real marketing title, meta description, all og:* and twitter:card tags, and four static hreflang link tags — visible to crawlers without JS execution"
    - "The production build emits the preloaded woff2 at the exact path referenced by the preload tag"
  artifacts:
    - path: "artifacts/web/index.html"
      provides: "Marketing meta/og/twitter/hreflang head + stable-path font preload; Google Fonts CDN removed"
      contains: "og:image"
    - path: "artifacts/web/src/main.tsx"
      provides: "Fontsource Inter + Noto Sans JP imports after ./instrument, before ./index.css"
      contains: "@fontsource-variable"
    - path: "artifacts/web/vite.config.ts"
      provides: "rollupOptions.output.assetFileNames emitting woff2 at stable assets/fonts/[name] path"
      contains: "assetFileNames"
    - path: "artifacts/web/package.json"
      provides: "@fontsource-variable/inter + @fontsource-variable/noto-sans-jp dependencies"
      contains: "@fontsource-variable"
  key_links:
    - from: "artifacts/web/index.html preload link"
      to: "dist/public/assets/fonts/noto-sans-jp-latin-wght-normal.woff2"
      via: "vite.config.ts assetFileNames emits the woff2 at the stable path the preload href references"
      pattern: "assets/fonts/noto-sans-jp-latin"
    - from: "artifacts/web/index.html og:image"
      to: "public/og-marketing.png"
      via: "og:image and twitter:image reference https://lala.la/og-marketing.png (file committed in Plan 02)"
      pattern: "og-marketing.png"
---

<objective>
Wire self-hosted fonts and lock the static SEO head. This is the integration plan: it
consumes Plan 02's committed `og-marketing.png`, replaces the Google Fonts CDN with
Fontsource, and resolves the font-preload cross-document tension so BOTH the
locked-decision preload AND the Vite production build succeed.

Purpose: MKT-15 requires Noto Sans JP with `font-display: swap` and (per CONTEXT D-05
Claude's Discretion, NON-NEGOTIABLE) a `<link rel="preload">` for the 400-weight woff2.
MKT-16 requires real marketing meta + og:image. MKT-17 requires static hreflang tags.
A naive preload href into node_modules breaks in the production dist; the resolution is
to configure `vite.config.ts` to emit the preloaded woff2 at a STABLE (unhashed) path and
reference that exact path in the index.html preload tag. The preload is retained, not dropped.

Output: Fontsource packages installed and imported in `main.tsx`; `vite.config.ts` stable
woff2 filenames; `index.html` with marketing meta/og/twitter/hreflang + the working preload;
Google Fonts CDN removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-foundation-isolation/05-UI-SPEC.md
@.planning/phases/05-foundation-isolation/05-PATTERNS.md
@.planning/phases/05-foundation-isolation/05-RESEARCH.md

<cross_doc_resolution>
RESEARCH.md and the UI-SPEC's first-draft preload approach said "omit the static preload
or use a node_modules path." That is OVERRIDDEN by the locked CONTEXT D-05 decision: the
`<link rel="preload">` for the Noto Sans JP 400-weight woff2 is NON-NEGOTIABLE alongside
`font-display: swap`. The production-safe resolution (RESEARCH § Pattern 3 "If the planner
wants to include the preload anyway", lines 335-345, and PATTERNS.md vite.config.ts addition
lines 451-488) is the stable-filename approach: configure `vite.config.ts`
`build.rollupOptions.output.assetFileNames` to emit woff2 files at `assets/fonts/[name][extname]`
(unhashed), then reference `/assets/fonts/noto-sans-jp-latin-wght-normal.woff2` in the preload.
This plan MUST implement the stable-filename approach. Do NOT use a node_modules href.
Do NOT drop the preload.
</cross_doc_resolution>

<interfaces>
<!-- Fontsource file names (VERIFIED via unpkg in RESEARCH.md lines 321-327): -->
<!--   @fontsource-variable/noto-sans-jp -> files/noto-sans-jp-latin-wght-normal.woff2 (latin 400, ~89KB) -->
<!--   @fontsource-variable/inter -> files/inter-latin-wght-normal.woff2 -->
<!-- main.tsx (6 lines): import "./instrument" is line 1 and must stay first. -->
<!-- vite.config.ts: build block lines 57-60 has outDir + emptyOutDir, no rollupOptions yet. -->
<!-- index.html (24 lines): "7of1" placeholder meta lines 6,9,10,13,14; three Google Fonts -->
<!--   CDN <link> tags lines 16-18; favicon link in-file. -->
<!-- Exact meta/og/twitter/hreflang strings LOCKED in 05-UI-SPEC.md Copywriting Contract -->
<!--   (lines 288-308) and PATTERNS.md target head block (lines 73-95). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Fontsource packages and import them in main.tsx (MKT-15)</name>
  <files>artifacts/web/package.json, artifacts/web/src/main.tsx</files>
  <read_first>
    - artifacts/web/src/main.tsx (current 6 lines; ./instrument must remain line 1)
    - artifacts/web/package.json (confirm no @fontsource-variable present; confirm pnpm-only)
    - .planning/phases/05-foundation-isolation/05-PATTERNS.md § "artifacts/web/src/main.tsx" (lines 101-127) and § "Font Import Order" (lines 562-573)
    - .planning/phases/05-foundation-isolation/05-RESEARCH.md § "Package Legitimacy Audit" (lines 117-128) — both packages slopcheck [OK], no postinstall
  </read_first>
  <action>
    Install with pnpm ONLY (npm/yarn blocked by preinstall hook):
    `pnpm --filter @workspace/web add @fontsource-variable/inter @fontsource-variable/noto-sans-jp`.
    These are pre-verified legitimate (RESEARCH Package Legitimacy Audit: both [OK], created
    2023-05-21, no postinstall scripts; the workspace `minimumReleaseAge` supply-chain guard applies).
    Then add two import lines to `artifacts/web/src/main.tsx`: `import '@fontsource-variable/inter';`
    and `import '@fontsource-variable/noto-sans-jp';`, placed AFTER `import "./instrument";` (line 1,
    Sentry must init first) and BEFORE `import "./index.css";`. The default Fontsource import pulls
    the weight-axis CSS which already declares `font-display: swap` — no manual @font-face override.
    The Google Fonts CDN removal happens in Task 3 (index.html).
  </action>
  <verify>
    <automated>cd artifacts/web && grep -q "@fontsource-variable/inter" src/main.tsx && grep -q "@fontsource-variable/noto-sans-jp" src/main.tsx && grep -q "@fontsource-variable/inter" package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both packages appear in `artifacts/web/package.json` dependencies
    - `main.tsx` import order is: `./instrument` first, then the two `@fontsource-variable/*` imports, then react-dom/App, then `./index.css` last
    - `pnpm install --frozen-lockfile` succeeds after add (lockfile consistent) — or note lockfile change in SUMMARY
    - No postinstall script ran for either Fontsource package (confirm install output)
    - `pnpm --filter @workspace/web run typecheck` exits 0
  </acceptance_criteria>
  <done>Both Fontsource packages are installed and imported in main.tsx in the correct order; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Configure vite.config.ts to emit woff2 at a stable unhashed path (preload prerequisite, MKT-15/MKT-17)</name>
  <files>artifacts/web/vite.config.ts</files>
  <read_first>
    - artifacts/web/vite.config.ts (current build block lines 57-60: outDir + emptyOutDir, no rollupOptions)
    - .planning/phases/05-foundation-isolation/05-PATTERNS.md § "artifacts/web/vite.config.ts" (lines 451-488) for the exact assetFileNames function
    - .planning/phases/05-foundation-isolation/05-RESEARCH.md § Pattern 3 "If the planner wants to include the preload anyway" (lines 335-345)
  </read_first>
  <action>
    Add `rollupOptions.output.assetFileNames` to the existing `build` block in
    `artifacts/web/vite.config.ts` (keep `outDir` and `emptyOutDir` unchanged). The function:
    if `assetInfo.name` ends with `.woff2`, return `'assets/fonts/[name][extname]'` (stable, unhashed)
    so the preloaded font lands at a path the index.html preload tag can reference; otherwise return
    `'assets/[name]-[hash][extname]'` (default content-hashing for all other assets, preserving cache-busting).
    This is the resolution to the font-preload cross-doc tension — it makes the production build emit
    `dist/public/assets/fonts/noto-sans-jp-latin-wght-normal.woff2` at a fixed path. Do NOT change
    ports, the plugin stack, or the PORT/BASE_PATH env handling.
  </action>
  <verify>
    <automated>cd artifacts/web && grep -q 'assetFileNames' vite.config.ts && pnpm run build && test -f dist/public/assets/fonts/noto-sans-jp-latin-wght-normal.woff2 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'assetFileNames' artifacts/web/vite.config.ts` succeeds
    - `pnpm --filter @workspace/web run build` exits 0
    - After build, `artifacts/web/dist/public/assets/fonts/noto-sans-jp-latin-wght-normal.woff2` exists (stable unhashed name)
    - Non-font assets still hashed: at least one file matching `dist/public/assets/*-*.js` exists (default hashing preserved)
    - Ports unchanged: `git diff artifacts/web/vite.config.ts` shows no edits to PORT/server/preview port config
  </acceptance_criteria>
  <done>vite.config.ts emits woff2 at the stable assets/fonts/ path; the build produces the preload target file; other assets remain content-hashed.</done>
</task>

<task type="auto">
  <name>Task 3: Replace index.html head — marketing meta/og/twitter/hreflang + working font preload; remove Google Fonts CDN (MKT-16, MKT-17, MKT-15)</name>
  <files>artifacts/web/index.html</files>
  <read_first>
    - artifacts/web/index.html (current 24 lines; "7of1" placeholders lines 6,9,10,13,14; Google Fonts CDN lines 16-18)
    - .planning/phases/05-foundation-isolation/05-UI-SPEC.md § "Copywriting Contract" (lines 288-308) for the exact meta/og/twitter/hreflang strings and § "Static SEO Assets Contract" -> index.html hreflang additions (lines 505-508)
    - .planning/phases/05-foundation-isolation/05-PATTERNS.md § "artifacts/web/index.html" target head block (lines 73-95)
    - This plan's <cross_doc_resolution> block (preload uses the stable /assets/fonts/ path from Task 2, NOT node_modules)
    - IMPORTANT: the PATTERNS.md target head block was historically missing the font preload — the <cross_doc_resolution> in this plan is authoritative; the `<link rel="preload">` for the 400-weight Noto Sans JP woff2 at `/assets/fonts/noto-sans-jp-latin-wght-normal.woff2` MUST be present per CONTEXT.md (non-negotiable). (PATTERNS.md has since been corrected to include it, but if any copy you read omits it, the preload is still REQUIRED.)
  </read_first>
  <action>
    Edit the `<head>` of `artifacts/web/index.html`.
    REMOVE: `<title>7of1</title>`; the "7of1" `<meta name="description">`, `og:title`, `og:description`,
    `twitter:title`, `twitter:description`; and all THREE Google Fonts CDN tags
    (`<link rel="preconnect" ...googleapis.com>`, `<link rel="preconnect" ...gstatic.com crossorigin>`,
    `<link href="...fonts.googleapis.com/css2?family=Inter...">`).
    ADD (exact strings from UI-SPEC Copywriting Contract):
    title = "lala.la — AI Digital Twin for Creators";
    meta description = "Your AI twin, fully managed — keep your fans engaged in your voice while you create. For 17 LIVE creators in JP, TW, and HK.";
    keep `<meta name="robots" content="index, follow" />`;
    og:title = "lala.la — AI Digital Twin for Creators"; og:description = "Your AI twin, fully managed. Chat, voice, and more — we handle everything."; og:type = "website"; og:url = "https://lala.la"; og:image = "https://lala.la/og-marketing.png";
    twitter:card = "summary_large_image"; twitter:title = (same as og:title); twitter:description = (same as og:description); twitter:image = "https://lala.la/og-marketing.png";
    keep the favicon `<link>`.
    ADD the four STATIC hreflang tags (raw HTML, NOT JS-injected — Pitfall 4/9):
    hreflang x-default -> https://lala.la/en; hreflang en -> https://lala.la/en;
    hreflang ja -> https://lala.la/ja; hreflang zh-TW -> https://lala.la/zh-TW.
    ADD the NON-NEGOTIABLE font preload using the STABLE path from Task 2:
    `<link rel="preload" as="font" type="font/woff2" href="/assets/fonts/noto-sans-jp-latin-wght-normal.woff2" crossorigin="anonymous" />`.
    Do NOT use a /node_modules/ href (breaks in production). Do NOT drop the preload. All meta text is
    English for all locales per D-05-03 (crawlers do not run JS; on-page copy still localizes via i18n).
  </action>
  <verify>
    <automated>cd artifacts/web && grep -q 'lala.la — AI Digital Twin for Creators' index.html && grep -q 'href="https://lala.la/og-marketing.png"' index.html && grep -q 'property="og:url"' index.html && test $(grep -c 'hreflang=' index.html) -eq 4 && grep -q 'rel="preload"' index.html && ! grep -q 'fonts.googleapis.com' index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `<title>` is exactly `lala.la — AI Digital Twin for Creators`
    - `grep -c 'hreflang=' artifacts/web/index.html` returns exactly 4 (x-default, en, ja, zh-TW), all in raw HTML before the `<script>` tag
    - `og:url` present: `grep -q 'property="og:url"' artifacts/web/index.html` exits 0 (value `https://lala.la`) — MKT-16 requires all og:* tags
    - `og:image` and `twitter:image` both equal `https://lala.la/og-marketing.png` (matches the file committed in Plan 02)
    - No Google Fonts CDN reference remains: `grep -E 'googleapis.com|gstatic.com' artifacts/web/index.html` returns nothing
    - The preload `<link>` href is exactly `/assets/fonts/noto-sans-jp-latin-wght-normal.woff2` (NOT a node_modules path); `grep node_modules artifacts/web/index.html` returns nothing
    - Crawler simulation without JS: after `pnpm --filter @workspace/web run build`, `grep -c 'hreflang=' artifacts/web/dist/public/index.html` returns 4 and `grep -q 'og:title' artifacts/web/dist/public/index.html` succeeds (proves tags survive the build as static HTML)
    - INTEGRATION GATE: after build, the preloaded woff2 exists at exactly the path referenced — `test -f artifacts/web/dist/public/assets/fonts/noto-sans-jp-latin-wght-normal.woff2` succeeds (preload does NOT 404 in production)
  </acceptance_criteria>
  <done>index.html carries the full marketing meta/og/twitter set (including og:url), four static hreflang tags, and a production-resolvable font preload; Google Fonts CDN is gone; the built dist contains the preloaded woff2 at the referenced path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> build | Adding @fontsource-variable packages; supply-chain entry point |
| static index.html -> external crawler/scraper | meta/og/hreflang read by untrusted third-party scrapers; no user input flows in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | static meta/OG/hreflang integrity | accept | All meta/og/hreflang are hardcoded English strings authored in source; no user input flows into them — no injection surface; low-risk |
| T-05-SC | Tampering | @fontsource-variable npm installs | mitigate | Both packages pre-audited [OK] by slopcheck in RESEARCH (created 2023-05-21, no postinstall); workspace `minimumReleaseAge: 1440` supply-chain guard active; pnpm lockfile pins versions; no install-time scripts expected — confirm in install output |
| T-05-08 | Information Disclosure | og:image / preload paths | accept | og:image points to the brand-only card from Plan 02 (no creator likeness); preload path is a static public font asset; no private data exposed |
| T-05-09 | Spoofing | new network/runtime attack surface | accept | Phase 5 adds no routes, no API calls, no auth changes; fan route, KYC gates, and HMAC binding are untouched (App.tsx routing handled in Plan 01 with order preserved) |

Both Fontsource packages returned slopcheck [OK] — no [ASSUMED]/[SUS]/[SLOP] packages, so no
blocking-human legitimacy checkpoint is required.
**No HIGH-severity threats. Gate passes.**
</threat_model>

<verification>
- `pnpm --filter @workspace/web run typecheck` exits 0
- `pnpm --filter @workspace/web run build` exits 0
- main.tsx imports both Fontsource packages after ./instrument, before ./index.css
- vite.config.ts emits woff2 at stable assets/fonts/ path; build produces the preload target
- index.html: marketing meta/og/twitter, 4 static hreflang, working preload; Google Fonts CDN removed
- Built dist/public/index.html retains all SEO tags statically; preloaded woff2 present at referenced path
</verification>

<success_criteria>
- ROADMAP Phase 5 success criterion 3 (real marketing meta + og:* + twitter:card static, crawler-visible without JS) satisfied
- ROADMAP Phase 5 success criterion 5 (Noto Sans JP font-display: swap + working 400-weight preload, no mobile FOIT) satisfied
- Font-preload cross-doc tension resolved: preload retained AND production build emits the woff2 at the referenced path
</success_criteria>

<output>
Create `.planning/phases/05-foundation-isolation/05-03-SUMMARY.md` when done
</output>
