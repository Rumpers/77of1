---
phase: 02-twin-runtime-core
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - artifacts/web/src/components/fan/MessageBubble.tsx
  - artifacts/web/src/components/fan/MessageInput.tsx
  - artifacts/web/src/components/fan/DisclosureBanner.tsx
  - artifacts/web/src/components/fan/DisclosureFooter.tsx
  - artifacts/web/src/components/fan/TypingIndicator.tsx
  - artifacts/web/src/components/fan/LocaleSwitcher.tsx
  - artifacts/web/src/components/fan/ReportDialog.tsx
  - artifacts/web/src/components/fan/PaywallDrawer.tsx
  - artifacts/web/src/pages/fan-page.tsx
  - artifacts/web/src/lib/i18n.ts
  - artifacts/web/src/lib/api.ts
  - artifacts/web/src/index.css
autonomous: true
requirements: [CHAT-01, CHAT-05, COMPLY-01, I18N-02]
tags: [phase-2, web-ui, fan-page, refactor, i18n, locale-switcher]

must_haves:
  truths:
    - "fan-page.tsx is ≤200 lines (down from 813) — composition shell only"
    - "Each of MessageBubble, MessageInput, DisclosureBanner, DisclosureFooter, TypingIndicator, LocaleSwitcher, ReportDialog, PaywallDrawer lives in its own .tsx file under components/fan/"
    - "All 8 components use Tailwind classes (no inline style={{...}} except brand-color CSS var)"
    - "LocaleSwitcher renders 3 items (EN / 日本語 / 繁中); click navigates to /${locale}/${handle}"
    - "Web SPA fetches GET /api/twin/:handle/profile on mount; CTA URL + brand_color flow from API (not fixture)"
    - "Disclosure banner auto-dismisses at 3s OR on click"
    - "i18n.ts `fan` namespace contains the new keys (empty_state, error_connection, error_paused, error_kyc, monetization_cta) in all 3 locales"
    - "Existing visual behavior preserved (3-trial paywall, report dialog, OTP flow) — refactor is hygiene, not behavior change"
  artifacts:
    - path: "artifacts/web/src/components/fan/"
      provides: "Directory containing 8 typed component files"
    - path: "artifacts/web/src/components/fan/LocaleSwitcher.tsx"
      provides: "DropdownMenu-based locale switcher with 3 items"
    - path: "artifacts/web/src/lib/api.ts"
      provides: "fetchTwinProfile(handle) → { handle, brand_color, monetization_url, platform_name }"
    - path: "artifacts/web/src/lib/i18n.ts"
      contains: "empty_state, error_connection, error_paused, error_kyc, monetization_cta keys in en/ja/zh-TW"
    - path: "artifacts/web/src/index.css"
      contains: ".dark { ... real HSL values from UI-SPEC Color section ... }"
  key_links:
    - from: "fan-page.tsx"
      to: "components/fan/*"
      via: "imports"
      pattern: "from \"\\.\\./components/fan/"
    - from: "fan-page.tsx"
      to: "lib/api.ts"
      via: "fetchTwinProfile(handle) on mount"
      pattern: "fetchTwinProfile"
    - from: "LocaleSwitcher.tsx"
      to: "wouter navigation"
      via: "useLocation()/setLocation()"
      pattern: "useLocation|setLocation"
---

<objective>
Refactor the 813-line inline-styled `fan-page.tsx` into 8 typed components under `artifacts/web/src/components/fan/` per UI-SPEC Component Inventory and D-02-11. Wire the new `LocaleSwitcher` (new component) and the API-driven creator profile (`/api/twin/:handle/profile` from 02-03) into the page composition. Extend `lib/i18n.ts` with the Phase 2 string keys.

Purpose: D-02-11 (refactor hygiene), CHAT-05 (CTA from API not fixture), I18N-02 (LocaleSwitcher UI surface), COMPLY-01 (DisclosureBanner + DisclosureFooter cleanly separated). Visual design is unchanged — this is hygiene + plumbing, not redesign.

Output: 8 new component files + refactored fan-page.tsx + extended i18n.ts + new lib/api.ts client + updated .dark CSS variables. CrisisHelplineBubble and MonetizationCTA components are NOT in this plan (they ship with the moderation pipeline in 02-05 since they depend on moderation response fields).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-twin-runtime-core/02-CONTEXT.md
@.planning/phases/02-twin-runtime-core/02-UI-SPEC.md
@.planning/phases/02-twin-runtime-core/02-PATTERNS.md
@.planning/phases/02-twin-runtime-core/02-03-SUMMARY.md
@artifacts/web/src/pages/fan-page.tsx
@artifacts/web/src/lib/i18n.ts
@artifacts/web/src/index.css
@artifacts/web/components.json
@CLAUDE.md
</context>

<interfaces>
<!-- shadcn primitives already installed in artifacts/web/src/components/ui/ -->
Existing shadcn blocks available:
- button.tsx, dialog.tsx, drawer.tsx, input.tsx, textarea.tsx, alert.tsx
- badge.tsx, separator.tsx, scroll-area.tsx, dropdown-menu.tsx
- label.tsx, skeleton.tsx, sonner (toast)

From wouter (already in deps):
- `useLocation(): [string, (path: string) => void]`
- `useParams<T>(): T`

From artifacts/web/src/lib/i18n.ts (existing):
- `Messages.fan` interface (extend with new keys)
- 3 locale files: `en`, `ja`, `zh-TW` namespace objects
- `useTranslation(locale, namespace)` hook returns a function `t(key, vars?)`

From plan 02-03 API contract:
- GET /api/twin/:handle/profile → `{handle, brand_color, monetization_url: string|null, platform_name, locale_default}`
- POST /api/twin/chat response shape: `{text, disclosure_footer, monetization_pivot: boolean, conversation_id}`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract 8 fan components from inline fan-page.tsx + extend i18n.ts</name>
  <files>artifacts/web/src/components/fan/MessageBubble.tsx, artifacts/web/src/components/fan/MessageInput.tsx, artifacts/web/src/components/fan/DisclosureBanner.tsx, artifacts/web/src/components/fan/DisclosureFooter.tsx, artifacts/web/src/components/fan/TypingIndicator.tsx, artifacts/web/src/components/fan/ReportDialog.tsx, artifacts/web/src/components/fan/PaywallDrawer.tsx, artifacts/web/src/lib/i18n.ts, artifacts/web/src/index.css</files>
  <read_first>
    - artifacts/web/src/pages/fan-page.tsx (full 813-line file — source of truth for what to extract)
    - artifacts/web/src/lib/i18n.ts (existing `fan` namespace, lines 46-86 type / 206-246 en / 409-449 ja / 612-652 zh-TW)
    - artifacts/web/src/index.css (existing CSS variables, lines around `:root` and `.dark`)
    - .planning/phases/02-twin-runtime-core/02-UI-SPEC.md (Component Inventory section + Color section + Copywriting Contract)
    - .planning/phases/02-twin-runtime-core/02-PATTERNS.md (E1-E10 — extraction patterns; E11 — i18n extension)
    - artifacts/web/src/components/ui/alert.tsx (shadcn alert — basis for DisclosureBanner)
    - artifacts/web/src/components/ui/textarea.tsx (basis for MessageInput)
    - artifacts/web/src/components/ui/dialog.tsx (basis for ReportDialog)
    - artifacts/web/src/components/ui/drawer.tsx (basis for PaywallDrawer)
  </read_first>
  <action>
    For each of the 7 components (MessageBubble, MessageInput, DisclosureBanner, DisclosureFooter, TypingIndicator, ReportDialog, PaywallDrawer):
    - Create `artifacts/web/src/components/fan/<Name>.tsx`
    - Extract the JSX + state from fan-page.tsx per PATTERNS E1-E10
    - Replace inline `style={{}}` with Tailwind classes (UI-SPEC Color/Spacing/Typography contract). Brand color stays as `style={{ background: "var(--brand)" }}` only on fan-message-bubble background and SendButton primary fill.
    - Props typed via inline TypeScript interface at top of file
    - Use `cn()` utility (likely at `artifacts/web/src/lib/utils.ts` — shadcn default) for conditional classes
    - Preserve EXACT visual behavior — no design tweaks; this is mechanical extraction

    Specific component notes:
    - **MessageBubble**: props `{role: "fan"|"ai"|"crisis"|"system", text: string, pending?: boolean, brandColor?: string}`. Per PATTERNS E1 — bubble shape via rounded-tl/tr/br/bl classes, role-conditional bg.
    - **MessageInput**: props `{value, onChange, onSubmit, disabled?, placeholder}`. Wraps shadcn Textarea + SendButton. Handle Enter/Shift+Enter per UI-SPEC Interaction Contract step 1.
    - **DisclosureBanner**: props `{locale}`. Auto-dismiss after 3s via useEffect setTimeout. shadcn Alert as base. `role="status" aria-live="polite"` per Accessibility Contract.
    - **DisclosureFooter**: props `{handle, locale, footerText?}`. Render `{footerText ?? defaultFromI18n} · @{handle}_ai` — use the `footerText` from API response when present (lets server be source of truth per D-02-12).
    - **TypingIndicator**: 3 dots animated via `tw-animate-css` `animate-bounce` with stagger. `aria-label="Twin is typing" role="status"`.
    - **ReportDialog**: refactor existing inline dialog from fan-page.tsx into shadcn Dialog. Props `{open, onOpenChange, messageId, onSubmit}`.
    - **PaywallDrawer**: refactor existing inline paywall into shadcn Drawer (vaul-based). Props `{open, onOpenChange, locale, handle}`.

    Extend `artifacts/web/src/lib/i18n.ts` per PATTERNS E11:
    - Add to `Messages.fan` interface: `empty_state: string; error_connection: string; error_paused: string; error_kyc: string; monetization_cta: string;`
    - Add values to all three locale objects (en/ja/zh-TW) — copy verbatim from UI-SPEC Copywriting Contract table

    Update `artifacts/web/src/index.css` `.dark { ... }` block: replace placeholder red values with real HSL per UI-SPEC Color section. Background `0 0% 6%`, elevated surface `0 0% 10%`, AI bubble `0 0% 16%`, muted text `0 0% 67%`, border `0 0% 13%`, destructive `0 84% 71%`, etc. Per D-02-08 dark-mode-only — leave `:root` placeholders for Phase 3.

    DO NOT touch fan-page.tsx in this task — Task 2 composes the extracted components.
  </action>
  <verify>
    <automated>cd artifacts/web && pnpm exec tsc --noEmit && for c in MessageBubble MessageInput DisclosureBanner DisclosureFooter TypingIndicator ReportDialog PaywallDrawer; do test -f "src/components/fan/$c.tsx" || { echo "MISSING: $c"; exit 1; }; done && grep -c "empty_state\|error_connection\|error_paused\|error_kyc\|monetization_cta" artifacts/web/src/lib/i18n.ts | awk '{exit ($1>=15)?0:1}'</automated>
  </verify>
  <done>
    - All 7 component files exist under components/fan/
    - artifacts/web typechecks
    - i18n.ts has the 5 new keys in all 3 locales (15 occurrences total)
    - index.css `.dark` block has real HSL values
  </done>
  <acceptance_criteria>
    - Per D-02-11: components extracted from inline styles to typed files
    - Per D-02-08: dark mode only — `:root` (light) values left as placeholder
    - Per UI-SPEC Copywriting Contract: all 5 new i18n keys present in en/ja/zh-TW verbatim
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: LocaleSwitcher component + lib/api.ts client + fan-page.tsx composition refactor</name>
  <files>artifacts/web/src/components/fan/LocaleSwitcher.tsx, artifacts/web/src/lib/api.ts, artifacts/web/src/pages/fan-page.tsx</files>
  <read_first>
    - artifacts/web/src/pages/fan-page.tsx (newly-stripped — see what's left after Task 1 components extracted)
    - artifacts/web/src/components/ui/dropdown-menu.tsx (basis for LocaleSwitcher)
    - artifacts/web/src/lib/creator-fixtures.ts (existing fixture lookup — REPLACE with API call)
    - .planning/phases/02-twin-runtime-core/02-UI-SPEC.md (Interaction Contract → Locale switcher section)
    - .planning/phases/02-twin-runtime-core/02-PATTERNS.md (E composition pattern; "No Analog Found" for LocaleSwitcher)
  </read_first>
  <action>
    Create `artifacts/web/src/components/fan/LocaleSwitcher.tsx`:
    - Use shadcn DropdownMenu with 3 items: `EN` / `日本語` / `繁中`
    - Trigger = Globe icon from lucide-react inside Button (variant=ghost size=icon)
    - 44×44px hit area (use `h-11 w-11` Tailwind = 44px)
    - On select: `setLocation(\`/${locale}/${handle}\`)` via wouter
    - Active locale shows lucide Check mark
    - Position: absolute top-3 right-3 (over cover image per UI-SPEC)
    - Props: `{currentLocale, handle}`

    Create `artifacts/web/src/lib/api.ts`:
    - Export `async fetchTwinProfile(handle): Promise<{handle, brand_color, monetization_url, platform_name, locale_default}>` — fetch `/api/twin/${handle}/profile`, throw on non-2xx
    - Export `async sendTwinMessage({handle, message, locale}): Promise<{text, disclosure_footer, monetization_pivot, conversation_id}>` — POST `/api/twin/chat` with credentials: 'include' (so HMAC cookie is sent/received)
    - Use `import.meta.env.VITE_API_BASE_URL ?? ""` for base URL (or relative `/api/...` for Replit same-origin)
    - All thrown errors include status code; caller decides UI mapping (423 → error_kyc string, 503 → error_paused, network → error_connection)

    Refactor `artifacts/web/src/pages/fan-page.tsx`:
    - REMOVE all extracted inline JSX (MessageBubble, MessageInput, DisclosureBanner, DisclosureFooter, TypingIndicator, ReportDialog, PaywallDrawer instances now imported)
    - Replace `getCreatorConfig(handle)` fixture call with `fetchTwinProfile(handle)` via TanStack Query: `const { data: profile } = useQuery({ queryKey: ['twin-profile', handle], queryFn: () => fetchTwinProfile(handle) })`
    - Replace inline fetch to `/api/twin/chat` with `sendTwinMessage({...})` from new lib/api.ts
    - Map errors: status 423 → render `error_kyc` MessageBubble role=system, status 503 → `error_paused`, network → `error_connection`
    - Add `<LocaleSwitcher currentLocale={locale} handle={handle} />` to header area
    - Add `<DisclosureBanner locale={locale} />` at page top mount
    - Target ≤200 lines for fan-page.tsx (composition only; logic in components and lib/api.ts)

    Keep all behavior identical: trial counter logic, OTP flow, paywall trigger at 3 messages, report submission. Just hoist the JSX into the extracted components.
  </action>
  <verify>
    <automated>cd artifacts/web && pnpm exec tsc --noEmit && wc -l src/pages/fan-page.tsx | awk '{exit ($1<=250)?0:1}' && grep -c "fetchTwinProfile\|sendTwinMessage" src/pages/fan-page.tsx | awk '{exit ($1>=2)?0:1}' && grep -c "import.*from.*\\./components/fan/" src/pages/fan-page.tsx | awk '{exit ($1>=5)?0:1}' && grep -c "LocaleSwitcher" src/pages/fan-page.tsx</automated>
  </verify>
  <done>
    - LocaleSwitcher.tsx exists with DropdownMenu structure
    - lib/api.ts exports fetchTwinProfile and sendTwinMessage
    - fan-page.tsx ≤250 lines (target ≤200 — allow slack)
    - fan-page.tsx imports ≥5 components from ./components/fan/
    - LocaleSwitcher rendered on page
    - Visual smoke test (founder): page renders same as before, with new locale switcher in top-right
  </done>
  <acceptance_criteria>
    - Per D-02-11: fan-page.tsx is composition shell; logic in components/lib
    - Per CHAT-05: monetization_url and brand_color come from API (fetchTwinProfile), not fixtures
    - Per I18N-02: LocaleSwitcher navigates to new locale URL
    - Per CHAT-01: send message flow uses sendTwinMessage from lib/api.ts (no inline fetch)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api response → React state | API contract from plan 02-03; trust API not to lie about disclosure_footer presence |
| user input textarea → fan state → API | fan-controlled; sent to /api/twin/chat (validated server-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | XSS via twin reply text | mitigate | React JSX escapes text by default; no dangerouslySetInnerHTML in any fan component |
| T-02-04-02 | Info Disclosure | brand color contrast bypass | accept | UI-SPEC Color section flags this; validateCreatorBrandColor() deferred to Phase 3 (per Deferred Ideas in CONTEXT.md); fixture defaults pass WCAG |
| T-02-04-03 | Spoofing | LocaleSwitcher navigation to malicious URL | mitigate | hardcoded 3-locale allow-list (EN / 日本語 / 繁中); setLocation only ever called with one of three known strings |
| T-02-04-04 | Info Disclosure | API base URL leak via build artifact | accept | Replit same-origin (relative paths); VITE_API_BASE_URL is a non-secret build var |
| T-02-04-SC | Tampering | npm installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `pnpm --filter web exec tsc --noEmit` exits 0 (or `cd artifacts/web && pnpm exec tsc --noEmit`)
- `pnpm --filter web run build` exits 0 (Vite build succeeds)
- Manual smoke (founder, browser): Visit `/en/sakura`, verify page renders identically to pre-refactor; LocaleSwitcher appears top-right; click 日本語 → URL becomes `/ja/sakura`; send a message → AI reply renders with disclosure footer
- `wc -l artifacts/web/src/pages/fan-page.tsx` returns ≤250
- `find artifacts/web/src/components/fan/ -name "*.tsx" | wc -l` returns ≥8
</verification>

<success_criteria>
- 8 components extracted (7 from existing inline + 1 new LocaleSwitcher)
- fan-page.tsx reduced from 813 to ≤250 lines
- Web SPA consumes /api/twin/:handle/profile (no more fixture lookup)
- i18n.ts has new keys for all 3 locales
- Dark theme CSS variables bound to real HSL values
- Visual behavior unchanged (founder smoke test)
</success_criteria>

<output>
Create `.planning/phases/02-twin-runtime-core/02-04-SUMMARY.md` with: final fan-page.tsx line count, files created, founder smoke-test confirmation.
</output>
