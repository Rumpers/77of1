---
phase: 01-baseline-repair
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - artifacts/hermes/src/db.ts
  - artifacts/hermes/src/consent.ts
  - artifacts/hermes/src/onboarding.ts
  - artifacts/hermes/src/asset-moderator.ts
  - artifacts/hermes/package.json
autonomous: true
requirements: [INFRA-02, INFRA-04]
must_haves:
  truths:
    - "Hermes bot starts, looks up a creator by Telegram user ID via Drizzle, and toggles creator_config.paused via Drizzle — without importing @supabase/supabase-js anywhere"
    - "Kill-switch SLA elapsed-ms logging (<=5s) is preserved in setPaused"
    - "commitConsent inserts rows into consent_grants via Drizzle with retention_category='operational' per D-14"
    - "Fan-payment-adjacent functions (blockFan, listFansForCreator, getCreatorStats fan count, isFanBlocked) are deleted — D-10"
    - "artifacts/hermes/package.json declares @workspace/db as a workspace dep and no longer declares @supabase/supabase-js"
  artifacts:
    - path: "artifacts/hermes/src/db.ts"
      provides: "Drizzle-backed Hermes DB helpers: findCreatorByTelegramId, setPaused (SLA preserved), getCreatorPreferences, setTimezone, setHermesLanguage, getTotpRecord, saveTotpEnabled, disableTotpRecord, updateRecoveryCodes"
      exports: ["findCreatorByTelegramId", "setPaused", "getCreatorPreferences", "setTimezone", "setHermesLanguage", "getTotpRecord", "saveTotpEnabled", "disableTotpRecord", "updateRecoveryCodes"]
    - path: "artifacts/hermes/src/consent.ts"
      provides: "commitConsent writes consent_grants rows via Drizzle; telegramIpHash preserved; in-memory state machine unchanged"
    - path: "artifacts/hermes/package.json"
      provides: "@workspace/db workspace:* in dependencies; no @supabase/supabase-js entry"
  key_links:
    - from: "artifacts/hermes/src/db.ts"
      to: "@workspace/db"
      via: "import { db, creatorsTable, creatorConfigTable, creatorKycTable, creatorTotpTable } from @workspace/db"
      pattern: "from .@workspace/db."
    - from: "artifacts/hermes/src/consent.ts"
      to: "@workspace/db"
      via: "import { db, consentGrantsTable } from @workspace/db"
      pattern: "consentGrantsTable"
    - from: "artifacts/hermes/package.json"
      to: "lib/db package"
      via: "workspace:* dependency reference"
      pattern: "@workspace/db.*workspace:"
---

<objective>
Rewrite the Hermes Telegram bot's data layer onto Drizzle + Replit PG so the creator's first user-visible interaction with lala.la — opening the bot, being recognized as a creator, pausing/resuming the twin — exercises the same `@workspace/db` singleton as the api-server. Remove every Supabase import from Hermes, delete fan-payment functions that referenced out-of-scope tables, and add `@workspace/db` as a workspace dependency. Hermes Telegraf scenes/sessions and the kill-switch SLA logging pattern are preserved untouched.

Purpose: D-09/D-10 mandate this Hermes migration; INFRA-02 requires Supabase fully removed; the kill-switch (`/pause`, `/resume`) is the only creator-side control that must stay within its 5s SLA in Phase 1, so that pattern must be carried over verbatim. Without this plan, Hermes still requires `SUPABASE_URL` env vars on cold start — blocking INFRA-01.

Output: artifacts/hermes/src/db.ts, consent.ts, onboarding.ts (partial), asset-moderator.ts and package.json all Drizzle-backed; Hermes cold-starts and exercises Drizzle for creator lookup, kill switch, and consent capture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-baseline-repair/01-CONTEXT.md
@.planning/phases/01-baseline-repair/01-RESEARCH.md
@.planning/phases/01-baseline-repair/01-PATTERNS.md
@.planning/phases/01-baseline-repair/01-01-SUMMARY.md
@artifacts/hermes/src/db.ts
@artifacts/hermes/src/consent.ts
@artifacts/hermes/src/onboarding.ts
@artifacts/hermes/src/asset-moderator.ts
@artifacts/hermes/package.json
@artifacts/api-server/package.json

<interfaces>
From @workspace/db (live after Plan 01):
- db, pool
- creatorsTable, creatorConfigTable, creatorKycTable, creatorTotpTable, consentGrantsTable, safetyAuditLogTable
- eq, and (drizzle-orm)

Function signatures Hermes downstream code expects (preserve verbatim):
- findCreatorByTelegramId(telegramUserId: number): Promise of { id: string; displayName: string } | null
- setPaused(creatorId: string, paused: boolean): Promise of { elapsed: number }
- getCreatorPreferences(creatorId: string): Promise of { paused: boolean; timezone: string; hermes_language: string } | null
- setTimezone(creatorId: string, timezone: string): Promise of void
- setHermesLanguage(creatorId: string, hermesLanguage: string): Promise of void
- getTotpRecord(creatorId: string): Promise of { totpSecret: string; totpEnabled: boolean; recoveryCodes: string[] } | null
- saveTotpEnabled(creatorId, totpSecret, recoveryCodes): Promise of void
- disableTotpRecord(creatorId: string): Promise of void
- updateRecoveryCodes(creatorId: string, recoveryCodes: string[]): Promise of void
- telegramIpHash(tgUserId: number): string   (from consent.ts — preserve verbatim)

Workspace dep pattern (already in api-server/package.json):
"@workspace/db": "workspace:*"

Consent modality literal values (must match consentGrantModalityEnum):
"persona_text" | "voice" | "image" | "talking_video" | "fullbody_video"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite artifacts/hermes/src/db.ts on Drizzle; delete fan-payment functions; preserve SLA logging</name>
  <files>artifacts/hermes/src/db.ts</files>
  <read_first>
    - artifacts/hermes/src/db.ts (entire current file — capture all 13+ function signatures before rewriting)
    - artifacts/hermes/src/db.ts.tmp.2877053.d3bd7e3d921e (a stale tmp; do not act on it — but read for context if it differs)
    - lib/db/src/schema/index.ts (after Plan 01 — confirm exported tables: creatorsTable, creatorConfigTable, creatorKycTable, creatorTotpTable)
    - .planning/phases/01-baseline-repair/01-PATTERNS.md section "artifacts/hermes/src/db.ts"
    - .planning/phases/01-baseline-repair/01-RESEARCH.md Pattern 3 (Drizzle Query Equivalents)
  </read_first>
  <behavior>
    - findCreatorByTelegramId(123) returns { id, displayName } or null via db.select({ id, displayName: creatorsTable.displayName }).from(creatorsTable).where(eq(creatorsTable.telegramUserId, String(telegramUserId))).limit(1).
    - setPaused(creatorId, true):
      - records t0 = Date.now() before the DB call
      - runs db.update(creatorConfigTable).set({ paused: true, updatedAt: new Date() }).where(eq(creatorConfigTable.creatorId, creatorId))
      - logs `[hermes] kill-switch creator_id=<id> paused=true db_write_ms=<elapsed>` to stdout
      - logs a console.error `[hermes] WARN kill-switch db write took <ms>ms — approaching 5s SLA` when elapsed > 4000
      - returns { elapsed }
    - getCreatorPreferences(creatorId) returns { paused, timezone, hermes_language } via Drizzle select on creatorConfigTable (or null when row absent).
    - setTimezone / setHermesLanguage run upserts: db.insert(creatorConfigTable).values({ creatorId, ... }).onConflictDoUpdate({ target: creatorConfigTable.creatorId, set: { ..., updatedAt: new Date() } }) so first-time-setter scenarios work without a separate insert path.
    - The 4 TOTP functions (getTotpRecord, saveTotpEnabled, disableTotpRecord, updateRecoveryCodes) all operate on creatorTotpTable from @workspace/db; saveTotpEnabled uses onConflictDoUpdate keyed on creatorId.
    - Functions removed entirely (D-10): blockFan, listFansForCreator, isFanBlocked, the fan-count branch of getCreatorStats. If getCreatorStats has any non-fan-count caller (e.g., creator-level stats), keep those branches; if not, delete getCreatorStats entirely.
    - File contains zero @supabase/supabase-js references and zero createClient / getDb() factory functions.
  </behavior>
  <action>Replace the entire current file. Top imports: import { db } from "@workspace/db"; import { creatorsTable, creatorConfigTable, creatorKycTable, creatorTotpTable } from "@workspace/db"; import { eq } from "drizzle-orm";. Preserve exported type signatures so all Hermes callers (in index.ts, consent.ts, onboarding.ts, i18n.ts) continue to compile without source changes; if a return shape differs (e.g., paused/timezone/hermes_language snake-case keys) keep the snake-case keys to match existing callers. Implement each kept function using the Drizzle patterns documented in PATTERNS.md "artifacts/hermes/src/db.ts" verbatim. The kill-switch SLA t0 = Date.now() -> elapsed = Date.now() - t0 pattern is non-negotiable — copy it lines-for-lines from the existing file. Delete the fan-payment functions enumerated in PATTERNS.md "Functions to REMOVE". Do not modify Telegraf/scene code in any other file. Do not rename functions even if their Drizzle implementations are simpler.</action>
  <acceptance_criteria>
    - artifacts/hermes/src/db.ts has zero matches for `@supabase/supabase-js` and zero matches for `createClient(`
    - artifacts/hermes/src/db.ts contains the string `from "@workspace/db"`
    - artifacts/hermes/src/db.ts contains `db_write_ms=` and `approaching` text (SLA logging preserved)
    - artifacts/hermes/src/db.ts contains at least one `onConflictDoUpdate` usage (upsert pattern)
    - artifacts/hermes/src/db.ts has zero matches for `blockFan`, `listFansForCreator`, `isFanBlocked`
    - `pnpm --filter @workspace/hermes exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @workspace/hermes exec tsc --noEmit && ! grep -qE 'supabase-js|createClient\(' artifacts/hermes/src/db.ts && grep -q 'from "@workspace/db"' artifacts/hermes/src/db.ts && grep -q 'db_write_ms=' artifacts/hermes/src/db.ts && grep -q 'approaching' artifacts/hermes/src/db.ts && grep -q 'onConflictDoUpdate' artifacts/hermes/src/db.ts && ! grep -qE 'blockFan|listFansForCreator|isFanBlocked' artifacts/hermes/src/db.ts</automated>
  </verify>
  <done>artifacts/hermes/src/db.ts is Supabase-free, Drizzle-backed, kill-switch SLA pattern preserved, fan-payment functions deleted, hermes typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite consent.ts commitConsent on Drizzle; preserve telegramIpHash; stub out-of-scope table writes</name>
  <files>artifacts/hermes/src/consent.ts</files>
  <read_first>
    - artifacts/hermes/src/consent.ts (current — locate commitConsent ~lines 224-265, telegramIpHash ~lines 151-154, in-memory state Map TODO)
    - lib/db/src/schema/index.ts (confirm consentGrantsTable column names and consentGrantModalityEnum literal values)
    - .planning/phases/01-baseline-repair/01-PATTERNS.md section "artifacts/hermes/src/consent.ts"
  </read_first>
  <behavior>
    - commitConsent(creatorId, answers, ipHash) (or the existing signature — preserve it) inserts one row per CONSENT_ITEMS entry into consent_grants via db.insert(consentGrantsTable).values([...]).
    - Each row has: creatorId, modality (cast to ConsentGrantModality from the schema enum literals), granted (boolean from answers[grantType]), grantedAt: new Date(), consentVersion: CONSENT_VERSION (preserve the existing const value, e.g., 'v1.0'), channel: "telegram", ipHash, retentionCategory: "operational" per D-14.
    - The version column is set to 1 on first commit; if a re-commit path exists in the current code, the upsert pattern uses (creatorId, modality, version) uniqueness — re-commit increments version via a MAX(version) + 1 subquery or by passing the version explicitly from the caller.
    - The creator_assets and creator_onboarding writes that the current commitConsent performs (out of Phase 1 schema) are replaced with `console.log("[hermes] STUB: creator_assets write deferred to Phase 2"); console.log("[hermes] STUB: creator_onboarding write deferred to Phase 2");` — keep the function shape but make the writes no-ops with a logged-trace.
    - telegramIpHash(tgUserId) is preserved verbatim: createHash('sha256').update(String(tgUserId)).digest('hex').
    - The in-memory consent state Map / TODO note for Redis migration is preserved as-is (deferred per CONTEXT.md).
    - No @supabase/supabase-js or createClient references remain.
  </behavior>
  <action>Update imports: remove the Supabase imports; add import { db } from "@workspace/db"; import { consentGrantsTable } from "@workspace/db";. Rewrite the body of commitConsent using the Drizzle insert pattern in PATTERNS.md "artifacts/hermes/src/consent.ts" verbatim, including retentionCategory: "operational". Replace the existing Supabase calls to creator_assets and creator_onboarding with the logged stubs above and a comment marker `// PHASE-1 STUB: <table> write deferred to Phase 2 — table not in current Drizzle schema`. Do not touch the Telegraf scene definitions, the CONSENT_ITEMS array, or the in-memory state machine.</action>
  <acceptance_criteria>
    - artifacts/hermes/src/consent.ts has zero matches for `@supabase/supabase-js` and zero matches for `createClient(`
    - artifacts/hermes/src/consent.ts contains `consentGrantsTable` and `retentionCategory: "operational"`
    - artifacts/hermes/src/consent.ts contains `telegramIpHash` (preserved)
    - artifacts/hermes/src/consent.ts contains the literal `PHASE-1 STUB` referencing creator_assets and creator_onboarding
    - `pnpm --filter @workspace/hermes exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @workspace/hermes exec tsc --noEmit && ! grep -qE 'supabase-js|createClient\(' artifacts/hermes/src/consent.ts && grep -q 'consentGrantsTable' artifacts/hermes/src/consent.ts && grep -q 'retentionCategory: "operational"' artifacts/hermes/src/consent.ts && grep -q 'telegramIpHash' artifacts/hermes/src/consent.ts && grep -qE 'PHASE-1 STUB.*creator_assets' artifacts/hermes/src/consent.ts && grep -qE 'PHASE-1 STUB.*creator_onboarding' artifacts/hermes/src/consent.ts</automated>
  </verify>
  <done>commitConsent writes via Drizzle with retention_category; telegramIpHash preserved; deferred out-of-scope writes stubbed with logged trace; consent.ts Supabase-free; hermes typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Strip Supabase from onboarding.ts and asset-moderator.ts; replace in-Phase-1 calls with Drizzle, log-and-stub the rest</name>
  <files>artifacts/hermes/src/onboarding.ts, artifacts/hermes/src/asset-moderator.ts</files>
  <read_first>
    - artifacts/hermes/src/onboarding.ts (current — find every Supabase call and classify: in-Phase-1 vs out-of-Phase-1 schema tables)
    - artifacts/hermes/src/asset-moderator.ts (current — confirm it writes to safety_audit_log; that table IS in Phase 1 schema)
    - lib/db/src/schema/index.ts (confirm safetyAuditLogTable shape; confirm absence of creator_assets, creator_onboarding, creator_content_embeddings)
    - .planning/phases/01-baseline-repair/01-RESEARCH.md Pattern 6 (Safety Audit Log Drizzle Write)
  </read_first>
  <behavior>
    - asset-moderator.ts: any Supabase audit_log insert is rewritten to db.insert(safetyAuditLogTable).values({ ..., retentionCategory: "audit" }) per RESEARCH Pattern 6. fan_id_hash and message_hash are computed via the existing sha256 helper (or createHash if absent). No raw fan_id or raw message text is passed into Drizzle.
    - onboarding.ts: any Supabase call that writes to creators, creator_kyc, or creator_config is rewritten to Drizzle. Any Supabase call that writes to creator_assets, creator_onboarding, creator_content_embeddings (NOT in Phase 1 schema) is replaced with `console.log("[hermes] STUB: <table> write deferred to Phase 2 — out of Phase 1 schema scope");` plus a comment `// PHASE-1 STUB: <table> not in @workspace/db`. The embedding store call is similarly stubbed.
    - Neither file imports @supabase/supabase-js or calls createClient.
    - The Telegraf scene/wizard machinery is untouched.
  </behavior>
  <action>For each Supabase call in onboarding.ts and asset-moderator.ts, decide using the live exports of @workspace/db: if the target table is in the Phase 1 schema (creators, twins, creator_kyc, creator_config, consent_grants, conversation_messages, generation_jobs, safety_audit_log, creator_totp), rewrite the call to the Drizzle equivalent following the patterns in RESEARCH.md Pattern 3 + PATTERNS.md "Shared Patterns". Otherwise, replace the call with the logged stub above. Keep all error handling, validation, and Telegraf reply logic untouched. Remove every Supabase import from both files. Where a function previously took a SupabaseClient argument, change the signature to drop that argument and use the singleton db from @workspace/db; update internal callers in the same file to match (cross-file callers are limited to hermes, which is in this plan's scope).</action>
  <acceptance_criteria>
    - artifacts/hermes/src/onboarding.ts and asset-moderator.ts both have zero matches for `@supabase/supabase-js` and `createClient(`
    - artifacts/hermes/src/asset-moderator.ts contains `safetyAuditLogTable` and a `retentionCategory:` value of `"audit"`
    - All audit log writes pass hashed values only (`sha256` or `createHash` called on the fan_id and message before insert)
    - `pnpm --filter @workspace/hermes exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @workspace/hermes exec tsc --noEmit && ! grep -qE 'supabase-js|createClient\(' artifacts/hermes/src/onboarding.ts artifacts/hermes/src/asset-moderator.ts && grep -q '@workspace/db' artifacts/hermes/src/asset-moderator.ts && grep -q 'safetyAuditLogTable' artifacts/hermes/src/asset-moderator.ts && grep -q 'retentionCategory:' artifacts/hermes/src/asset-moderator.ts</automated>
  </verify>
  <done>onboarding.ts and asset-moderator.ts are Supabase-free; safety_audit_log writes via Drizzle with retention_category='audit'; out-of-scope writes are logged stubs; hermes typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 4: Update artifacts/hermes/package.json — add @workspace/db, remove @supabase/supabase-js; run install + typecheck</name>
  <files>artifacts/hermes/package.json</files>
  <read_first>
    - artifacts/hermes/package.json (current dependencies)
    - artifacts/api-server/package.json (reference: `@workspace/db` is already in this file as `workspace:*`)
    - pnpm-workspace.yaml (confirm `artifacts/*` is listed under packages so workspace resolution works)
  </read_first>
  <action>In artifacts/hermes/package.json: add `"@workspace/db": "workspace:*"` to dependencies (alphabetical order with other workspace deps if such an ordering exists in the file). Remove the `"@supabase/supabase-js": "^2.106.1"` entry from dependencies. Do not touch devDependencies or scripts. Run `pnpm install --frozen-lockfile=false` from the repo root to update the lockfile. Run `pnpm --filter @workspace/hermes exec tsc --noEmit` to confirm types resolve.</action>
  <acceptance_criteria>
    - artifacts/hermes/package.json contains the string `"@workspace/db": "workspace:*"` in dependencies
    - artifacts/hermes/package.json contains NO `@supabase/supabase-js` entry in any dependencies block
    - `pnpm install --frozen-lockfile=false` exits 0
    - `pnpm --filter @workspace/hermes exec tsc --noEmit` exits 0
    - `grep -RE 'supabase-js' artifacts/hermes/src/` returns nothing
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"@workspace/db"' artifacts/hermes/package.json && ! grep -q 'supabase-js' artifacts/hermes/package.json && pnpm install --frozen-lockfile=false && pnpm --filter @workspace/hermes exec tsc --noEmit && ! grep -RqE 'supabase-js' artifacts/hermes/src/</automated>
  </verify>
  <done>artifacts/hermes/package.json swaps Supabase for @workspace/db; lockfile updated; hermes typecheck passes; no Supabase imports remain in hermes source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram webhook -> hermes bot | Untrusted creator input crosses here; Telegraf already verifies webhook secret in production mode |
| hermes -> Replit PG (Drizzle) | All DB writes now flow through the typed @workspace/db singleton; no per-function createClient calls |
| /pause and /resume command -> setPaused -> creator_config | Latency-sensitive: must complete within 5s SLA to honor ONBOARD-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | /pause command lookup exceeds 5s SLA after migration | mitigate | Task 1 preserves the elapsed-ms logging and >4000ms warning verbatim per PATTERNS.md "Kill-switch SLA"; this surfaces regressions immediately in stdout, which the founder monitors during Phase 1 manual smoke tests |
| T-03-02 | Information Disclosure | hermes logs leak raw Telegram user IDs in plaintext | mitigate | telegramIpHash (sha256 of tgUserId) is preserved (Task 2); existing callers continue to pass the hash to consent_grants.ipHash. Audit-log writes in asset-moderator (Task 3) pass sha256(fan_id) and sha256(message) per RESEARCH Pattern 6 |
| T-03-03 | Tampering | A removed fan-payment function is reintroduced by accident in a later phase | mitigate | Task 1 deletes blockFan/listFansForCreator/isFanBlocked entirely from db.ts; CONTEXT.md D-10 documents the policy; reintroduction would require explicit re-planning |
| T-03-04 | Elevation of Privilege | A creator with no Replit PG creators row commands /pause and inadvertently flips someone else's flag | mitigate | findCreatorByTelegramId already returns null when no row matches; the hermes index.ts caller treats null as "not a registered creator" and refuses the command — Task 1 preserves this exact return contract |
| T-03-SC | Tampering | npm/pnpm installs (workspace dep add) | accept | The only dep added is the local workspace package @workspace/db; no external registry interaction; Supabase package is being REMOVED, not added; legitimacy gate not applicable per RESEARCH.md "Package Legitimacy Audit" |
</threat_model>

<verification>
- `pnpm --filter @workspace/hermes exec tsc --noEmit` exits 0
- `grep -RE 'supabase-js|createClient\(' artifacts/hermes/src/` returns nothing
- artifacts/hermes/package.json: zero `@supabase/supabase-js`, one `@workspace/db` workspace:* entry
- Kill-switch SLA pattern preserved verbatim in setPaused
- consent_grants inserts include retention_category='operational'
- safety_audit_log writes (from asset-moderator) include retention_category='audit' and only hashed identifiers
</verification>

<success_criteria>
Hermes is fully Drizzle-backed. A creator opening the bot, getting recognized, pausing the twin, and submitting consent answers all flow through @workspace/db with no Supabase client involvement. The kill-switch SLA logging remains in place. Fan-payment-adjacent functions are gone. The package.json no longer requires SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY env vars to start.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-repair/01-03-SUMMARY.md` when done.
</output>
