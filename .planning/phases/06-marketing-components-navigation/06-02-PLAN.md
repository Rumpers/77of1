---
phase: 06-marketing-components-navigation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - artifacts/web/src/components/marketing/HeroSection.tsx
  - artifacts/web/src/components/marketing/ValuePropSection.tsx
  - artifacts/web/src/components/marketing/FourPillarsSection.tsx
  - artifacts/web/src/components/marketing/DemoTranscriptSection.tsx
autonomous: true
requirements: [MKT-01, MKT-02, MKT-03, MKT-06]
must_haves:
  truths:
    - "Hero shows a localized headline, sub-headline, decorative bloom, and a single primary Telegram CtaButton"
    - "Value-prop section renders the managed-service outcome statement"
    - "Four pillars render chat+voice as live and image+video with a 'coming soon' badge"
    - "Demo transcript renders a static 2-turn twin conversation card with AI attribution"
  artifacts:
    - path: "artifacts/web/src/components/marketing/HeroSection.tsx"
      provides: "MKT-01 hero with CtaButton + HeroOrb"
      exports: ["HeroSection"]
      min_lines: 30
    - path: "artifacts/web/src/components/marketing/ValuePropSection.tsx"
      provides: "MKT-02 value proposition"
      exports: ["ValuePropSection"]
    - path: "artifacts/web/src/components/marketing/FourPillarsSection.tsx"
      provides: "MKT-03 four pillars with coming-soon badges"
      exports: ["FourPillarsSection"]
    - path: "artifacts/web/src/components/marketing/DemoTranscriptSection.tsx"
      provides: "MKT-06 static localized demo transcript"
      exports: ["DemoTranscriptSection"]
  key_links:
    - from: "artifacts/web/src/components/marketing/HeroSection.tsx"
      to: "CtaButton"
      via: "import from ./CtaButton"
      pattern: "CtaButton"
    - from: "artifacts/web/src/components/marketing/FourPillarsSection.tsx"
      to: "t.pillars.image_coming_soon / t.pillars.video_coming_soon"
      via: "PillarCard comingSoonLabel prop"
      pattern: "coming_soon"
---

<objective>
Build the hero and three of the content sections: `HeroSection` (MKT-01), `ValuePropSection` (MKT-02), `FourPillarsSection` (MKT-03), and `DemoTranscriptSection` (MKT-06).

Purpose: These four sections plus plan 06-03's three sections cover all six content requirements. This plan owns a disjoint file set from 06-03 so the two run in parallel. Neither plan touches `index.ts` (the barrel was written in full by 06-01) or `index.css`.
Output: HeroSection.tsx, ValuePropSection.tsx, FourPillarsSection.tsx, DemoTranscriptSection.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-marketing-components-navigation/06-RESEARCH.md
@.planning/phases/06-marketing-components-navigation/06-PATTERNS.md
@.planning/phases/06-marketing-components-navigation/06-UI-SPEC.md
@.planning/phases/06-marketing-components-navigation/06-01-SUMMARY.md

<interfaces>
From artifacts/web/src/components/marketing/CtaButton.tsx (built in 06-01):
```typescript
export function CtaButton(props: { label: string; fallbackLabel: string; size?: "sm" | "md" }): JSX.Element;
```
From artifacts/web/src/components/marketing/HeroOrb.tsx (built in 06-01):
```typescript
export function HeroOrb(): JSX.Element;
```
From artifacts/web/src/lib/i18n.ts:
```typescript
export type Locale = "en" | "ja" | "zh-TW";
export function getMessages(locale: string): Messages; // getMessages(locale).marketing has the shape below
```
Marketing copy slice (`t = getMessages(locale).marketing`):
- t.hero.{headline, subheadline, cta_primary, cta_no_telegram}
- t.value_prop.{title, subtitle}
- t.pillars.{title, chat_label, chat_desc, voice_label, voice_desc, image_label, image_desc, image_coming_soon, video_label, video_desc, video_coming_soon}
- t.demo.{title, label}

Type the `t` prop as `ReturnType<typeof getMessages>["marketing"]` (no named MarketingMessages export exists). Components receiving `t` as a prop should use this type alias.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeroSection (MKT-01)</name>
  <files>artifacts/web/src/components/marketing/HeroSection.tsx</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (HeroSection section lines 210-292 — exact analog including the LCP-safe motion wrapper)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Component Contract 4 "HeroSection" lines 256-294; Interaction States "Hero entrance animation" + LCP CRITICAL note lines 590-604)
    - artifacts/web/src/components/marketing/CtaButton.tsx (06-01 — the primitive to compose)
    - artifacts/web/src/components/marketing/HeroOrb.tsx (06-01)
    - artifacts/web/src/index.css lines 403-451 (@layer marketing-tokens)
  </read_first>
  <action>
    Create `HeroSection.tsx` exporting `HeroSection({ locale, t })` per the 06-PATTERNS analog. Outer `<section className="relative overflow-hidden w-full">` (overflow-hidden is mandatory — HeroOrb is 600px). Render `<HeroOrb />` then the `w-full max-w-[1120px] mx-auto px-6 py-24 lg:py-32` wrapper with an asymmetric `grid grid-cols-1 lg:grid-cols-[1.15fr_0.85fr] gap-12 items-center`. Left column is a `motion.div` wrapper (framer-motion) animating only the WRAPPER (`initial={{opacity:0,y:16}} animate={{opacity:1,y:0}}`) — the `<h1>` itself must render at full opacity (LCP rule: never put `initial opacity:0` on the headline text node). Contents top-to-bottom: optional eyebrow (Small, uppercase, tracking 0.14em, `text-[--mkt-accent]`); `<h1>` with `t.hero.headline` at Display size `clamp(2.25rem,5vw,4rem)` weight 700, `var(--mkt-font-display)`, `text-[--mkt-fg]`, leading 1.04, tracking -0.02em; `<p>` `t.hero.subheadline` Body `var(--mkt-font-sans)` `text-[--mkt-muted-fg]` mt-4; `<CtaButton label={t.hero.cta_primary} fallbackLabel={t.hero.cta_no_telegram} />` mt-8; the SB-243 disclosure pill (`border border-[--mkt-accent] rounded-[--mkt-radius-pill] px-3 py-1`, fuchsia dot `bg-[--mkt-glow-to]`, text "AI twin · not a real person" Small `text-[--mkt-muted-fg]`) mt-6 — hardcode the EN disclosure string for Phase 6 (Phase 7 localizes). Right column is the `hidden lg:block` decorative spacer. Apply fonts via inline `style` only, never `font-sans`.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -q "HeroOrb" artifacts/web/src/components/marketing/HeroSection.tsx && grep -q "CtaButton" artifacts/web/src/components/marketing/HeroSection.tsx && grep -q "overflow-hidden" artifacts/web/src/components/marketing/HeroSection.tsx && ! grep -E "bg-primary|text-foreground|font-sans\b|bg-card|border-border" artifacts/web/src/components/marketing/HeroSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - HeroSection imports and renders `HeroOrb` and a single `CtaButton` (one primary CTA — MKT-01).
    - The outer section has `overflow-hidden` so the 600px orb cannot cause horizontal scroll at 375px.
    - The `<h1>` text node is NOT given `initial={{ opacity: 0 }}`; only the outer `motion.div` wrapper animates (LCP rule).
    - Headline/subheadline read from `t.hero.headline` / `t.hero.subheadline`; CTA uses `t.hero.cta_primary` + `t.hero.cta_no_telegram`.
    - Zero fan-page utilities; fonts via `style={{ fontFamily: "var(--mkt-font-display|sans)" }}` (grep gate passes).
  </acceptance_criteria>
  <done>Hero renders localized headline + sub + bloom + single CTA, LCP-safe, no overflow source; grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: ValuePropSection + FourPillarsSection (MKT-02, MKT-03)</name>
  <files>artifacts/web/src/components/marketing/ValuePropSection.tsx, artifacts/web/src/components/marketing/FourPillarsSection.tsx</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (ValuePropSection lines 324-366; FourPillarsSection lines 370-432; shared Section Wrapper/Heading/Subtitle lines 793-846; min-w-0 rule lines 875-882)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Contract 6 ValueProp lines 312-330; Contract 7 FourPillars lines 332-359)
    - artifacts/web/src/index.css lines 403-451 (@layer marketing-tokens)
  </read_first>
  <action>
    Create `ValuePropSection.tsx` exporting `ValuePropSection({ t })`: a `text-center mx-auto max-w-[640px]` block inside the universal section wrapper (`<section className="w-full py-20"><div className="w-full max-w-[1120px] mx-auto px-6">`). `<h2>` = `t.value_prop.title` at Display size, `<p>` = `t.value_prop.subtitle` Body `text-[--mkt-muted-fg]` mt-4. Keep it minimal — no feature-list sub-element (UI-SPEC recommends deferring that to Phase 7). Create `FourPillarsSection.tsx` exporting `FourPillarsSection({ t })`: section wrapper + `<h2>` `t.pillars.title` + a `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`. Define an inline `PillarCard` sub-component per 06-PATTERNS (relative card, `border-[--mkt-border] bg-[--mkt-surface-1] p-5 min-w-0`, lucide icon 24px `text-[--mkt-muted-fg]`, `<h3>` Subheading weight 600 `var(--mkt-font-sans)`, `<p>` Small desc). Render 4 cards: Chat (`MessageCircle`, live=true), Voice (`Mic`, live=true), Image (`Image`, live=false, comingSoonLabel=`t.pillars.image_coming_soon`), Video (`Video`, live=false, comingSoonLabel=`t.pillars.video_coming_soon`). The coming-soon badge is `absolute right-3 top-3 rounded-[--mkt-radius-pill] border border-[--mkt-border] bg-[--mkt-surface-2] px-2 py-0.5` Small, rendered only when `!live && comingSoonLabel`. Every grid child carries `min-w-0`. Fonts via inline style only.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -q "value_prop.title" artifacts/web/src/components/marketing/ValuePropSection.tsx && grep -q "image_coming_soon" artifacts/web/src/components/marketing/FourPillarsSection.tsx && grep -q "video_coming_soon" artifacts/web/src/components/marketing/FourPillarsSection.tsx && grep -q "min-w-0" artifacts/web/src/components/marketing/FourPillarsSection.tsx && ! grep -rE "bg-primary|text-foreground|font-sans\b|bg-card|border-border" artifacts/web/src/components/marketing/ValuePropSection.tsx artifacts/web/src/components/marketing/FourPillarsSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ValuePropSection renders `t.value_prop.title` (`<h2>`) and `t.value_prop.subtitle` (`<p>`), centered, max-w-640px.
    - FourPillarsSection renders exactly 4 PillarCards; Image and Video cards render a badge from `t.pillars.image_coming_soon` / `t.pillars.video_coming_soon`; Chat and Voice render no badge.
    - Pillar grid uses `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` and every card has `min-w-0`.
    - Both files contain zero fan-page utilities; fonts via inline style (grep gate passes).
  </acceptance_criteria>
  <done>Value-prop statement renders; four pillars show chat/voice live and image/video coming-soon badges; grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: DemoTranscriptSection (MKT-06)</name>
  <files>artifacts/web/src/components/marketing/DemoTranscriptSection.tsx</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (DemoTranscriptSection lines 521-574 — static transcript constant + bubble adaptation)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Contract 10 DemoTranscript lines 417-444; Copywriting guardrails lines 534-539)
    - artifacts/web/src/components/fan/MessageBubble.tsx (analog: bubble alignment by role — read for structure only, swap tokens)
    - artifacts/web/src/index.css lines 403-451 (@layer marketing-tokens)
  </read_first>
  <action>
    Create `DemoTranscriptSection.tsx` exporting `DemoTranscriptSection({ t })`. Section wrapper + `<h2>` `t.demo.title`. Define a hardcoded English `DEMO_TRANSCRIPT` constant (2 turns) per 06-PATTERNS: fan "Are you really her?" / twin "I'm her AI twin — same energy, available whenever you are. The real one will check in too 💜". Render a `max-w-lg mx-auto rounded-[--mkt-radius-lg] border border-[--mkt-border] bg-[--mkt-surface-1] p-6 flex flex-col gap-4` card. Fan turns right-aligned `bg-[--mkt-surface-2]` Small; twin turns left-aligned with `background: color-mix(in oklch, var(--mkt-accent) 15%, transparent)` Small, followed by an attribution line "AI twin · @claire_ai" Small `text-[--mkt-muted-fg]`. Comply with MKT-19 guardrails: do NOT name a real creator beyond the generic "@claire_ai" handle placeholder (acceptable per UI-SPEC contract example), never use "indistinguishable"/"real person"/"fans won't know". The AI attribution must be present on the twin bubble. Phase 6 shows the EN transcript for all locales (Phase 7 localizes). Fonts via inline style only.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -q "demo.title" artifacts/web/src/components/marketing/DemoTranscriptSection.tsx && grep -q "AI twin" artifacts/web/src/components/marketing/DemoTranscriptSection.tsx && ! grep -iE "indistinguishable|fans won't know|real person|authentic human" artifacts/web/src/components/marketing/DemoTranscriptSection.tsx && ! grep -E "bg-primary|text-foreground|font-sans\b|bg-card|border-border" artifacts/web/src/components/marketing/DemoTranscriptSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DemoTranscriptSection renders `t.demo.title` heading and a 2-turn static transcript card.
    - Twin bubble carries a visible "AI twin · @claire_ai" attribution line.
    - No prohibited copy (`indistinguishable`, `fans won't know`, `real person`, `authentic human`) appears (MKT-19).
    - Zero fan-page utilities; fonts via inline style (grep gate passes).
  </acceptance_criteria>
  <done>Static demo transcript card renders with AI attribution and compliant copy; grep gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n namespace → DOM | Localized copy rendered as React text children (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering (XSS) | section copy rendering | mitigate | All copy rendered as React JSX text children (auto-escaped); no `dangerouslySetInnerHTML` anywhere — enforced by acceptance criteria (sections read `t.*` and a static constant only) |
| T-06-05 | Repudiation/Compliance | demo transcript + hero pill copy | mitigate | MKT-19 copy guardrails enforced via grep gate (no "real person"/"indistinguishable"); AI attribution present |
| T-06-SC | Tampering | npm/pnpm installs | accept | No new packages installed; lucide-react + framer-motion already present (RESEARCH) |

No HIGH threats: static rendering, no user input, no `dangerouslySetInnerHTML`, no network calls.
</threat_model>

<verification>
- `grep -rn "bg-primary\|bg-background\|bg-card\|text-foreground\|text-primary\|border-border\|font-sans" artifacts/web/src/components/marketing/` returns zero new matches in this plan's files.
- Each of the 4 components exports its named function.
- FourPillars badges read from the i18n coming-soon keys.
- No prohibited MKT-19 copy in any file.
</verification>

<success_criteria>
- MKT-01 hero: localized headline + sub + bloom + single primary CTA, LCP-safe.
- MKT-02 value-prop statement renders.
- MKT-03 four pillars: chat/voice live, image/video "coming soon".
- MKT-06 static localized demo transcript with AI attribution.
</success_criteria>

<output>
Create `.planning/phases/06-marketing-components-navigation/06-02-SUMMARY.md` when done.
</output>
