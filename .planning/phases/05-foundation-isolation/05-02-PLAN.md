---
phase: 05-foundation-isolation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/web/public/sitemap.xml
  - artifacts/web/public/robots.txt
  - artifacts/web/public/og-marketing.png
autonomous: true
requirements: [MKT-16, MKT-17]
must_haves:
  truths:
    - "public/sitemap.xml lists exactly the 3 locale URLs (/en, /ja, /zh-TW) with hreflang alternates including x-default=en"
    - "public/robots.txt allows the 3 marketing roots and disallows two-segment fan chat paths and /payment/"
    - "public/og-marketing.png exists at 1200x630 with no creator likeness, ready to be served at https://lala.la/og-marketing.png"
    - "All three assets are copied verbatim into dist/public/ by the Vite build"
  artifacts:
    - path: "artifacts/web/public/sitemap.xml"
      provides: "Static sitemap with 3 locale URLs + hreflang alternates"
      contains: "https://lala.la/zh-TW"
    - path: "artifacts/web/public/robots.txt"
      provides: "Crawler directives allowing marketing roots, disallowing fan pages"
      contains: "Sitemap: https://lala.la/sitemap.xml"
    - path: "artifacts/web/public/og-marketing.png"
      provides: "1200x630 brand OG card (logo + tagline, no creator likeness)"
  key_links:
    - from: "artifacts/web/public/sitemap.xml"
      to: "wouter /:locale route shape"
      via: "loc URLs match /en, /ja, /zh-TW exactly"
      pattern: "https://lala.la/(en|ja|zh-TW)"
    - from: "artifacts/web/public/robots.txt"
      to: "public/sitemap.xml"
      via: "Sitemap directive references the committed sitemap URL"
      pattern: "Sitemap: https://lala.la/sitemap.xml"
---

<objective>
Commit the three static SEO assets that crawlers consume directly from `dist/public/`:
the locale sitemap, the robots directives, and the Open Graph share card. These are
file-only deliverables with no code dependencies, so they run in parallel with Plan 01.

Purpose: Social-card scrapers and search crawlers do not execute JavaScript. The sitemap
(3 locale URLs + hreflang), robots.txt (allow marketing roots, block fan pages), and the
og-marketing.png referenced by the index.html `og:image` (committed in Plan 03) must exist
as raw static files served at stable public URLs.

Output: `public/sitemap.xml`, `public/robots.txt` (replaced), and `public/og-marketing.png`
(new). The og:image meta tag and hreflang `<link>` tags that reference these are added in
Plan 03; this plan guarantees the referenced files exist first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-foundation-isolation/05-UI-SPEC.md
@.planning/phases/05-foundation-isolation/05-PATTERNS.md

<interfaces>
<!-- The exact sitemap.xml and robots.txt content are LOCKED verbatim in 05-UI-SPEC.md -->
<!--   § "Static SEO Assets Contract" (lines 443-509) and 05-PATTERNS.md (lines 492-556). -->
<!-- Static-asset delivery: files in artifacts/web/public/ are copied as-is into -->
<!--   dist/public/ at the root path (public/sitemap.xml -> https://lala.la/sitemap.xml). -->
<!-- Existing public/: favicon.svg, opengraph.jpg (22KB JPG — distinct from the new -->
<!--   og-marketing.png), robots.txt (currently "Allow: /"). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write public/sitemap.xml with 3 locale URLs + hreflang alternates (MKT-17)</name>
  <files>artifacts/web/public/sitemap.xml</files>
  <read_first>
    - .planning/phases/05-foundation-isolation/05-UI-SPEC.md § "Static SEO Assets Contract" → `public/sitemap.xml` (lines 454-487) — copy this XML verbatim
    - .planning/phases/05-foundation-isolation/05-PATTERNS.md § "artifacts/web/public/sitemap.xml" (lines 519-556) for the verbatim file content and delivery note
    - artifacts/web/public/robots.txt (existing analog for the public/ delivery mechanism)
  </read_first>
  <action>
    Create new file `artifacts/web/public/sitemap.xml` with the exact content from
    05-UI-SPEC.md lines 455-487. It is a `<urlset>` with `xmlns:xhtml`, containing exactly
    three `<url>` entries with `<loc>` values `https://lala.la/en` (priority 1.0),
    `https://lala.la/ja` (0.9), and `https://lala.la/zh-TW` (0.9). Each `<url>` carries four
    `<xhtml:link rel="alternate">` entries: hreflang en, ja, zh-TW, and x-default=https://lala.la/en
    (per D-05-06, EN is x-default). The loc URLs MUST match the wouter `/:locale` route shape
    exactly (`/en`, `/ja`, `/zh-TW`) — no trailing slash, no handle segment. Do not add a fourth
    URL, do not list fan handles, do not list /onboard /dashboard /account or any internal route
    (those must not be exposed to crawlers — see threat model).
  </action>
  <verify>
    <automated>cd artifacts/web && test -f public/sitemap.xml && grep -c '<loc>' public/sitemap.xml | grep -qx 3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<loc>' artifacts/web/public/sitemap.xml` returns exactly 3
    - The three loc values are exactly `https://lala.la/en`, `https://lala.la/ja`, `https://lala.la/zh-TW`: `grep -oE 'https://lala.la/(en|ja|zh-TW)</loc>' artifacts/web/public/sitemap.xml | sort -u` lists exactly those three
    - `grep -c 'hreflang="x-default"' artifacts/web/public/sitemap.xml` returns 3 (one per url block), each pointing to https://lala.la/en
    - No internal route appears: `grep -E '/onboard|/dashboard|/account|/payment' artifacts/web/public/sitemap.xml` returns nothing
    - File is well-formed XML: `python3 -c "import xml.dom.minidom,sys; xml.dom.minidom.parse('artifacts/web/public/sitemap.xml')"` exits 0
  </acceptance_criteria>
  <done>sitemap.xml exists with exactly the 3 locale URLs and hreflang alternates; no internal/fan routes leak; valid XML.</done>
</task>

<task type="auto">
  <name>Task 2: Replace public/robots.txt with marketing allow / fan-page disallow directives (MKT-17)</name>
  <files>artifacts/web/public/robots.txt</files>
  <read_first>
    - artifacts/web/public/robots.txt (current 2-line "Allow: /" — to be replaced in place)
    - .planning/phases/05-foundation-isolation/05-UI-SPEC.md § "Static SEO Assets Contract" → `public/robots.txt` (lines 489-503) — copy verbatim
    - .planning/phases/05-foundation-isolation/05-PATTERNS.md § "artifacts/web/public/robots.txt" (lines 492-515) for the trailing-slash rationale
  </read_first>
  <action>
    Replace the entire contents of `artifacts/web/public/robots.txt` with the locked content:
    `User-agent: *`, then `Allow: /en`, `Allow: /ja`, `Allow: /zh-TW` (marketing roots crawlable),
    then `Disallow: /en/`, `Disallow: /ja/`, `Disallow: /zh-TW/` (trailing slash blocks two-segment
    fan chat paths like /en/claire), then `Disallow: /payment/`, then
    `Sitemap: https://lala.la/sitemap.xml`. The trailing slash on the Disallow lines is
    load-bearing: it keeps `/en` (marketing root) crawlable while blocking `/en/<handle>` fan pages.
    Do not deanonymize any creator handle or list internal admin/onboard routes individually.
  </action>
  <verify>
    <automated>cd artifacts/web && grep -q '^Disallow: /en/$' public/robots.txt && grep -q '^Sitemap: https://lala.la/sitemap.xml$' public/robots.txt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^Allow: /(en|ja|zh-TW)$' artifacts/web/public/robots.txt` returns 3
    - `grep -cE '^Disallow: /(en|ja|zh-TW)/$' artifacts/web/public/robots.txt` returns 3 (trailing slash present)
    - `grep -q '^Disallow: /payment/$' artifacts/web/public/robots.txt` succeeds
    - `grep -q '^Sitemap: https://lala.la/sitemap.xml$' artifacts/web/public/robots.txt` succeeds
    - No creator handle string or `/onboard`/`/dashboard`/`/account` path is listed: `grep -E 'claire|onboard|dashboard|account' artifacts/web/public/robots.txt` returns nothing
  </acceptance_criteria>
  <done>robots.txt allows the three marketing roots, disallows two-segment fan paths and /payment/, and references the sitemap; no handles or internal routes leak.</done>
</task>

<task type="auto">
  <name>Task 3: Create public/og-marketing.png — 1200x630 brand card, no creator likeness (MKT-16)</name>
  <files>artifacts/web/public/og-marketing.png</files>
  <read_first>
    - .planning/phases/05-foundation-isolation/05-UI-SPEC.md § "Static SEO Assets Contract" → `public/og-marketing.png` (lines 447-452) for dimensions, content, and the no-creator-likeness rule (D-05-04)
    - .planning/phases/05-foundation-isolation/05-PATTERNS.md § "No Analog Found" (lines 601-607) for the ImageMagick placeholder command and the brand-card constraint
  </read_first>
  <action>
    Create `artifacts/web/public/og-marketing.png` as a 1200x630 PNG. Content per D-05-04:
    `lala.la` wordmark + the brand tagline ("Your AI twin, fully managed") on a solid brand-color
    background. NO creator likeness, NO Claire photo, NO asset from the private Object Storage
    bucket — this is a brand-level static card only (marketing-use authorization is not yet on file).
    Target file size under 300 KB. Prefer ImageMagick if available:
    `convert -size 1200x630 xc:#7c3aed -pointsize 96 -fill white -gravity center -annotate +0-40 "lala.la" -pointsize 40 -annotate +0+60 "Your AI twin, fully managed" artifacts/web/public/og-marketing.png`.
    First check `command -v convert` (or `magick`). If ImageMagick is unavailable, generate the
    PNG via a Node script using the `canvas` package only if already installed, otherwise produce a
    minimal valid 1200x630 PNG (solid color + text) by any available tool. A placeholder card is
    explicitly acceptable for Phase 5 (final artwork is Phase 7). The file MUST exist and be a valid
    1200x630 PNG so the og:image URL (added in Plan 03) does not 404.
  </action>
  <verify>
    <automated>cd artifacts/web && test -f public/og-marketing.png && file public/og-marketing.png | grep -qi 'PNG image data, 1200 x 630' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `file artifacts/web/public/og-marketing.png` reports `PNG image data, 1200 x 630`
    - File size under 300 KB: `test $(stat -c%s artifacts/web/public/og-marketing.png) -lt 307200`
    - The file is distinct from the existing `public/opengraph.jpg` (different filename, PNG not JPG)
    - No creator photo used: the asset was generated programmatically from brand text/color only (assert in SUMMARY which tool produced it)
  </acceptance_criteria>
  <done>A valid 1200x630 PNG brand card exists at public/og-marketing.png, under 300KB, containing no creator likeness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static public/ asset → external crawler/scraper | robots.txt and sitemap.xml are read by untrusted third-party crawlers; they must expose only intended public marketing URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | sitemap.xml / robots.txt route exposure | mitigate | sitemap lists only /en,/ja,/zh-TW marketing roots; robots Disallow blocks two-segment fan paths (/en/<handle>) and /payment/; acceptance criteria grep-assert that no handle or internal route (onboard/dashboard/account) is listed — prevents fan-handle deanonymization |
| T-05-05 | Information Disclosure | og-marketing.png leaking creator likeness | mitigate | Brand-only card (logo + tagline); D-05-04 prohibits creator imagery; not sourced from private Object Storage; static public asset only |
| T-05-06 | Tampering | static meta/SEO content integrity | accept | All content is hardcoded English strings authored in source; no user input flows into sitemap/robots/og — no injection surface |

No new endpoints, no auth, no crypto. ASVS V2-V6 do not apply.
**No HIGH-severity threats. Gate passes.**
</threat_model>

<verification>
- sitemap.xml: exactly 3 locale URLs, hreflang alternates, valid XML, no internal routes
- robots.txt: allow 3 roots, disallow fan two-segment paths + /payment/, sitemap referenced
- og-marketing.png: valid 1200x630 PNG, <300KB, no creator likeness
- `pnpm --filter @workspace/web run build` copies all three into dist/public/ verbatim
</verification>

<success_criteria>
- ROADMAP Phase 5 success criterion 4 (og-marketing.png, sitemap.xml 3 URLs, robots.txt committed and served) satisfied
- Assets exist at stable public URLs ready for the index.html references added in Plan 03
</success_criteria>

<output>
Create `.planning/phases/05-foundation-isolation/05-02-SUMMARY.md` when done
</output>
