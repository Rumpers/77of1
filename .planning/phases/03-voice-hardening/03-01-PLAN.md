---
phase: 03-voice-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - artifacts/api-server/package.json
  - artifacts/worker/package.json
  - lib/twin-runtime/src/locale.ts
  - lib/twin-runtime/src/index.ts
  - lib/twin-runtime/src/__tests__/locale.test.ts
autonomous: false
requirements: [VOICE-01, I18N-01]
tags: [voice, opossum, circuit-breaker, locale, gmi-tts, replit-object-storage]

user_setup:
  - service: gmi-cloud-tts
    why: "VOICE-01 requires the production GMI Cloud TTS endpoint + model identifier + request shape. The catalog lists 5+ models (MiniMax 2.6 HD/Turbo, Step Audio Edit X, Chatterbox) but the public docs do NOT publish the API path or request schema. Wave 0 must lock these BEFORE 03-06 writes the provider."
    env_vars:
      - name: GMI_TTS_ENDPOINT_PATH
        source: "Founder: run `curl` test against GMI's audio endpoint with GMI_API_KEY and document the path (e.g., /v1/audio/tts). Add to Replit Secrets if it differs from the chat-completions base."
      - name: GMI_TTS_MODEL_ID
        source: "Founder: confirm which voice-clone model to use (recommend step-audio-edit-x or minimax-audio-voice-clone-speech-2.6-turbo per RESEARCH §Standard Stack)."
      - name: VOICE_URL_SIGNING_SECRET
        source: "Founder: generate 64-char hex (`openssl rand -hex 32`) and add to Replit Secrets. Used by 03-07 voice-token proxy."
    dashboard_config:
      - task: "Verify GMI Cloud account has TTS access enabled and the documented model is reachable from Replit IP range."
        location: "GMI Cloud dashboard"

must_haves:
  truths:
    - "opossum @9.x is approved by founder via package-legitimacy checkpoint before any install"
    - "@replit/object-storage is approved by founder via package-legitimacy checkpoint and installed into @workspace/api-server AND @workspace/worker as part of Wave 0"
    - "GMI TTS endpoint + model + request shape is documented in repo (RESEARCH log or new file) before 03-06 begins"
    - "VOICE_URL_SIGNING_SECRET is set in Replit Secrets (≥32 chars)"
    - "normalizeLocale() converts case-variant inputs ('zh-tw', 'zh-TW', 'ja-JP', 'en-US') to the canonical Locale type used by twin-runtime"
  artifacts:
    - path: "lib/twin-runtime/src/locale.ts"
      provides: "normalizeLocale exported and re-exported from lib/twin-runtime/src/index.ts"
      contains: "export function normalizeLocale"
    - path: "lib/twin-runtime/src/__tests__/locale.test.ts"
      provides: "Unit tests for normalizeLocale covering Hermes 'zh-tw' → 'zh-TW' coercion"
    - path: ".planning/phases/03-voice-hardening/03-01-GMI-TTS-CONTRACT.md"
      provides: "Founder-confirmed GMI TTS endpoint, model id, request body shape, response shape; OR explicit 'voice deferred to Phase 3.5' decision"
    - path: "artifacts/api-server/package.json"
      provides: "@replit/object-storage dependency entry"
      contains: "@replit/object-storage"
    - path: "artifacts/worker/package.json"
      provides: "@replit/object-storage dependency entry"
      contains: "@replit/object-storage"
  key_links:
    - from: "Hermes 'zh-tw' string keys"
      to: "twin-runtime Locale type 'zh-TW'"
      via: "normalizeLocale()"
      pattern: "normalizeLocale\\("
---

<objective>
Wave 0 unblock: founder-side gates that must clear before any other Phase 3 work begins.

Purpose: VOICE-* and the voice-token proxy cannot be implemented until the GMI TTS contract is known and `VOICE_URL_SIGNING_SECRET` is provisioned. The opossum package must clear the supply-chain gate before install. The `@replit/object-storage` SDK is used by both 03-04 (DSAR sweep) and 03-06 (voice upload) and must clear the same supply-chain gate (CONTEXT D-8 did not exempt any package class). I18N consumer drift (Hermes 'zh-tw' vs twin-runtime 'zh-TW') needs a single normalizer before either DSAR/mask-review or voice work consumes Hermes locale strings.

Output: founder checkpoints recorded, secrets provisioned, @replit/object-storage installed into api-server + worker, locale normalizer shipped with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-voice-hardening/03-CONTEXT.md
@.planning/phases/03-voice-hardening/03-RESEARCH.md
@.planning/phases/03-voice-hardening/03-PATTERNS.md
@CLAUDE.md
@lib/twin-runtime/src/locale.ts
@lib/twin-runtime/src/index.ts

<interfaces>
From `lib/twin-runtime/src/locale.ts` (current state):
```typescript
export type Locale = "en" | "ja" | "zh-TW";
export const SUPPORTED_LOCALES: readonly Locale[] = ["en", "ja", "zh-TW"];
export const DEFAULT_LOCALE: Locale = "en";
// matchLocaleTag(tag: string): Locale | null  — INTERNAL (line 23)
export function detectLocale(...): Locale  // line 49
```

From `artifacts/hermes/src/i18n.ts` (current state):
```typescript
export type Lang = 'en' | 'ja' | 'zh-tw';   // ← LOWERCASE — drift from BCP-47
```

Target shape after this plan:
```typescript
// lib/twin-runtime/src/locale.ts (additions)
export function normalizeLocale(input: string | null | undefined): Locale;
// Accepts: 'en', 'EN', 'en-US', 'ja', 'ja-JP', 'zh-tw', 'zh-TW', 'zh-Hant-TW', null
// Returns: 'en' (default for unrecognized), 'ja', or 'zh-TW'
// Re-exports `matchLocaleTag` as the underlying matcher.
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — opossum + @types/opossum</name>
  <what-built>
    Per RESEARCH §Package Legitimacy Audit, `opossum@9.0.0` (Apache-2.0, Node.js Foundation nodeshift team, slopcheck [OK]) and `@types/opossum@4.x` (DefinitelyTyped, slopcheck [OK]) are the new packages Phase 3 adds. These are the ONLY new package adds across the entire phase (per RESEARCH §Installation Reference). `minimax-voice-clone` is a hallucinated package — flagged [SLOP] — must NEVER be referenced.
  </what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/opossum — confirm latest version is 9.x, weekly downloads >100k, repo points to github.com/nodeshift/opossum.
    2. Visit https://www.npmjs.com/package/@types/opossum — confirm DefinitelyTyped publishes it, version 4.x available.
    3. Confirm Apache-2.0 license on opossum repo README.
    4. (Negative) Confirm `npm view minimax-voice-clone` returns "not in registry" — this package must NEVER appear in any task.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize opossum + @types/opossum install in 03-06, or "rejected" with rationale.</resume-signal>
  <read_first>
    - .planning/phases/03-voice-hardening/03-RESEARCH.md (§Package Legitimacy Audit)
  </read_first>
  <acceptance_criteria>
    - Founder response recorded as "approved" or "rejected".
    - If rejected: plan 03-06 must switch to hand-rolled 30-line state machine (RESEARCH §Alternatives Considered) and document the deviation.
  </acceptance_criteria>
  <done>Founder has explicitly approved or rejected the opossum + @types/opossum supply-chain entry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Founder gate: @replit/object-storage supply-chain approval + install</name>
  <what-built>
    `@replit/object-storage` is the SDK both 03-04 Task 3 (DSAR sweep — list+delete `creators/{id}/*`) and 03-06 Task 3 (voice generation upload) depend on. CONTEXT D-8 gated `opossum` without exempting any package class, so this Replit-first-party SDK still requires an explicit founder acknowledgement before install. Approving here lifts the install out of the worker plans and into a deterministic Wave-0 step: both `@workspace/api-server` and `@workspace/worker` receive the SDK in this task.

    Why a one-line acknowledgement (not a full slopcheck): `@replit/object-storage` is published by Replit and required by the Replit Object Storage product itself; verifying its npm page is the entirety of the legitimacy work. The acknowledgement closes the supply-chain audit trail.
  </what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/@replit/object-storage — confirm publisher is `replit`, latest version exists, and the package description matches the official Replit Object Storage product.
    2. Type "approved" to authorize the install in step 3 below, or "rejected" with rationale (rejection switches 03-04 + 03-06 to the raw-fetch fallback documented in their tasks).
    3. On "approved", Claude runs these installs as part of this same task (single transaction):
       ```
       pnpm --filter @workspace/api-server add @replit/object-storage
       pnpm --filter @workspace/worker add @replit/object-storage
       ```
       Then verifies both `artifacts/api-server/package.json` and `artifacts/worker/package.json` contain a `@replit/object-storage` dependency entry.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the supply-chain entry + install, or "rejected" with rationale.</resume-signal>
  <read_first>
    - CLAUDE.md (§Object Storage — Replit Object Storage is the mandated bucket)
    - .planning/phases/03-voice-hardening/03-CONTEXT.md (D-8 — package legitimacy gate scope)
  </read_first>
  <acceptance_criteria>
    - Founder response recorded as "approved" or "rejected".
    - On "approved": `grep -c "@replit/object-storage" artifacts/api-server/package.json` >= 1 AND `grep -c "@replit/object-storage" artifacts/worker/package.json` >= 1.
    - On "approved": `pnpm install --frozen-lockfile` exits 0 after the adds (no peer-dep conflicts).
    - On "rejected": planner must update 03-04 Task 3 and 03-06 Task 3 to use the raw-fetch fallback path documented in their plans.
  </acceptance_criteria>
  <done>@replit/object-storage approved + installed into both api-server and worker package.json, OR explicit rejection logged with downstream raw-fetch fallback path activated.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: GMI TTS contract confirmation + VOICE_URL_SIGNING_SECRET provisioning</name>
  <what-built>
    Two founder-only actions Claude cannot automate:

    (a) **GMI TTS endpoint contract:** Confirm the production endpoint path, model identifier, auth header, request body shape, and response shape. Per RESEARCH §Common Pitfalls #1 and §Open Questions #1, this is the highest-risk unknown in Phase 3. The catalog at docs.gmicloud.ai lists models but not API contracts.

    (b) **VOICE_URL_SIGNING_SECRET:** A 64-character hex secret used by the voice-token HMAC proxy (03-07). Cannot be generated by Claude (must be added to Replit Secrets which Claude has no access to).
  </what-built>
  <how-to-verify>
    1. Open a terminal with GMI_API_KEY in env. Try one of:
       ```
       curl -sS -X POST "https://api.gmi-serving.com/v1/audio/speech" \
         -H "Authorization: Bearer $GMI_API_KEY" \
         -H "Content-Type: application/json" \
         -d '{"model":"step-audio-edit-x","input":"Hello","voice":"alloy"}'
       ```
       OR contact GMI support and request: (a) audio endpoint URL, (b) supported zero-shot voice-clone models (excluding ElevenLabs variants per CLAUDE.md), (c) example request body, (d) example response body (sync bytes vs async job ID).
    2. Write the confirmed contract to `.planning/phases/03-voice-hardening/03-01-GMI-TTS-CONTRACT.md`. Required fields: `endpoint_path`, `model_id`, `auth_header_format`, `request_body_schema`, `response_body_schema`, `supports_signed_reference_url` (true/false), `sync_or_async` (sync/async-job).
    3. Generate the signing secret: `openssl rand -hex 32` (produces 64-char hex).
    4. Add to Replit Secrets:
       - `VOICE_URL_SIGNING_SECRET` = (the 64-char hex from step 3)
       - `GMI_TTS_ENDPOINT_PATH` = (the path from step 1, e.g., `/v1/audio/speech`)
       - `GMI_TTS_MODEL_ID` = (the model from step 1)
    5. Reply with the contents of `03-01-GMI-TTS-CONTRACT.md` so the planner record contains it.
  </how-to-verify>
  <resume-signal>Reply with either: (a) "contract confirmed" + paste the contract file contents, or (b) "voice deferred to Phase 3.5" if GMI support is unreachable.</resume-signal>
  <read_first>
    - .planning/phases/03-voice-hardening/03-RESEARCH.md (§Common Pitfalls #1, §Open Questions #1, §Assumptions Log A1–A3)
    - lib/providers/src/providers/gmi-client.ts (the analog for GMI HTTP shape)
  </read_first>
  <acceptance_criteria>
    - File `.planning/phases/03-voice-hardening/03-01-GMI-TTS-CONTRACT.md` exists OR an explicit "voice deferred to Phase 3.5" decision is logged in CONTEXT addendum.
    - `VOICE_URL_SIGNING_SECRET` exists in Replit Secrets (founder confirms).
    - If deferred: planner must split-mark plans 03-06 and 03-07 as DEFERRED in subsequent planning step; 03-08 must drop voice-related verification.
  </acceptance_criteria>
  <done>GMI TTS contract documented OR voice path explicitly deferred; signing secret provisioned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Export `normalizeLocale` from twin-runtime</name>
  <files>lib/twin-runtime/src/locale.ts, lib/twin-runtime/src/index.ts, lib/twin-runtime/src/__tests__/locale.test.ts</files>
  <behavior>
    - normalizeLocale("en") → "en"
    - normalizeLocale("EN") → "en"
    - normalizeLocale("en-US") → "en"
    - normalizeLocale("ja") → "ja"
    - normalizeLocale("ja-JP") → "ja"
    - normalizeLocale("zh-tw") → "zh-TW"  (Hermes-flavor lowercase)
    - normalizeLocale("zh-TW") → "zh-TW"
    - normalizeLocale("zh-Hant-TW") → "zh-TW"
    - normalizeLocale("zh-CN") → "en"  (unsupported simplified Chinese; falls back to default)
    - normalizeLocale("") → "en"
    - normalizeLocale(null) → "en"
    - normalizeLocale(undefined) → "en"
  </behavior>
  <read_first>
    - lib/twin-runtime/src/locale.ts  (current file — extend, do NOT replace)
    - lib/twin-runtime/src/index.ts  (barrel re-exports)
    - .planning/phases/03-voice-hardening/03-RESEARCH.md (§Common Pitfalls #6 — locale drift)
    - .planning/phases/03-voice-hardening/03-PATTERNS.md (§Pattern: Workspace import paths)
  </read_first>
  <action>
    Extend `lib/twin-runtime/src/locale.ts`:
    1. The internal `matchLocaleTag(tag: string): Locale | null` already exists (line 23). Export a NEW public function `normalizeLocale(input: string | null | undefined): Locale` that: returns `DEFAULT_LOCALE` ('en') for null/undefined/empty; otherwise calls `matchLocaleTag(input)` and returns the match or `DEFAULT_LOCALE` for non-matches.
    2. Add `export { matchLocaleTag }` at the bottom of `locale.ts` so cross-lib consumers (Hermes, fan-twin) can use it without re-implementing the BCP-47 match logic.
    3. Add `export { normalizeLocale, matchLocaleTag } from "./locale.js"` to `lib/twin-runtime/src/index.ts` AND to `lib/twin-runtime/src/locale.ts` re-exports if a `locale` subpath export is already in `package.json` (verify by reading `lib/twin-runtime/package.json` exports map; do not invent a new subpath).
    4. Create `lib/twin-runtime/src/__tests__/locale.test.ts` with Vitest tests covering every case in the `<behavior>` block above. Follow the existing Vitest pattern used by `lib/twin-runtime/src/__tests__/moderation.test.ts` (read first).
    5. No production callers are wired in this task — wiring happens in 03-04 (Hermes DSAR scene) and 03-05 (mask review scene). This task ships the primitive only.
  </action>
  <verify>
    <automated>pnpm --filter @workspace/twin-runtime exec vitest run src/__tests__/locale.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function normalizeLocale" lib/twin-runtime/src/locale.ts` returns exactly one match.
    - `grep -n "normalizeLocale" lib/twin-runtime/src/index.ts` returns at least one match (re-export).
    - The new test file imports `normalizeLocale` from the workspace package (NOT a relative path from outside `lib/twin-runtime`) and all 11 cases above pass.
    - `pnpm run typecheck:libs` exits 0.
  </acceptance_criteria>
  <done>normalizeLocale is exported, tested, and ready for Hermes/fan-twin to call when crossing into twin-runtime locale-typed APIs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Founder ↔ GMI Cloud | API credentials and TTS contract negotiation — outside repo |
| Founder ↔ Replit Secrets | Signing secret never enters git; only Replit env var |
| Hermes string keys ↔ twin-runtime Locale type | Cross-module locale identifier drift (Pitfall 6) |
| npm registry ↔ pnpm install | Supply-chain entry for opossum + @replit/object-storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Founder pastes wrong GMI contract → bad downstream provider impl | mitigate | Founder writes contract to `03-01-GMI-TTS-CONTRACT.md` reviewed before 03-06 implementation begins; 03-06 must read this file as its first task |
| T-03-01-02 | Information disclosure | VOICE_URL_SIGNING_SECRET leaks (logged accidentally, committed to git) | mitigate | Secret added ONLY to Replit Secrets; never appears in any committed file. 03-07 code must access via `process.env.VOICE_URL_SIGNING_SECRET` only |
| T-03-01-SC | Tampering | npm install of compromised opossum | mitigate | Blocking human checkpoint (Task 1); slopcheck [OK] verified; Apache-2.0 / Node Foundation backing |
| T-03-01-SC2 | Tampering | npm install of compromised @replit/object-storage | mitigate | Blocking human checkpoint (Task 2); Replit-first-party publisher verified on npm before install |
| T-03-01-03 | Spoofing | locale spoofing causes wrong-locale crisis helpline injection | accept | `normalizeLocale` falls back to 'en' for unknown; 03-05/03-04 will pass through to twin-runtime which already has SB 243 helpline-locale-routing logic |
</threat_model>

<verification>
- Task 1 + 2 + 3 checkpoints: founder reply captured in session transcript.
- Task 4: Vitest passes; typecheck passes.
- Manual: `cat .planning/phases/03-voice-hardening/03-01-GMI-TTS-CONTRACT.md` returns content OR a deferral marker exists.
- Manual: `grep -c "@replit/object-storage" artifacts/api-server/package.json artifacts/worker/package.json` returns >= 2 lines (one per file).
</verification>

<success_criteria>
- opossum + @types/opossum approved (or rejected with documented fallback).
- @replit/object-storage approved + installed into api-server + worker (or rejected with raw-fetch fallback documented).
- GMI TTS contract documented in repo OR voice-path deferral logged.
- VOICE_URL_SIGNING_SECRET exists in Replit Secrets.
- `normalizeLocale("zh-tw") === "zh-TW"` is true at test time.
</success_criteria>

<output>
Create `.planning/phases/03-voice-hardening/03-01-SUMMARY.md` when done, including: opossum decision, @replit/object-storage decision + install result, GMI contract status (confirmed/deferred), normalizeLocale test result.
</output>
</content>
</invoke>