---
phase: quick-260601-f4l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/web/src/index.css
  - artifacts/web/src/main.tsx
  - artifacts/web/package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [QUICK-260601-f4l]
must_haves:
  truths:
    - "Marketing surface tokens (--mkt-*) carry the Luminous Infrastructure violet-pop values from DESIGN.md"
    - "Three new self-hosted Fontsource families (Bricolage Grotesque, Geist, Noto Sans TC) install and import without module-not-found errors"
    - "The fan chat page (:root / .dark / @theme) is byte-for-byte unchanged"
    - "Every --mkt-* declaration remains inside the [data-surface=\"marketing\"] scope only"
  artifacts:
    - path: "artifacts/web/src/index.css"
      provides: "Retrofitted --mkt-* token values inside marketing-tokens layer"
      contains: "--mkt-glow-from"
    - path: "artifacts/web/src/main.tsx"
      provides: "Three additional @fontsource imports alongside the existing two"
      contains: "@fontsource-variable/geist"
    - path: "artifacts/web/package.json"
      provides: "Three new fontsource dependencies"
      contains: "@fontsource-variable/bricolage-grotesque"
  key_links:
    - from: "artifacts/web/src/index.css"
      to: "[data-surface=\"marketing\"]"
      via: "all --mkt-* live only inside this selector inside @layer marketing-tokens"
      pattern: "data-surface=.marketing"
---

<objective>
Retrofit the marketing site's `--mkt-*` design tokens in `artifacts/web/src/index.css` to the exact values from DESIGN.md ("Luminous Infrastructure" violet-pop), and load three new self-hosted Fontsource families used by that design (Bricolage Grotesque display, Geist body, Noto Sans TC CJK).

Purpose: Land the foundation token values + font loading so Phase 6 marketing components can consume real tokens. This is foundation-only — no marketing component consumes these tokens yet, so there is no visual/browser check in scope (that is Phase 6 territory).

Output: Updated `index.css` marketing-tokens block, three new font imports in `main.tsx`, three new deps in `package.json` + refreshed `pnpm-lock.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@DESIGN.md

<current_state>
`artifacts/web/src/index.css` lines 403-438 hold the EXISTING marketing-tokens block with PLACEHOLDER values:

```
@layer marketing-tokens {
  [data-surface="marketing"] {
    --mkt-bg:            #ffffff;
    --mkt-fg:            #111111;
    --mkt-surface-1:     #f5f5f5;
    --mkt-surface-2:     #ebebeb;
    --mkt-border:        #e0e0e0;
    --mkt-accent:        hsl(263 80% 58%);
    --mkt-accent-fg:     #ffffff;
    --mkt-accent-hover:  hsl(263 80% 50%);
    --mkt-muted-fg:      #666666;
    --mkt-radius-sm:     0.375rem;
    --mkt-radius-md:     0.625rem;
    --mkt-radius-lg:     1rem;
    --mkt-font-sans:     'Inter Variable', 'Noto Sans JP Variable', system-ui, ...;
    --mkt-text-*  / --mkt-leading-*  / --mkt-spacing-*   (KEEP EXACTLY)
  }
}
```

`artifacts/web/src/main.tsx` current font imports (lines 2-3):
```
import '@fontsource-variable/inter';
import '@fontsource-variable/noto-sans-jp';
```

`artifacts/web/package.json` dependencies currently include only `@fontsource-variable/inter` (^5.2.8) and `@fontsource-variable/noto-sans-jp` (^5.2.10).
</current_state>

<verified_packages>
Confirmed to EXIST on the npm registry (checked 2026-06-01 — executor must still re-confirm before install):
- @fontsource-variable/bricolage-grotesque (latest 5.2.10) — variable, display
- @fontsource-variable/geist (latest 5.2.9) — variable, body. NOTE: package is `geist`, NOT `geist-sans`. The variable form `@fontsource-variable/geist-sans` does NOT exist (404).
- @fontsource-variable/noto-sans-tc (latest 5.2.10) — variable, CJK ZH-TW
All three are variable packages, so use `@fontsource-variable/<name>` (matching the existing inter/noto-sans-jp import pattern). No static `@fontsource/<name>` fallback is needed.
</verified_packages>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit --mkt-* token values to DESIGN.md</name>
  <files>artifacts/web/src/index.css</files>
  <action>
  Edit ONLY the `[data-surface="marketing"]` block inside `@layer marketing-tokens` (currently lines ~404-438). Do NOT touch `:root`, `.dark`, `@theme inline`, or any other block — the fan chat page depends on those and must stay visually identical.

  Replace these values exactly (keep alignment/comment header intact):
  - --mkt-bg: #0D0A14
  - --mkt-fg: #F4F1FA
  - --mkt-surface-1: #16111F
  - --mkt-surface-2: #1F1830
  - --mkt-border: #2A2238
  - --mkt-accent: #7C3AED
  - --mkt-accent-hover: #6D28D9
  - --mkt-accent-fg: #FFFFFF  (value unchanged; casing normalization optional)
  - --mkt-muted-fg: #9D94B5
  - --mkt-radius-sm: 0.5rem   (was 0.375rem)
  - --mkt-radius-md: 0.875rem (was 0.625rem)
  - --mkt-radius-lg: 1.25rem  (was 1rem)
  - --mkt-font-sans: 'Geist', 'Noto Sans JP', 'Noto Sans TC', system-ui, -apple-system, "Hiragino Sans", "Meiryo", sans-serif

  ADD these NEW tokens inside the same block (group sensibly — glows near accent, radius-pill near other radii, font-display near font-sans):
  - --mkt-glow-from: #7C3AED
  - --mkt-glow-to: #D946EF
  - --mkt-radius-pill: 999px
  - --mkt-font-display: 'Bricolage Grotesque', system-ui, sans-serif

  KEEP all --mkt-text-*, --mkt-leading-*, --mkt-spacing-* tokens EXACTLY as they are — do not modify or reorder them.

  Per DESIGN.md token map (SOURCE OF TRUTH). If any value above conflicts with DESIGN.md, DESIGN.md wins — flag the conflict in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -c -E "\-\-mkt-(glow-from|glow-to|radius-pill|font-display)" artifacts/web/src/index.css</automated>
  </verify>
  <done>
  All nine retrofitted values present with the exact strings above; the four new tokens (--mkt-glow-from, --mkt-glow-to, --mkt-radius-pill, --mkt-font-display) added so the grep -c above returns 4. All --mkt-text-*/--mkt-leading-*/--mkt-spacing-* unchanged. Manual scope check: every line matching `--mkt-` in index.css sits between the `@layer marketing-tokens {` line and its closing brace — none appear in :root, .dark, or @theme blocks. :root / .dark / @theme inline byte-identical to before.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install and import three self-hosted font families</name>
  <files>artifacts/web/package.json, artifacts/web/src/main.tsx, pnpm-lock.yaml</files>
  <action>
  pnpm ONLY — never npm or yarn (preinstall hook blocks them). Adding deps, so do NOT use --frozen-lockfile.

  Step 1 — Re-confirm exact package names against the npm registry before installing (DO NOT GUESS). The verified names (as of 2026-06-01) are:
  - @fontsource-variable/bricolage-grotesque
  - @fontsource-variable/geist   (NOT geist-sans — that variable form does not exist)
  - @fontsource-variable/noto-sans-tc
  Quick check: `pnpm view @fontsource-variable/geist version` (and likewise for the other two) — each must return a version, not an error. If a variable package unexpectedly does not resolve, fall back to the static `@fontsource/<name>` form for that one font and note it in the SUMMARY.

  Step 2 — Install into artifacts/web as runtime deps:
  `pnpm --filter @workspace/web add @fontsource-variable/bricolage-grotesque @fontsource-variable/geist @fontsource-variable/noto-sans-tc`

  Step 3 — Add three imports in artifacts/web/src/main.tsx directly beneath the existing two font imports (lines 2-3), matching the existing bare-import pattern:
  - import '@fontsource-variable/bricolage-grotesque';
  - import '@fontsource-variable/geist';
  - import '@fontsource-variable/noto-sans-tc';
  DO NOT remove or alter the existing '@fontsource-variable/inter' or '@fontsource-variable/noto-sans-jp' imports. Keep `import "./instrument";` as the first line.

  Step 4 — Leave artifacts/web/index.html UNTOUCHED. Keep the Noto Sans JP preload as-is. A Bricolage preload is OPTIONAL and ONLY if you can confirm the emitted stable hashed asset path from a build; if you cannot confirm the exact path, do not guess — leave index.html alone.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && pnpm install && pnpm --filter @workspace/web exec tsc --noEmit && grep -c -E "@fontsource-variable/(bricolage-grotesque|geist|noto-sans-tc)" artifacts/web/src/main.tsx</automated>
  </verify>
  <done>
  `pnpm install` succeeds (no lockfile error). `tsc --noEmit` passes with zero errors — confirms all three new @fontsource imports resolve (no module-not-found). The grep above returns 3 (three new imports present). The two original imports (`inter`, `noto-sans-jp`) remain present and unaltered. package.json lists all three new deps; pnpm-lock.yaml refreshed. index.html unchanged.
  </done>
</task>

</tasks>

<verification>
- `pnpm install` succeeds after adding deps.
- `pnpm --filter @workspace/web exec tsc --noEmit` passes (catches any unresolved font import).
- `grep -n "\-\-mkt-" artifacts/web/src/index.css` — every match is inside the `@layer marketing-tokens { [data-surface="marketing"] { ... } }` block; zero --mkt-* in :root, .dark, or @theme inline.
- `grep -n "@fontsource" artifacts/web/src/main.tsx` shows exactly five imports: inter, noto-sans-jp (original) + bricolage-grotesque, geist, noto-sans-tc (new).
- NOTE: a full visual/browser check is Phase 6 territory — no marketing component consumes these tokens yet. This task lands foundation token values + font loading only.
</verification>

<success_criteria>
- All nine --mkt-* values retrofitted to the exact DESIGN.md strings; four new tokens (--mkt-glow-from, --mkt-glow-to, --mkt-radius-pill, --mkt-font-display) added.
- All --mkt-text-*/--mkt-leading-*/--mkt-spacing-* tokens unchanged.
- :root, .dark, @theme inline untouched — fan chat page visually identical.
- No --mkt-* exists outside [data-surface="marketing"].
- Three new variable Fontsource families installed via pnpm and imported in main.tsx; original two imports intact.
- tsc passes; index.html untouched.
</success_criteria>

<output>
Create `.planning/quick/260601-f4l-marketing-token-retrofit-to-design-md/260601-f4l-SUMMARY.md` when done.
</output>
