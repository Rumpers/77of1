---
phase: 06-marketing-components-navigation
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - artifacts/web/src/components/marketing/MarketingNav.tsx
  - artifacts/web/src/components/marketing/MarketingFooter.tsx
  - artifacts/web/src/pages/home.tsx
autonomous: false
requirements: [MKT-07, MKT-08, MKT-11, MKT-14]
must_haves:
  truths:
    - "Sticky MarketingNav shows wordmark + MarketingLocaleSwitcher (desktop) + a small CTA"
    - "MarketingFooter shows company name, contact email, privacy-policy link, AI-disclosure notice, a CTA, and a mobile-accessible locale switcher"
    - "home.tsx is replaced wholesale with the MarketingPage shell composing all 9 sections in order under [data-surface=marketing]"
    - "No marketing layout overflows at 375px viewport width"
  artifacts:
    - path: "artifacts/web/src/components/marketing/MarketingNav.tsx"
      provides: "MKT-14 sticky nav with locale switcher + CTA"
      exports: ["MarketingNav"]
    - path: "artifacts/web/src/components/marketing/MarketingFooter.tsx"
      provides: "MKT-07 footer + MKT-08 footer CTA"
      exports: ["MarketingFooter"]
    - path: "artifacts/web/src/pages/home.tsx"
      provides: "Assembled MarketingPage shell"
      contains: 'data-surface="marketing"'
      min_lines: 25
  key_links:
    - from: "artifacts/web/src/pages/home.tsx"
      to: "all 9 marketing sections via @/components/marketing barrel"
      via: "import from @/components/marketing"
      pattern: "@/components/marketing"
    - from: "artifacts/web/src/components/marketing/MarketingFooter.tsx"
      to: "/:locale/account/data-request"
      via: "privacy link href"
      pattern: "account/data-request"
---

<objective>
Build the chrome (`MarketingNav` MKT-14, `MarketingFooter` MKT-07/MKT-08), replace `home.tsx` wholesale with the assembled `MarketingPage` shell, and run the full responsive QA gate (production build + token-bleed grep + fan-route safety + 375px overflow human-verify).

Purpose: This is the integration wave. It depends on every component from 06-01/02/03. Replacing `home.tsx` last means the page renders only once every section exists, so the build succeeds in one pass. The footer holds the third CTA repeat position (MKT-08) and the mobile-accessible locale switcher (the nav switcher is hidden below sm).
Output: MarketingNav.tsx, MarketingFooter.tsx, home.tsx (replaced), verified building + at 375px.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-marketing-components-navigation/06-RESEARCH.md
@.planning/phases/06-marketing-components-navigation/06-PATTERNS.md
@.planning/phases/06-marketing-components-navigation/06-UI-SPEC.md
@.planning/phases/06-marketing-components-navigation/06-01-SUMMARY.md
@.planning/phases/06-marketing-components-navigation/06-02-SUMMARY.md
@.planning/phases/06-marketing-components-navigation/06-03-SUMMARY.md

<interfaces>
From the marketing barrel @/components/marketing (built across 06-01/02/03 + this plan):
```typescript
export function MarketingLocaleSwitcher(): JSX.Element;
export function CtaButton(props: { label: string; fallbackLabel: string; size?: "sm" | "md" }): JSX.Element;
export function HeroSection(props: { locale: Locale; t: Marketing }): JSX.Element;
export function ValuePropSection(props: { t: Marketing }): JSX.Element;
export function FourPillarsSection(props: { t: Marketing }): JSX.Element;
export function HowItWorksSection(props: { t: Marketing }): JSX.Element;
export function MultiChannelSection(props: { t: Marketing }): JSX.Element;
export function DemoTranscriptSection(props: { t: Marketing }): JSX.Element;
export function CtaSection(props: { t: Marketing }): JSX.Element;
// MarketingNav and MarketingFooter are built in THIS plan; the barrel already lists them (06-01).
```
From artifacts/web/src/lib/i18n.ts:
```typescript
export type Locale = "en" | "ja" | "zh-TW";
export const DEFAULT_LOCALE: Locale = "en";
export function getMessages(locale: string): Messages;
export function isValidLocale(locale: string): locale is Locale;
```
`Marketing` = `ReturnType<typeof getMessages>["marketing"]` (no named export). Keys used here: `t.nav.cta_creator`, `t.hero.cta_no_telegram`, `t.cta.button`, `t.cta.no_telegram`, `t.footer.{tagline, privacy, contact, ai_disclosure}`.

Route confirmed: artifacts/web/src/App.tsx line 46 `<Route path="/:locale">` → HomePage, ABOVE line 73 `<Route path="/:locale/:handle">` → FanPage. No App.tsx change needed (MKT-20 invariant established in Phase 5). home.tsx is lazy-imported as the DEFAULT export.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MarketingNav + MarketingFooter (MKT-14, MKT-07, MKT-08)</name>
  <files>artifacts/web/src/components/marketing/MarketingNav.tsx, artifacts/web/src/components/marketing/MarketingFooter.tsx</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (MarketingNav lines 615-654; MarketingFooter lines 657-710)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Contract 1 MarketingNav lines 173-197; Contract 12 MarketingFooter lines 474-494)
    - artifacts/web/src/components/marketing/CtaButton.tsx + MarketingLocaleSwitcher.tsx (06-01 — the primitives composed here)
    - artifacts/web/src/components/fan/DisclosureFooter.tsx (analog: attribution line pattern — read for structure only, do NOT copy tokens)
    - artifacts/web/src/index.css lines 403-451 (@layer marketing-tokens)
  </read_first>
  <action>
    Create `MarketingNav.tsx` exporting `MarketingNav({ locale, t })` per 06-PATTERNS: `<nav className="sticky top-0 z-50 w-full border-b border-[--mkt-border] bg-[--mkt-bg]/90 backdrop-blur-md">` with inner `max-w-[1120px] mx-auto px-6 h-14 flex items-center justify-between`. Left: wordmark "lala.la" `var(--mkt-font-display)` weight 700 `text-[--mkt-fg]`. Right: a `flex items-center gap-4` row with `<div className="hidden sm:flex"><MarketingLocaleSwitcher /></div>` (hidden on mobile) and `<CtaButton label={t.nav.cta_creator} fallbackLabel={t.hero.cta_no_telegram} size="sm" />`. No hamburger, no anchor links. Create `MarketingFooter.tsx` exporting `MarketingFooter({ locale, t })` per 06-PATTERNS: `<footer className="w-full py-16 border-t border-[--mkt-border] bg-[--mkt-bg]">` with inner `max-w-[1120px] mx-auto px-6 flex flex-col items-center gap-8 text-center`. Top block: `<CtaButton label={t.cta.button} fallbackLabel={t.cta.no_telegram} />` (MKT-08 third repeat) then `<MarketingLocaleSwitcher />` (mobile-accessible duplicate). Legal row `flex flex-wrap gap-4 justify-center items-center` with: tagline `t.footer.tagline` (Small `text-[--mkt-muted-fg]`); privacy `<a href={`/${locale}/account/data-request`}>` `t.footer.privacy` (underlined Small); contact `<a href="mailto:contact@lala.la">` `t.footer.contact` (underlined Small); AI-disclosure `t.footer.ai_disclosure` (Small). The privacy href is a known multi-segment path resolving to the existing DsarPortal route — never a bare `/${locale}/<word>` that would hit the fan handle route. Fonts via inline style only, never `font-sans`.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -q "sticky top-0" artifacts/web/src/components/marketing/MarketingNav.tsx && grep -q "hidden sm:flex" artifacts/web/src/components/marketing/MarketingNav.tsx && grep -q "account/data-request" artifacts/web/src/components/marketing/MarketingFooter.tsx && grep -q "footer.ai_disclosure" artifacts/web/src/components/marketing/MarketingFooter.tsx && grep -q "t.cta.button" artifacts/web/src/components/marketing/MarketingFooter.tsx && ! grep -rE "bg-primary|text-foreground|font-sans\b|bg-card|border-border" artifacts/web/src/components/marketing/MarketingNav.tsx artifacts/web/src/components/marketing/MarketingFooter.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MarketingNav is `sticky top-0 z-50`, shows the wordmark + a `size="sm"` CtaButton, and wraps MarketingLocaleSwitcher in `hidden sm:flex` (hidden on mobile).
    - MarketingFooter renders: tagline (`t.footer.tagline`), privacy link to `/${locale}/account/data-request` (`t.footer.privacy`), contact mailto (`t.footer.contact`), AI-disclosure (`t.footer.ai_disclosure`), a CtaButton using `t.cta.button`, and a MarketingLocaleSwitcher.
    - Footer CTA reuses the shared CtaButton with `t.cta.button` — identical wording with hero + mid-page (MKT-08).
    - Both files contain zero fan-page utilities; fonts via inline style (grep gate passes).
  </acceptance_criteria>
  <done>Sticky nav + complete footer render with isolated locale switching, third CTA repeat, and all required footer items; grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace home.tsx with MarketingPage shell</name>
  <files>artifacts/web/src/pages/home.tsx</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (home.tsx shell lines 32-83 — imports + locale read + ordered composition)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Page Assembly Order lines 664-681; Fan-Route Safety lines 685-695)
    - artifacts/web/src/pages/home.tsx (the 1,137-line prototype being replaced wholesale)
    - artifacts/web/src/pages/onboard-step1.tsx (locale read pattern, lines 24-26)
  </read_first>
  <action>
    Replace `artifacts/web/src/pages/home.tsx` wholesale (delete the entire variant/compare prototype) with the `MarketingPage` shell per 06-PATTERNS. Read locale via `useParams<{ locale: string }>()` guarded by `isValidLocale` defaulting to `DEFAULT_LOCALE`; compute `const t = getMessages(locale).marketing`. Return a single `<div data-surface="marketing" className="min-h-screen bg-[--mkt-bg] overflow-x-hidden">` containing the 9 sections in this exact order: MarketingNav, HeroSection, ValuePropSection, FourPillarsSection, HowItWorksSection, MultiChannelSection, CtaSection, DemoTranscriptSection, MarketingFooter. Import all from the `@/components/marketing` barrel. Pass `locale` + `t` to components that need locale (MarketingNav, HeroSection, MarketingFooter); pass `t` only to the rest. The component MUST remain the DEFAULT export (App.tsx lazy-imports `@/pages/home` default). No logic, state, hooks, variant-switching, or compare-mode beyond the locale read — all rendering lives in the section components. Discard the old LandingActionButton/SectionHeading/HeroPanel/LocaleSwitcher/LandingFooter atoms entirely.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -q 'data-surface="marketing"' artifacts/web/src/pages/home.tsx && grep -q "overflow-x-hidden" artifacts/web/src/pages/home.tsx && grep -q "@/components/marketing" artifacts/web/src/pages/home.tsx && grep -q "export default" artifacts/web/src/pages/home.tsx && ! grep -E "variant|compare|steady-pay|spotlight" artifacts/web/src/pages/home.tsx</automated>
  </verify>
  <acceptance_criteria>
    - home.tsx default-exports `MarketingPage`, reads locale via `useParams` + `isValidLocale` + `DEFAULT_LOCALE`, and wraps everything in `<div data-surface="marketing" ... overflow-x-hidden>`.
    - All 9 sections imported from `@/components/marketing` and rendered in UI-SPEC order (Nav → Hero → ValueProp → FourPillars → HowItWorks → MultiChannel → Cta → Demo → Footer).
    - No remnants of the old prototype (`variant`, `compare`, `steady-pay`, `spotlight`) remain.
    - File contains zero section-rendering logic beyond locale read + composition.
  </acceptance_criteria>
  <done>home.tsx is the lean assembled MarketingPage shell composing all 9 sections under the marketing surface.</done>
</task>

<task type="auto">
  <name>Task 3: QA gate — production build, token-bleed grep, fan-route safety (MKT-11, MKT-20)</name>
  <files>artifacts/web/src/components/marketing/, artifacts/web/src/pages/home.tsx</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (QA Grep Gate lines 928-937)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Responsive Contract lines 542-571; Fan-Route Safety lines 685-695)
    - artifacts/web/src/App.tsx (confirm route order unchanged: /:locale above /:locale/:handle)
  </read_first>
  <action>
    Run the automated QA gates and fix any failures in marketing files only. (1) Token-bleed grep across the whole marketing tree — must be zero matches; any match is a bug, fix the offending marketing component. (2) Production build of artifacts/web — must succeed, proving the barrel resolves all 12 components and TypeScript compiles (first point all referenced modules exist). (3) Confirm App.tsx still has `/:locale` ABOVE `/:locale/:handle` (no change was needed; verify nothing regressed). Do NOT modify App.tsx, do NOT touch any fan-page component. If the build fails on a missing module or a fan-token type error, fix the offending marketing component. The live 375px overflow measurement is the human-verify checkpoint (Task 4), because `document.documentElement.scrollWidth` requires a running browser at a 375px viewport.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && test -z "$(grep -rlE 'bg-primary|bg-background|bg-card|text-foreground|text-primary|border-border|font-sans' artifacts/web/src/components/marketing/ 2>/dev/null)" && pnpm --filter @workspace/web run build && grep -n '"/:locale"' artifacts/web/src/App.tsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rlE 'bg-primary|bg-background|bg-card|text-foreground|text-primary|border-border|font-sans' artifacts/web/src/components/marketing/` returns no files (zero token bleed across the entire marketing tree).
    - `pnpm --filter @workspace/web run build` exits 0 (barrel + all 12 components + home.tsx typecheck and bundle cleanly).
    - App.tsx still routes `/:locale` above `/:locale/:handle`; no fan-page file modified.
  </acceptance_criteria>
  <done>Production build green, zero token bleed across marketing tree, fan-route order intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — 375px overflow + visual + locale switch + CTA deep-link</name>
  <what-built>
    The complete Luminous Infrastructure marketing page: sticky nav, hero with violet bloom + single Telegram CTA, value-prop, four pillars (chat/voice live, image/video coming-soon), how-it-works 3 steps, multi-channel, mid-page CTA, demo transcript, footer with CTA + locale switcher + privacy/contact/AI-disclosure. CTA repeated at hero, mid-page, and footer with identical wording. MarketingLocaleSwitcher routes to /:locale only.
  </what-built>
  <how-to-verify>
    1. Run the fan SPA dev server: `pnpm --filter @workspace/web run dev` (port 22333).
    2. Open `http://localhost:22333/en` — confirm the dark violet marketing page renders (NOT the old multi-variant prototype, NOT fan-page light styling).
    3. Open DevTools responsive mode, set viewport width to 375px. In the console run `document.documentElement.scrollWidth` — it MUST equal 375 (no horizontal scroll). Scroll the full page at 375px and confirm no section overflows (MKT-11, SC5).
    4. Confirm all four content sections render with accurate EN copy; the Image and Video pillars show a "Coming soon" badge; Chat and Voice do not (MKT-03, SC2).
    5. Confirm the primary CTA ("Start with Telegram") appears at hero, mid-page, AND footer with identical wording; click each — each opens the same `https://t.me/...` deep-link in a new tab (or, if `VITE_HERMES_BOT_URL` is unset, each shows the "No Telegram? Contact us" fallback). All three must match (MKT-08/MKT-09, SC3).
    6. Click the MarketingLocaleSwitcher pills (EN / 日本語 / 繁中). URL changes to `/ja`, `/zh-TW`, `/en` (no handle appended); the page stays on the marketing page and does NOT navigate to a fan handle (MKT-14, SC4). Copy stays EN for all locales in Phase 6 (intentional scaffold).
    7. Visit `http://localhost:22333/en/somehandle` and confirm the FAN chat page still loads (fan route intact, MKT-20) and looks visually unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any overflow, token-bleed, wording mismatch, or fan-route regression observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client router → route resolution | `/:locale` must win over `/:locale/:handle`; a malformed marketing link could leak to the fan route |
| i18n namespace → DOM | Localized copy rendered as auto-escaped React text |
| client → external (t.me / mailto) | Footer + nav CTA navigate to the build-time deep-link / public contact email |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Spoofing / Elevation | MarketingFooter privacy link + locale switcher | mitigate | Both navigate only to known fixed paths (`/${locale}/account/data-request`, `/${locale}`) where `${locale}` is one of the 3 allow-listed `LOCALES` strings — never a free-text segment that could resolve to a fan handle (Task 1 acceptance criteria + Task 3 route-order check) |
| T-06-01 | Tampering | nav/footer external CTA links | mitigate | Reuse CtaButton primitive (`rel="noopener noreferrer"` from 06-01 T-06-01); no new external link constructed |
| T-06-04 | Tampering (XSS) | home.tsx + nav + footer copy | mitigate | All copy rendered as auto-escaped React text children; no `dangerouslySetInnerHTML` (acceptance criteria) |
| T-06-SC | Tampering | npm/pnpm installs | accept | No new packages installed; build uses existing deps only (RESEARCH "No New Installs Required") |

No HIGH threats: static client-rendered marketing surface, no auth, no DB writes, no user-input fields. The realistic surface (external links + route collision) is mitigated via allow-listed locales, fixed paths, `rel="noopener noreferrer"`, and the verified route-order invariant. Rationale documented per security_enforcement requirement.
</threat_model>

<verification>
- `pnpm --filter @workspace/web run build` succeeds (all 12 marketing components + home.tsx compile and bundle).
- `grep -rlE 'bg-primary|bg-background|bg-card|text-foreground|text-primary|border-border|font-sans' artifacts/web/src/components/marketing/` returns no files.
- App.tsx `/:locale` route stays above `/:locale/:handle`; fan page unchanged.
- Human-verify checkpoint: `document.documentElement.scrollWidth === 375` at 375px; CTA wording identical at hero/mid/footer; locale switch stays on marketing; fan route still loads.
</verification>

<success_criteria>
- MKT-07 footer renders company name, contact email, privacy link, AI-disclosure.
- MKT-08 CTA repeated at hero, mid-page, footer with identical wording + same URL.
- MKT-11 no overflow at 375px (human-verified).
- MKT-14 MarketingLocaleSwitcher routes to /:locale, isolated from fan switcher.
- home.tsx assembled, production build green, fan route intact.
</success_criteria>

<output>
Create `.planning/phases/06-marketing-components-navigation/06-04-SUMMARY.md` when done.
</output>
