---
phase: 05-foundation-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/web/src/index.css
  - artifacts/web/src/lib/i18n.ts
  - artifacts/web/src/App.tsx
autonomous: true
requirements: [MKT-10, MKT-13, MKT-20]
must_haves:
  truths:
    - "All marketing CSS tokens are scoped under [data-surface=\"marketing\"] inside @layer marketing-tokens; the fan chat page renders visually unchanged"
    - "The marketing i18n namespace exists in all three locales (en/ja/zh-TW) with compile-time key-completeness enforced"
    - "Adding a marketing key to EN but not JA/ZH-TW produces a TypeScript compile error"
    - "All page components load via React.lazy; the fan-page chunk no longer bundles marketing code"
    - "The fan catch-all route /:locale/:handle remains the last named route before the wildcard"
  artifacts:
    - path: "artifacts/web/src/index.css"
      provides: "@layer marketing-tokens with [data-surface=\"marketing\"]-scoped --mkt-* tokens"
      contains: "@layer marketing-tokens"
    - path: "artifacts/web/src/lib/i18n.ts"
      provides: "Typed marketing namespace populated across en/ja/zh-TW with satisfies enforcement"
      contains: "marketing:"
    - path: "artifacts/web/src/App.tsx"
      provides: "React.lazy() page imports + Suspense wrapper; route order preserved"
      contains: "React.lazy"
  key_links:
    - from: "artifacts/web/src/index.css"
      to: "[data-surface=\"marketing\"]"
      via: "every --mkt-* token nested inside the attribute selector inside @layer marketing-tokens"
      pattern: "@layer marketing-tokens"
    - from: "artifacts/web/src/lib/i18n.ts"
      to: "Record<Locale, Messages>"
      via: "satisfies annotation on the messages const enforces marketing key completeness"
      pattern: "satisfies Record<Locale, Messages>"
---

<objective>
Lock the two compile-time/CSS-cascade isolation prerequisites that all later marketing
component work depends on, plus the bundle-isolation prerequisite (code-splitting), in a
single self-contained pass over three files in `artifacts/web`.

Purpose: After this plan, no Phase 6 component can (a) leak a marketing token into the
fan page, (b) introduce an i18n key in one locale but forget another, or (c) force the
fan-page chunk to download marketing code. These three guarantees are structural; they
must exist before any visible section is built.

Output: `@layer marketing-tokens` block in `index.css`, a typed+complete `marketing`
namespace in `i18n.ts`, and `React.lazy()`-wrapped page imports in `App.tsx` with route
order preserved. No visible marketing content is added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation-isolation/05-UI-SPEC.md
@.planning/phases/05-foundation-isolation/05-PATTERNS.md

<interfaces>
<!-- Exact token names and i18n type shape are LOCKED in 05-UI-SPEC.md. -->
<!-- Executor MUST reproduce token names and the marketing type shape verbatim. -->
<!-- index.css existing structure (do NOT touch): @theme inline (lines 7-67), -->
<!--   .dark (189-272), @layer base (274-282), @layer utilities (304-394). -->
<!--   Append @layer marketing-tokens AFTER the @layer utilities closing brace. -->
<!-- i18n.ts: Messages type lines 13-174; const messages declared line 176; -->
<!--   getMessages exported ~line 799. No `satisfies` keyword currently present. -->
<!-- App.tsx: 10 page imports static (lines 3-12); Router() wraps <Switch> without -->
<!--   Suspense; /:locale/:handle is the last named route (line 65). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append @layer marketing-tokens with [data-surface="marketing"]-scoped --mkt-* tokens (MKT-10)</name>
  <files>artifacts/web/src/index.css</files>
  <read_first>
    - artifacts/web/src/index.css (read the FULL file once; confirm the closing brace of @layer utilities near line 394, the @theme inline block lines 7-67, and the .dark block lines 189-272 — none of these may be modified)
    - .planning/phases/05-foundation-isolation/05-UI-SPEC.md § "CSS Isolation Contract" (lines 37-139) and § "Spacing Scale"/"Typography"/"Color" for the exact token names and placeholder values
    - .planning/phases/05-foundation-isolation/05-PATTERNS.md § "artifacts/web/src/index.css" (lines 131-201) for the exact block to append and placement rule
  </read_first>
  <action>
    Append ONE new block at the very end of `artifacts/web/src/index.css`, AFTER the closing
    brace of the existing `@layer utilities` block. The block is `@layer marketing-tokens { [data-surface="marketing"] { ... } }`
    containing exactly these placeholder tokens with the names locked in 05-UI-SPEC.md:
    surface (`--mkt-bg` #ffffff, `--mkt-surface-1` #f5f5f5, `--mkt-surface-2` #ebebeb),
    text (`--mkt-fg` #111111, `--mkt-muted-fg` #666666, `--mkt-accent-fg` #ffffff),
    accent (`--mkt-accent` hsl(263 80% 58%), `--mkt-accent-hover` hsl(263 80% 50%)),
    border (`--mkt-border` #e0e0e0),
    radius (`--mkt-radius-sm` 0.375rem, `--mkt-radius-md` 0.625rem, `--mkt-radius-lg` 1rem),
    font (`--mkt-font-sans`: 'Inter Variable', 'Noto Sans JP Variable', system-ui, -apple-system, "Hiragino Sans", "Meiryo", sans-serif),
    type scale (`--mkt-text-xs` 0.75rem, `--mkt-text-sm` 0.875rem, `--mkt-text-base` 1rem,
    `--mkt-text-lg` 1.125rem, `--mkt-text-xl` 1.25rem, `--mkt-text-2xl` 1.5rem, `--mkt-text-3xl` 1.875rem,
    `--mkt-text-hero` clamp(2.25rem, 5vw, 4rem), `--mkt-text-section` clamp(1.5rem, 3vw, 2.25rem)),
    line-height (`--mkt-leading-tight` 1.2, `--mkt-leading-body` 1.6, `--mkt-leading-cjk` 1.8),
    spacing (`--mkt-spacing-xs` 0.25rem through `--mkt-spacing-3xl` 4rem — the full 7-step scale per UI-SPEC).
    These are placeholder values per D-05-05 — do NOT invent a finished palette. Use the
    PATTERNS.md block verbatim (lines 152-195) including its comment header. Do NOT add any
    `--mkt-*` token to `:root`, `body`, `*`, `html`, or the `@theme inline` block — that would
    contaminate the dark-mode fan page (Pitfall 2). Do NOT touch the `.dark` block.
  </action>
  <verify>
    <automated>cd artifacts/web && grep -q '@layer marketing-tokens' src/index.css && grep -q '\[data-surface="marketing"\]' src/index.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'marketing-tokens' artifacts/web/src/index.css` returns a line that appears AFTER the line number of the `@layer utilities` closing brace
    - `grep -c -- '--mkt-' artifacts/web/src/index.css` returns at least 30 (full token set present)
    - No `--mkt-` token appears on a `:root`, `body`, `*`, or `html` selector: `grep -nE '^(:root|body|html|\*)' artifacts/web/src/index.css | grep -i mkt` returns nothing
    - `@theme inline` and `.dark` blocks are byte-for-byte unchanged (git diff shows additions only at end of file)
    - `pnpm --filter @workspace/web run build` exits 0 (CSS parses)
  </acceptance_criteria>
  <done>The marketing token block exists at end of index.css, scoped to [data-surface="marketing"] inside @layer marketing-tokens; no fan-page token block is modified; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed `marketing` i18n namespace populated across all three locales with `satisfies` enforcement (MKT-13)</name>
  <files>artifacts/web/src/lib/i18n.ts</files>
  <read_first>
    - artifacts/web/src/lib/i18n.ts (read the FULL file; confirm Messages type lines 13-174, `const messages: Record<Locale, Messages> = {` at line 176, the three locale keys en/ja/'zh-TW', and the getMessages export near line 799)
    - .planning/phases/05-foundation-isolation/05-UI-SPEC.md § "i18n Namespace Contract (MKT-13)" (lines 319-399) for the exact marketing type shape (locked)
    - .planning/phases/05-foundation-isolation/05-PATTERNS.md § "artifacts/web/src/lib/i18n.ts" (lines 285-447) for the three-step modification order and the verbatim English scaffold object
  </read_first>
  <action>
    Three steps, IN THIS ORDER (reversing causes a cascading typecheck failure — Pitfall 3):
    (1) Append the `marketing: { ... }` key to the `Messages` type after the `onboard` block,
    using the exact nested shape from 05-UI-SPEC.md lines 329-393 (meta{title,description},
    nav{cta_creator}, hero{headline,subheadline,cta_primary,cta_no_telegram},
    value_prop{title,subtitle}, pillars{title + chat/voice/image/video label+desc + image/video coming_soon},
    channels{title,subtitle,lala_label,telegram_label,social_label},
    onboarding{title,subtitle,step1-3 label+desc}, cta{headline,subheadline,button,no_telegram},
    footer{tagline,privacy,contact,ai_disclosure}, demo{title,label}).
    (2) Add the identical English scaffold `marketing` object (verbatim from PATTERNS.md lines
    376-431) to EACH of the three locale entries `en`, `ja`, `'zh-TW'`. Use English scaffolding
    for all three locales — final JA/ZH-TW copy is a Phase 7 concern (D-05-05, UI-SPEC § Copywriting).
    (3) Change the declaration at line 176 from `const messages: Record<Locale, Messages> = {`
    to `const messages = {` and add `} satisfies Record<Locale, Messages>;` at the close of the
    object literal. Do this ONLY after all three locales are populated. Do NOT install i18next or
    add any runtime i18n library — extend the existing hand-rolled system only.
    File grows from 806 to ~930 lines; accept as a one-time exception (do not split to a separate file).
  </action>
  <verify>
    <automated>pnpm --filter @workspace/web run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @workspace/web run typecheck` exits 0
    - `grep -c 'marketing:' artifacts/web/src/lib/i18n.ts` returns at least 4 (one in Messages type + one per locale)
    - `grep -q 'satisfies Record<Locale, Messages>' artifacts/web/src/lib/i18n.ts` succeeds
    - Drift guard: temporarily delete the `demo` block from the `en.marketing` object → `pnpm --filter @workspace/web run typecheck` exits NON-zero (proves compile-time completeness); then restore it and confirm typecheck exits 0
    - `i18next` does not appear in artifacts/web/package.json dependencies
  </acceptance_criteria>
  <done>The marketing namespace is declared in the Messages type, populated in all three locales, enforced by satisfies, and typecheck passes; a missing key in any locale fails the build.</done>
</task>

<task type="auto">
  <name>Task 3: Convert all page imports to React.lazy() + Suspense; preserve route order (MKT-20, Pitfall 11)</name>
  <files>artifacts/web/src/App.tsx</files>
  <read_first>
    - artifacts/web/src/App.tsx (read the FULL file; confirm static page imports lines 3-12, Router() wrapping <Switch> without Suspense, and that <Route path="/:locale/:handle" component={FanPage}/> is the last named route at line 65 before <Route component={NotFound}/>)
    - .planning/phases/05-foundation-isolation/05-PATTERNS.md § "artifacts/web/src/App.tsx" (lines 204-282) for the exact lazy import block and Suspense wrapping
    - .planning/phases/05-foundation-isolation/05-UI-SPEC.md § "Route Safety Contract (MKT-20)" (lines 403-421) and § "Code-Split Contract" (lines 424-440)
  </read_first>
  <action>
    Replace the 10 static page-component imports (NotFound, HomePage, FanPage, PaymentSuccessPage,
    PaymentCancelPage, OnboardStep1, OnboardStep2, OnboardStep3, DsarPortal, CreatorDashboard)
    with `const X = React.lazy(() => import("@/pages/..."))` declarations, and add
    `import React, { Suspense } from "react";` at the top. Keep the non-page imports
    (wouter, @tanstack/react-query, `{ DEFAULT_LOCALE, isValidLocale }` from @/lib/i18n,
    CookieConsentBanner) as static. Wrap the `<Switch>` inside `Router()` with `<Suspense fallback={null}>`
    — `fallback={null}` is correct for this plumbing phase (no marketing content yet).
    Do NOT change route order, paths, or the redirect logic. The invariant: `/:locale/:handle`
    (two-segment fan catch-all) MUST remain the last named `<Route>` before the `<Route component={NotFound}/>`
    wildcard. Phase 5 adds NO new routes. Per MKT-20, the fixed Replit ports and the fan route
    must be untouched — verify no path string is altered.
  </action>
  <verify>
    <automated>cd artifacts/web && grep -q 'React.lazy' src/App.tsx && grep -q 'Suspense' src/App.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'React.lazy' artifacts/web/src/App.tsx` returns 10 (all page components lazified)
    - `grep -q '<Suspense' artifacts/web/src/App.tsx` succeeds
    - The last `path="/:locale/:handle"` route still appears before the wildcard `component={NotFound}` route (verify by line ordering in `grep -n 'path=\|component={NotFound}' artifacts/web/src/App.tsx`)
    - No route path string changed: `git diff artifacts/web/src/App.tsx` shows no edits to any `path="..."` literal
    - `pnpm --filter @workspace/web run typecheck` exits 0 and `pnpm --filter @workspace/web run build` exits 0
  </acceptance_criteria>
  <done>All page imports are React.lazy with a Suspense boundary; route order and paths are unchanged; typecheck and build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → static CSS/JS bundle | Token values and i18n strings are authored in source and compiled; no runtime user input crosses into them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | index.css marketing tokens leaking to :root/body | mitigate | Every --mkt-* token nested inside [data-surface="marketing"] inside @layer marketing-tokens; acceptance criterion greps for stray :root/body --mkt-* declarations |
| T-05-02 | Information Disclosure | i18n scaffold strings | accept | Scaffold strings are non-sensitive English placeholders; no PII, no secrets; replaced with final copy in Phase 7 |
| T-05-03 | Denial of Service | React.lazy chunk-split altering fan route behavior | accept | No new routes, no path changes; Suspense fallback={null}; fan catch-all order preserved and asserted; web test suite has no page-level tests (RESEARCH A2) |

No new endpoints, no authentication, no crypto, no data handling in this plan. ASVS V2-V6 do not apply.
**No HIGH-severity threats. Gate passes.**
</threat_model>

<verification>
- `pnpm --filter @workspace/web run typecheck` exits 0
- `pnpm --filter @workspace/web run build` exits 0
- index.css: marketing tokens scoped only under [data-surface="marketing"]; fan-page blocks unchanged
- i18n.ts: marketing namespace complete across en/ja/zh-TW; satisfies enforces completeness
- App.tsx: all pages lazy; Suspense present; route order intact
</verification>

<success_criteria>
- ROADMAP Phase 5 success criterion 1 (CSS isolation under @layer marketing-tokens) satisfied
- ROADMAP Phase 5 success criterion 2 (typed marketing namespace, missing-key compile error) satisfied
- MKT-20 route-ordering and Pitfall 11 bundle-isolation prerequisites in place
</success_criteria>

<output>
Create `.planning/phases/05-foundation-isolation/05-01-SUMMARY.md` when done
</output>
