---
phase: 06-marketing-components-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/web/src/components/marketing/CtaButton.tsx
  - artifacts/web/src/components/marketing/MarketingLocaleSwitcher.tsx
  - artifacts/web/src/components/marketing/HeroOrb.tsx
  - artifacts/web/src/components/marketing/index.ts
  - artifacts/web/src/index.css
  - .env.example
autonomous: true
requirements: [MKT-08, MKT-09, MKT-14, MKT-11]
must_haves:
  truths:
    - "A single CtaButton primitive opens the Telegram deep-link and always shows a no-Telegram fallback link"
    - "MarketingLocaleSwitcher navigates to /:locale (no handle) and does not import or modify the fan LocaleSwitcher"
    - "The marketing barrel exports all 12 component names so downstream plans never edit index.ts"
    - "prefers-reduced-motion disables marketing transitions/animations"
  artifacts:
    - path: "artifacts/web/src/components/marketing/CtaButton.tsx"
      provides: "Reusable Telegram CTA primitive with always-visible mailto fallback"
      exports: ["CtaButton"]
      min_lines: 30
    - path: "artifacts/web/src/components/marketing/MarketingLocaleSwitcher.tsx"
      provides: "Pill locale switcher navigating to /:locale"
      exports: ["MarketingLocaleSwitcher"]
    - path: "artifacts/web/src/components/marketing/HeroOrb.tsx"
      provides: "Static violet→fuchsia decorative bloom"
      exports: ["HeroOrb"]
    - path: "artifacts/web/src/components/marketing/index.ts"
      provides: "Named re-export barrel for all marketing components"
      contains: "export { CtaButton }"
  key_links:
    - from: "artifacts/web/src/components/marketing/CtaButton.tsx"
      to: "import.meta.env.VITE_HERMES_BOT_URL"
      via: "build-time env read at module load"
      pattern: "VITE_HERMES_BOT_URL"
    - from: "artifacts/web/src/components/marketing/MarketingLocaleSwitcher.tsx"
      to: "setLocation(`/${locale}`)"
      via: "wouter useLocation"
      pattern: "setLocation\\(`/\\$\\{locale\\}`\\)"
---

<objective>
Build the three leaf primitives every marketing section composes — the reusable Telegram `CtaButton`, the `MarketingLocaleSwitcher`, and the decorative `HeroOrb` — plus the complete named-export barrel and the global supporting changes (`.env.example` vars, reduced-motion CSS guard).

Purpose: These are the lowest-level building blocks. Building them first (and writing the barrel with ALL 12 component names upfront) lets the two Wave-2 section plans run fully in parallel without ever touching `index.css` or `index.ts`.
Output: CtaButton.tsx, MarketingLocaleSwitcher.tsx, HeroOrb.tsx, index.ts barrel, `.env.example` VITE vars, reduced-motion `@media` rule in index.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-marketing-components-navigation/06-RESEARCH.md
@.planning/phases/06-marketing-components-navigation/06-PATTERNS.md
@.planning/phases/06-marketing-components-navigation/06-UI-SPEC.md

<interfaces>
<!-- Confirmed from lib/i18n.ts — use these directly, no exploration needed. -->
From artifacts/web/src/lib/i18n.ts:
```typescript
export type Locale = "en" | "ja" | "zh-TW";
export const LOCALES: Locale[] = ["en", "ja", "zh-TW"];
export const DEFAULT_LOCALE: Locale = "en";
export function getMessages(locale: string): Messages;
export function isValidLocale(locale: string): locale is Locale;
```
The marketing copy shape (no named type export — derive with `ReturnType<typeof getMessages>["marketing"]` or accept the needed slice as props):
`getMessages(locale).marketing` → { nav, hero, value_prop, pillars, channels, onboarding, cta, footer, demo }

From artifacts/web/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>

<env_note>
The Hermes `/start` handler (artifacts/hermes/src/index.ts line 65) does NOT parse a `start` payload — it routes any unlinked creator to `${WEB_BASE_URL}/creator/connect`. The `?start=creator_onboard` payload is therefore inert today but harmless: the deep-link still lands a creator inside Hermes onboarding. Keep `creator_onboard` as the documented payload value (alphanumeric, satisfies MKT-09) and record this in the SUMMARY as a known no-op until Hermes parses it.
</env_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CtaButton primitive (MKT-08, MKT-09)</name>
  <files>artifacts/web/src/components/marketing/CtaButton.tsx</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (CtaButton section, lines 144-207 — the exact analog code)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Component Contract 3 "CtaButton", lines 218-253)
    - artifacts/web/src/components/fan/MonetizationCTA.tsx (analog: <a target=_blank rel=noopener pattern — read for structure only, do NOT copy its tokens)
    - artifacts/web/src/index.css lines 403-451 (@layer marketing-tokens — the real --mkt-* token names)
  </read_first>
  <action>
    Create `CtaButton.tsx` exporting a named `CtaButton` component per the 06-PATTERNS CtaButton analog. Read `VITE_HERMES_BOT_URL` and `VITE_CONTACT_EMAIL` once at module load via `import.meta.env` (fallback empty string and `contact@lala.la` respectively). Props: `label: string`, `fallbackLabel: string`, `size?: "sm" | "md"` (default "md"; "sm" → `px-4 py-2`, "md" → `px-6 py-3`). Render a primary `<a>` to the bot URL with `target="_blank" rel="noopener noreferrer"`, the lucide-react `Send` icon (16px, gap-2, aria-hidden), `bg-[--mkt-accent] text-[--mkt-accent-fg]` weight 600, `rounded-[--mkt-radius-pill]`, `min-h-[44px]`, hover `bg-[--mkt-accent-hover]`, active `scale-[0.98]`, focus-visible ring per UI-SPEC, and the violet glow box-shadow `0 0 24px color-mix(in oklch, var(--mkt-glow-from) 40%, transparent)`. Always render the fallback `<a href="mailto:${CONTACT_EMAIL}">` below the button as a Small (0.875rem) underlined `text-[--mkt-muted-fg]` link. Apply `style={{ fontFamily: "var(--mkt-font-sans)" }}` to both links (never the `font-sans` utility class). When the bot URL is empty string, set the primary anchor `href={HERMES_BOT_URL || undefined}` so it does not navigate; the fallback link is always present — this satisfies MKT-09's graceful-fallback clause.
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -q "VITE_HERMES_BOT_URL" artifacts/web/src/components/marketing/CtaButton.tsx && grep -q 'rel="noopener noreferrer"' artifacts/web/src/components/marketing/CtaButton.tsx && grep -q "mailto:" artifacts/web/src/components/marketing/CtaButton.tsx && ! grep -E "bg-primary|text-foreground|font-sans\b|bg-card|border-border" artifacts/web/src/components/marketing/CtaButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CtaButton.tsx exports `CtaButton` and reads `import.meta.env.VITE_HERMES_BOT_URL` at module scope (not inside a click handler).
    - Primary link has `target="_blank"` AND `rel="noopener noreferrer"`.
    - A mailto fallback `<a>` is always rendered (not conditionally hidden when Telegram is present).
    - File contains zero fan-page utilities: no `bg-primary`, `text-foreground`, `bg-card`, `border-border`, or bare `font-sans`; fonts applied via `style={{ fontFamily: "var(--mkt-font-sans)" }}`.
    - All color/radius classes use `--mkt-*` arbitrary-value syntax.
  </acceptance_criteria>
  <done>CtaButton renders the deep-link button + always-visible mailto fallback using only --mkt-* tokens; grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: MarketingLocaleSwitcher + HeroOrb (MKT-14, MKT-11)</name>
  <files>artifacts/web/src/components/marketing/MarketingLocaleSwitcher.tsx, artifacts/web/src/components/marketing/HeroOrb.tsx</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (MarketingLocaleSwitcher section lines 87-141; HeroOrb section lines 294-320)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Component Contract 2 "MarketingLocaleSwitcher" lines 200-215; Contract 5 "HeroOrb" lines 297-308)
    - artifacts/web/src/components/fan/LocaleSwitcher.tsx (analog ONLY — uses Radix DropdownMenu + fan tokens; do NOT import from it, do NOT modify it)
    - artifacts/web/src/index.css lines 403-451 (@layer marketing-tokens)
  </read_first>
  <action>
    Create `MarketingLocaleSwitcher.tsx` exporting `MarketingLocaleSwitcher` per the 06-PATTERNS analog. Read current locale from `useParams<{ locale: string }>()` guarded by `isValidLocale` defaulting to `DEFAULT_LOCALE`. Render a `flex gap-1` row of 3 pill `<button>`s mapping over `LOCALES` with `LOCALE_LABELS = { en: "EN", ja: "日本語", "zh-TW": "繁中" }`. Each button `onClick={() => setLocation(`/${locale}`)}` (single-segment path — NEVER `/${locale}/${handle}`), `aria-current="page"` when active, `rounded-[--mkt-radius-pill] px-3 py-1` Small size, active `bg-[--mkt-accent] text-[--mkt-accent-fg]`, inactive `text-[--mkt-muted-fg] hover:text-[--mkt-fg]`, focus-visible ring per UI-SPEC, `style={{ fontFamily: "var(--mkt-font-sans)" }}`. Do NOT import `Globe`, `Check`, `DropdownMenu`, or `Button` from the fan component. Then create `HeroOrb.tsx` exporting `HeroOrb` — a pure static decorative element per 06-PATTERNS HeroOrb code: `aria-hidden` outer `pointer-events-none absolute inset-0 overflow-hidden`, inner 600px circle radial-gradient `color-mix(in oklch, var(--mkt-glow-from) 30%, transparent)` with `blur(60px)`, centered via translate. No framer-motion, no `initial opacity:0` (animation is Phase 7).
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -q 'setLocation(`/${locale}`)' artifacts/web/src/components/marketing/MarketingLocaleSwitcher.tsx && ! grep -E "DropdownMenu|/\$\{.*handle" artifacts/web/src/components/marketing/MarketingLocaleSwitcher.tsx && grep -q "overflow-hidden" artifacts/web/src/components/marketing/HeroOrb.tsx && ! grep -rE "bg-primary|text-foreground|font-sans\b|bg-card|border-border" artifacts/web/src/components/marketing/MarketingLocaleSwitcher.tsx artifacts/web/src/components/marketing/HeroOrb.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MarketingLocaleSwitcher navigates with `setLocation(`/${locale}`)` — no handle segment anywhere in the file.
    - File does NOT import from `@/components/fan/LocaleSwitcher` and does NOT import Radix `DropdownMenu`.
    - Active pill uses `bg-[--mkt-accent]`; `aria-current="page"` set on the active locale.
    - HeroOrb is `aria-hidden`, has `overflow-hidden` on its container, and contains no framer-motion import and no `initial` opacity prop.
    - Both files contain zero fan-page utilities (grep gate passes).
  </acceptance_criteria>
  <done>Locale switcher routes to /:locale only without touching the fan switcher; HeroOrb is a static contained bloom; grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: Barrel, env vars, reduced-motion guard (MKT-11)</name>
  <files>artifacts/web/src/components/marketing/index.ts, .env.example, artifacts/web/src/index.css</files>
  <read_first>
    - .planning/phases/06-marketing-components-navigation/06-PATTERNS.md (index.ts barrel section lines 714-732 — the full export list; Reduced-Motion Guard lines 905-916)
    - .planning/phases/06-marketing-components-navigation/06-UI-SPEC.md (Environment Variables lines 649-660; Interaction States reduced-motion lines 609-619)
    - .env.example (full — to append VITE vars in a new web section)
    - artifacts/web/src/index.css lines 403-451 (@layer marketing-tokens — to add the reduced-motion @media after it)
  </read_first>
  <action>
    Create `index.ts` barrel with named re-exports for ALL 12 marketing components exactly as listed in 06-PATTERNS barrel section: MarketingNav, MarketingLocaleSwitcher, CtaButton, HeroSection, HeroOrb, ValuePropSection, FourPillarsSection, HowItWorksSection, MultiChannelSection, DemoTranscriptSection, CtaSection, MarketingFooter. Writing all names now (even for components built in later plans) means no later plan edits this file. Note: TypeScript will not error on the not-yet-created modules at runtime, but `pnpm run typecheck` of artifacts/web WILL fail until all referenced files exist — this is expected; the barrel is verified by `pnpm run build` only at the end of plan 06-04. Append to `.env.example` a new "Marketing web (Vite)" section with `VITE_HERMES_BOT_URL=` (commented: complete t.me URL, e.g. `https://t.me/DonnaFollowFun_bot?start=creator_onboard`; empty → fallback link renders) and `VITE_CONTACT_EMAIL=contact@lala.la`. Add a `@media (prefers-reduced-motion: reduce)` rule scoping `[data-surface="marketing"] *` to `animation-duration: 0.01ms !important; transition-duration: 0.01ms !important;` placed immediately after the `@layer marketing-tokens` block in index.css (per 06-PATTERNS Reduced-Motion Guard).
  </action>
  <verify>
    <automated>cd /home/joe/Workspace/77of1 && grep -c "^export {" artifacts/web/src/components/marketing/index.ts | grep -qE "^(12)$" && grep -q "VITE_HERMES_BOT_URL" .env.example && grep -q "VITE_CONTACT_EMAIL" .env.example && grep -q "prefers-reduced-motion" artifacts/web/src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contains exactly 12 `export { X } from "./X"` lines (one per marketing component named in 06-PATTERNS).
    - `.env.example` contains `VITE_HERMES_BOT_URL=` and `VITE_CONTACT_EMAIL=contact@lala.la` in a marketing/web section.
    - index.css contains a `@media (prefers-reduced-motion: reduce)` block scoped under `[data-surface="marketing"]`.
    - No fan-page index.css token block (`@theme inline`, `:root` fan tokens) is modified — only an additive @media rule appended after @layer marketing-tokens.
  </acceptance_criteria>
  <done>Barrel exports all 12 names; VITE vars documented; reduced-motion guard added under the marketing scope only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time env → client bundle | `VITE_HERMES_BOT_URL` / `VITE_CONTACT_EMAIL` are inlined by Vite at build; not user input |
| client → external (t.me / mailto) | Outbound navigation to Telegram and the public contact email |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | CtaButton `target="_blank"` link | mitigate | `rel="noopener noreferrer"` on every external `<a>` (tab-napping prevention) — enforced in Task 1 acceptance criteria |
| T-06-02 | Information Disclosure | mailto fallback | accept | Exposes only the public contact email (`contact@lala.la`), already public; no PII |
| T-06-03 | Tampering | deep-link URL build | accept | `VITE_HERMES_BOT_URL` is build-time env, not user input; no runtime interpolation of untrusted data into the `t.me` URL |
| T-06-SC | Tampering | npm/pnpm installs | mitigate | No new packages installed this phase (RESEARCH "No New Installs Required"); no install task exists |

No HIGH threats: this is a static client-rendered surface with no auth, no DB writes, no user-input fields, and no `dangerouslySetInnerHTML` (all copy comes from the typed i18n namespace). Rationale documented per security_enforcement requirement.
</threat_model>

<verification>
- `grep -rn "bg-primary\|bg-background\|bg-card\|text-foreground\|text-primary\|border-border\|font-sans" artifacts/web/src/components/marketing/` returns zero matches.
- CtaButton, MarketingLocaleSwitcher, HeroOrb each export their named component.
- index.ts barrel lists all 12 components.
- `.env.example` documents both VITE vars.
</verification>

<success_criteria>
- Leaf primitives (CtaButton, MarketingLocaleSwitcher, HeroOrb) built using only --mkt-* tokens.
- Barrel pre-populated with all 12 names so Wave-2 plans never edit index.ts.
- Telegram deep-link wired with always-visible mailto fallback (MKT-09 fallback clause).
- Locale switcher routes to /:locale only, isolated from the fan switcher (MKT-14).
- Reduced-motion guard scoped to the marketing surface.
</success_criteria>

<output>
Create `.planning/phases/06-marketing-components-navigation/06-01-SUMMARY.md` when done. Record the Hermes `/start` payload no-op caveat (creator_onboard is inert until Hermes parses it).
</output>
