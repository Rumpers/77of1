---
phase: 01-baseline-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/src/schema/index.ts
  - lib/db/drizzle.config.ts
  - lib/db/package.json
  - artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts
autonomous: false
requirements: [INFRA-03, PERSONA-03, COMPLY-03, INFRA-04]
must_haves:
  truths:
    - "lib/db/src/schema/index.ts exports all 8 Drizzle table objects and their insert schemas/types"
    - "drizzle-kit push runs clean against Replit PG and creates all 8 tables with the new 3-value kyc_status pgEnum"
    - "A failing end-to-end test exists that exercises POST /api/twin/chat and asserts HTTP 423 when kyc.status != 'signed' — turns GREEN in plan 02"
    - "twins.visibility column exists with twin_visibility('public','private') pgEnum default 'private'"
    - "safety_audit_log persists only sha256 hashes for fan_id and message — no raw PII column exists"
  artifacts:
    - path: "lib/db/src/schema/index.ts"
      provides: "Drizzle schema: 8 tables + creator_totp + enums + insert schemas + select types"
      contains: "creatorsTable, twinsTable, creatorKycTable, creatorConfigTable, consentGrantsTable, conversationMessagesTable, generationJobsTable, safetyAuditLogTable, creatorTotpTable, kycStatusEnum, twinVisibilityEnum, retentionCategoryEnum, consentGrantModalityEnum, generationJobStatusEnum, messageRoleEnum, crisisLevelEnum"
    - path: "artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts"
      provides: "Failing end-to-end test: POST /api/twin/chat with handle pointing at a creator whose kyc row has status='pending' returns 423; with status='signed' it does NOT return 423"
  key_links:
    - from: "lib/db/src/schema/index.ts"
      to: "Replit PostgreSQL"
      via: "drizzle-kit push --config ./drizzle.config.ts"
      pattern: "pgTable\\(.creator_kyc."
    - from: "lib/db/drizzle.config.ts"
      to: "process.env.DATABASE_URL"
      via: "dbCredentials.url"
      pattern: "DATABASE_URL"
---

<objective>
Establish the Walking Skeleton's data backbone: write the full Drizzle schema for all 8 core tables (plus creator_totp) in `lib/db/src/schema/index.ts`, push it to Replit PostgreSQL via `drizzle-kit push`, and seed a failing end-to-end test that proves the KYC gate at `POST /api/twin/chat` is unimplemented. Plans 02-04 implement the gate and the migrations off Supabase; this plan is the foundation they push code against.

Purpose: Without the schema landed in Replit PG, no downstream plan can run a single Drizzle query — every later vertical slice depends on these table types being exported from `@workspace/db`. The failing E2E test pins the user-visible behavior the KYC slice (Plan 02) must satisfy.

Output: Drizzle schema file with 9 pgTable definitions and 7 pgEnum definitions, a `drizzle.config.ts` confirmed compatible with non-pooled Replit DATABASE_URL, a live Replit PG database with all tables created, and a failing E2E test in api-server's vitest suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-repair/01-CONTEXT.md
@.planning/phases/01-baseline-repair/01-RESEARCH.md
@.planning/phases/01-baseline-repair/01-PATTERNS.md
@lib/db/src/index.ts
@lib/db/src/schema/index.ts
@lib/db/drizzle.config.ts
@lib/db/package.json
@supabase/migrations/20260524000001_schema_v1.sql
@supabase/migrations/20260525000001_creator_kyc.sql
@supabase/migrations/20260525000001_safety_audit_log.sql
@lib/queue/src/types.ts

<interfaces>
<!-- Singleton pool already exists; do NOT create another Pool. -->
From lib/db/src/index.ts (existing — do not modify in this plan):
- export const pool = new Pool({ connectionString: process.env.DATABASE_URL })
- export const db = drizzle(pool, { schema })
- export * from "./schema"

Drizzle imports required in schema/index.ts:
- import { pgTable, pgEnum, uuid, text, boolean, jsonb, integer, timestamp, real, index, unique } from "drizzle-orm/pg-core"
- import { createInsertSchema } from "drizzle-zod"
- import { z } from "zod/v4"

Type/schema export pattern per table:
- export const insertXxxSchema = createInsertSchema(xxxTable).omit({ id: true, createdAt: true, updatedAt: true })
- export type Xxx = typeof xxxTable.$inferSelect
- export type InsertXxx = z.infer<typeof insertXxxSchema>

Column patterns to use verbatim:
- updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow().$onUpdateFn(() => new Date())
- enum forward-reference order: define pgEnum BEFORE the pgTable that uses it
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Write failing E2E test for the KYC gate (RED step of MVP slice)</name>
  <files>artifacts/api-server/src/__tests__/kyc-gate.e2e.test.ts</files>
  <read_first>
    - artifacts/api-server/src/routes/twin.ts (confirm current handler has NO KYC gate; the test must fail today)
    - artifacts/api-server/src/index.ts or app.ts (confirm Express app factory export pattern for supertest)
    - artifacts/api-server/package.json (confirm vitest + supertest availability)
    - .planning/phases/01-baseline-repair/01-CONTEXT.md decisions D-05, D-06
  </read_first>
  <behavior>
    - Test 1: POST /api/twin/chat with body `{ handle: "test-pending", message: "hi" }` returns HTTP 423 with JSON body `{ error: ..., code: "KYC_UNSIGNED" }` when the seeded creator row has `creator_kyc.status = 'pending'`. (FAILS today — handler has no gate, returns 200.)
    - Test 2: POST /api/twin/chat with body `{ handle: "test-signed", message: "hi" }` does NOT return 423 when the seeded creator row has `creator_kyc.status = 'signed'`. (Will PASS in Plan 02 once gate exists; today it returns 200 either way and is considered passing for now via skip-or-assert-not-423.)
    - Test 3: POST /api/twin/chat with `{ handle: "nonexistent", message: "hi" }` returns HTTP 404 with `{ error: "Creator not found" }`. (FAILS today.)
  </behavior>
  <action>Create vitest test file `kyc-gate.e2e.test.ts` using supertest against the Express app factory. Use Drizzle (imported via `@workspace/db` once Task 1 lands; for this task you may use `vi.mock` or seed via raw `pool.query` if schema not yet pushed) to insert two `creators` rows with handles `test-pending` and `test-signed` and matching `creator_kyc` rows with `status='pending'` and `status='signed'` in a `beforeAll` block; clean up in `afterAll`. Tests 1 and 3 MUST fail on `vitest run` today against the current handler (which always returns the canned 200 stub). Add `it.todo` only if blocked on app factory — never silently skip. Tag the file with a `describe("Phase 1 KYC gate E2E", ...)` block. Do NOT modify `twin.ts` in this task — the test is the contract Plan 02 must satisfy.</action>
  <verify>
    <automated>pnpm --filter @workspace/api-server exec vitest run src/__tests__/kyc-gate.e2e.test.ts 2>&1 | tee /tmp/p1t0.txt; grep -E "(FAIL|fail|failed)" /tmp/p1t0.txt</automated>
  </verify>
  <done>Test file exists, executes under vitest, Test 1 and Test 3 FAIL with current handler (proving the gate is missing); the failure output names the missing 423/404 status as the assertion gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Write complete Drizzle schema for all 8 core tables + creator_totp</name>
  <files>lib/db/src/schema/index.ts</files>
  <read_first>
    - lib/db/src/schema/index.ts (current placeholder)
    - lib/db/src/index.ts (confirm `export * from "./schema"` is in place)
    - supabase/migrations/20260524000001_schema_v1.sql (source-of-truth column types for creators, consent_grants, generation_jobs)
    - supabase/migrations/20260525000001_creator_kyc.sql (creator_kyc legacy columns; collapse 8-state enum per D-05)
    - supabase/migrations/20260525000001_safety_audit_log.sql (safety_audit_log canonical columns)
    - .planning/phases/01-baseline-repair/01-RESEARCH.md sections "Drizzle Schema Design — 8 Tables" and "Supplemental Tables"
    - .planning/phases/01-baseline-repair/01-PATTERNS.md sections "lib/db/src/schema/index.ts" and "Shared Patterns"
    - artifacts/hermes/src/db.ts (lines 84–132 — infer creator_totp columns from usage per PATTERNS.md "Supplemental: creator_totp Table")
    - lib/queue/src/types.ts (confirm `consentGrantId`, `creatorId`, status field names match Drizzle column names for generation_jobs)
  </read_first>
  <behavior>
    - All 9 pgTable exports compile cleanly under `pnpm --filter @workspace/db exec tsc --noEmit` with no implicit any errors.
    - Every status/role/category column uses `pgEnum` (not free `text`): `kycStatusEnum` = ['pending','signed','rejected']; `twinVisibilityEnum` = ['public','private']; `messageRoleEnum` = ['user','assistant']; `retentionCategoryEnum` = ['operational','transcript','audit']; `consentGrantModalityEnum` = ['persona_text','voice','image','talking_video','fullbody_video']; `generationJobStatusEnum` = ['queued','processing','complete','failed','cancelled','dlq']; `crisisLevelEnum` = ['none','low','medium','high'].
    - Every table touching fan interactions (`consent_grants`, `conversation_messages`, `generation_jobs`, `safety_audit_log`) has `retentionCategory` column NOT NULL with default — D-14.
    - `safety_audit_log` has `fanIdHash text("fan_id_hash").notNull()` and `messageHash text("message_hash").notNull()` and NO raw `fan_id` or `message_text` column — COMPLY-03 / D-02.
    - `creator_kyc.status` is `kycStatusEnum("status").notNull().default("pending")` — never nullable (Pitfall #4).
    - `twins.visibility` is `twinVisibilityEnum("visibility").notNull().default("private")` — PERSONA-03.
    - `generation_jobs.consent_grant_id` is `uuid("consent_grant_id").notNull().references(() => consentGrantsTable.id)` — D-04.
    - `creators` exports both `replitUserId text("replit_user_id").unique()` AND `telegramUserId text("telegram_user_id").unique()` so both Hermes (Telegram lookup) and api-server (Replit identity lookup) work.
    - `creator_totp` table exists with columns `creatorId` (PK), `totpSecret`, `totpEnabled`, `recoveryCodes` (text[]), `enabledAt`, `updatedAt` — inferred per PATTERNS.md.
    - For every table, `insertXxxSchema` and the `Xxx` / `InsertXxx` types are exported.
    - Indexes match the patterns in RESEARCH.md (`safety_audit_log_created_at_idx`, `safety_audit_log_creator_created_idx`, `conversation_messages_conversation_idx`, `conversation_messages_creator_created_idx`, `generation_jobs_creator_idx`, `generation_jobs_revocation_idx`, plus a `unique().on(creatorId, modality, version)` on `consent_grants`).
  </behavior>
  <action>Replace the placeholder content of `lib/db/src/schema/index.ts` with the full schema. Follow the column lists and pgEnum names in RESEARCH.md "Drizzle Schema Design — 8 Tables" verbatim. Use the import block, `$onUpdateFn(() => new Date())` for `updatedAt`, and the `(t) => ({ idx: index(...) })` table-options pattern documented in PATTERNS.md. Define each pgEnum BEFORE the pgTable that uses it (forward-reference issue). For `creator_totp`, derive the column set from hermes/db.ts usage as documented in PATTERNS.md "Supplemental: creator_totp Table" — do NOT defer this; downstream Hermes work in Plan 03 needs it. For each table also export `insertXxxSchema = createInsertSchema(xxxTable).omit({ id: true, createdAt: true, updatedAt: true })`, `export type Xxx = typeof xxxTable.$inferSelect`, `export type InsertXxx = z.infer<typeof insertXxxSchema>`. Do not introduce new packages. Do not include any out-of-scope tables (no `fan_credits`, `fan_blocks`, `credit_transactions`, `fan_accounts`, `fan_subscriptions`, `creator_assets`, `creator_onboarding`, `creator_content_embeddings`) — D-01.</action>
  <verify>
    <automated>pnpm --filter @workspace/db exec tsc --noEmit 2>&1 | tee /tmp/p1t1-tsc.txt; test ! -s /tmp/p1t1-tsc.txt && grep -v '^#' lib/db/src/schema/index.ts | grep -cE 'pgTable\(' | (read N; test "$N" -ge 9 || { echo "expected >=9 pgTable; got $N"; exit 1; }) && grep -v '^#' lib/db/src/schema/index.ts | grep -cE 'pgEnum\(' | (read N; test "$N" -ge 7 || { echo "expected >=7 pgEnum; got $N"; exit 1; }) && ! grep -E 'fan_credits|fan_blocks|credit_transactions|fan_accounts|creator_assets|creator_onboarding|creator_content_embeddings' lib/db/src/schema/index.ts</automated>
  </verify>
  <done>typecheck passes for `@workspace/db`; schema file contains ≥9 `pgTable(` and ≥7 `pgEnum(` declarations; no out-of-scope table names present; all required types/insert schemas exported.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm drizzle.config.ts uses non-pooled DATABASE_URL + verify pgvector (Pitfall #6 + Open Q #3)</name>
  <files>lib/db/drizzle.config.ts</files>
  <read_first>
    - lib/db/drizzle.config.ts (current config)
    - .planning/phases/01-baseline-repair/01-RESEARCH.md Pitfall #6 (drizzle-kit push on pooled connection)
    - .planning/phases/01-baseline-repair/01-RESEARCH.md Open Question #3 (pgvector availability)
  </read_first>
  <action>Inspect `process.env.DATABASE_URL`: if it contains `pgbouncer=true` or port `:6543`, log a warning to stderr and prefer `process.env.DATABASE_URL_DIRECT` when present (fall back to `DATABASE_URL` otherwise). Update `dbCredentials.url` to read from this resolved value. Also run a one-shot pgvector check from the command line (NOT in the config file): `psql "$DATABASE_URL" -c "SELECT extname FROM pg_extension WHERE extname = 'vector';"` and record the result in `.planning/phases/01-baseline-repair/notes/pgvector-check.txt` — if vector is absent, this is informational only and does NOT block Phase 1 (embeddings are out of scope per CONTEXT.md deferred list). Do not install pgvector here.</action>
  <acceptance_criteria>
    - lib/db/drizzle.config.ts reads `process.env.DATABASE_URL_DIRECT ?? process.env.DATABASE_URL` for `dbCredentials.url`
    - lib/db/drizzle.config.ts contains a runtime check that prints to stderr (via `console.warn`) when the resolved URL contains `pgbouncer` or `:6543`
    - File `.planning/phases/01-baseline-repair/notes/pgvector-check.txt` exists and contains either `vector` (extension present) or `absent` (extension missing) — informational only
    - typecheck passes: `pnpm --filter @workspace/db exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -E 'DATABASE_URL_DIRECT' lib/db/drizzle.config.ts && grep -E 'pgbouncer|6543' lib/db/drizzle.config.ts && test -f .planning/phases/01-baseline-repair/notes/pgvector-check.txt && pnpm --filter @workspace/db exec tsc --noEmit</automated>
  </verify>
  <done>drizzle.config.ts prefers a direct (non-pooled) URL when available; pgvector status is recorded; typecheck passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Backfill existing kyc status rows before schema push (Pitfall #1 + Assumption A5)</name>
  <what-built>Drizzle schema is written. Before `drizzle-kit push`, any existing `creator_kyc` rows with legacy status values (`id_submitted`, `id_verified`, `signing_initiated`, `rights_signed`, `tax_submitted`, `ops_approved`, `complete`) must be reduced to the new 3-value enum, otherwise the push fails on the CHECK/enum constraint.</what-built>
  <how-to-verify>
    1. From a shell with `DATABASE_URL` set, run: `psql "$DATABASE_URL" -c "SELECT to_regclass('public.creator_kyc');"`
       - If result is `NULL` → the table does not exist yet → no backfill needed → reply `approved (fresh-db)`.
       - If result is `creator_kyc` → proceed to step 2.
    2. Run: `psql "$DATABASE_URL" -c "SELECT status, COUNT(*) FROM creator_kyc GROUP BY status;"`
    3. For any rows with old statuses, run the backfill: `psql "$DATABASE_URL" -c "UPDATE creator_kyc SET status='signed' WHERE status='complete'; UPDATE creator_kyc SET status='pending' WHERE status IN ('id_submitted','id_verified','signing_initiated','rights_signed','tax_submitted','ops_approved'); UPDATE creator_kyc SET status='rejected' WHERE status='rejected';"`
    4. Re-run step 2 and confirm only `pending`, `signed`, `rejected` remain.
    5. Reply `approved (backfilled)` with the row counts.
  </how-to-verify>
  <files>(no files modified — manual DB shell action against Replit PG)</files>
  <action>This is a manual founder action: open a shell with DATABASE_URL set, run the psql probes in <how-to-verify>, and execute the backfill UPDATEs if any rows have legacy status values. Do NOT proceed to Task 4 (drizzle-kit push) until the resume-signal is received with one of the two approved variants.</action>
  <verify>
    <automated>echo "Awaiting founder approval signal: 'approved (fresh-db)' or 'approved (backfilled)' must precede Task 4 push"</automated>
  </verify>
  <done>Founder has confirmed either the table does not exist (fresh DB) OR the backfill SQL has been run and only pending/signed/rejected status values remain in creator_kyc.</done>
  <resume-signal>Type `approved (fresh-db)` if the table doesn't exist OR `approved (backfilled)` with row counts after running the SQL. Type any other message to abort the push.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Run drizzle-kit push to materialize the schema on Replit PG</name>
  <files>lib/db/src/schema/index.ts</files>
  <read_first>
    - lib/db/package.json (confirms `push` script)
    - lib/db/drizzle.config.ts (post-Task 2 state — non-pooled URL preferred)
    - .planning/phases/01-baseline-repair/01-RESEARCH.md "Pitfall 6" (transaction errors on pooled connections)
  </read_first>
  <action>Run `pnpm --filter @workspace/db run push` non-interactively from the repo root. If drizzle-kit emits an interactive prompt asking to create/rename tables, abort and surface the prompt content — do NOT auto-accept renames. After push succeeds, verify the live schema by running `psql "$DATABASE_URL" -c "\\dt"` (or `SELECT tablename FROM pg_tables WHERE schemaname='public' ORDER BY tablename;`) and confirm all 9 expected tables exist: `creators`, `twins`, `creator_kyc`, `creator_config`, `consent_grants`, `conversation_messages`, `generation_jobs`, `safety_audit_log`, `creator_totp`. Also verify enum types: `SELECT typname FROM pg_type WHERE typtype='e' ORDER BY typname;` lists at least `kyc_status`, `twin_visibility`, `message_role`, `retention_category`, `consent_grant_modality`, `generation_job_status`, `crisis_level`. Record both queries' output in `.planning/phases/01-baseline-repair/notes/push-verification.txt`.</action>
  <acceptance_criteria>
    - `pnpm --filter @workspace/db run push` exits 0 without an unanswered interactive prompt
    - All 9 table names listed above are returned by `pg_tables` query against the live DB
    - All 7 pgEnum type names listed above are returned by the `pg_type` query
    - `creator_kyc.status` column has `kyc_status` user-defined type with `NOT NULL DEFAULT 'pending'`: `SELECT column_default, is_nullable, udt_name FROM information_schema.columns WHERE table_name='creator_kyc' AND column_name='status';` returns `'pending'::kyc_status`, `NO`, `kyc_status`
    - `safety_audit_log` has NO column named `fan_id` or `message_text` or `content`: `SELECT column_name FROM information_schema.columns WHERE table_name='safety_audit_log' AND column_name IN ('fan_id','message_text','content');` returns zero rows (COMPLY-03)
    - `.planning/phases/01-baseline-repair/notes/push-verification.txt` exists and contains the table + enum listings
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @workspace/db run push && psql "${DATABASE_URL_DIRECT:-$DATABASE_URL}" -tAc "SELECT COUNT(*) FROM pg_tables WHERE schemaname='public' AND tablename IN ('creators','twins','creator_kyc','creator_config','consent_grants','conversation_messages','generation_jobs','safety_audit_log','creator_totp');" | grep -q '^9$' && psql "${DATABASE_URL_DIRECT:-$DATABASE_URL}" -tAc "SELECT COUNT(*) FROM information_schema.columns WHERE table_name='safety_audit_log' AND column_name IN ('fan_id','message_text','content');" | grep -q '^0$' && test -f .planning/phases/01-baseline-repair/notes/push-verification.txt</automated>
  </verify>
  <done>All 9 tables live on Replit PG; safety_audit_log has no raw-PII columns; verification artifact committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → Replit PG | `drizzle-kit push` executes DDL; any rogue schema edit lands directly in prod-shaped DB |
| api-server route → Replit PG | Once schema is live, every downstream Drizzle query inherits its constraints (NOT NULL, enums, FKs) |
| safety_audit_log write path → DB | Must store hashes only — schema enforces no raw-PII column exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | drizzle-kit push on legacy data | mitigate | Task 3 backfill checkpoint runs SQL UPDATEs to collapse old 8-state enum into 3-state before push; without this, push fails or silently corrupts (Pitfall #1) |
| T-01-02 | Information Disclosure | safety_audit_log schema accidentally accepts raw fan_id / message_text | mitigate | Task 1 omits raw-PII columns; Task 4 acceptance criterion explicitly asserts `information_schema.columns` has zero rows for `fan_id`/`message_text`/`content` on safety_audit_log (D-02, COMPLY-03) |
| T-01-03 | Elevation of Privilege | creator_kyc.status nullable → null bypass at gate | mitigate | Task 1 declares `status` with `.notNull().default("pending")`; Task 4 acceptance verifies `is_nullable=NO` and `column_default='pending'::kyc_status` (Pitfall #4) |
| T-01-04 | Denial of Service | drizzle-kit push on PgBouncer-pooled URL | mitigate | Task 2 reads DATABASE_URL_DIRECT preferentially; emits stderr warning when pooled URL detected (Pitfall #6) |
| T-01-05 | Spoofing | creators.handle collision allowing two creators on same /lala.la/[handle] | mitigate | Task 1 declares `handle text("handle").notNull().unique()` — DB-level uniqueness constraint |
| T-01-SC | Tampering | npm/pnpm installs | accept | No new packages added in Plan 01 (all Drizzle deps already present in catalog); legitimacy gate not applicable per RESEARCH.md "Package Legitimacy Audit" |
</threat_model>

<verification>
- `pnpm --filter @workspace/db exec tsc --noEmit` exits 0
- `pnpm --filter @workspace/db run push` exits 0; all 9 tables present in `pg_tables`
- `kyc-gate.e2e.test.ts` exists and currently FAILS (Tests 1 + 3 — the contract for Plan 02)
- `safety_audit_log` schema enforces COMPLY-03: zero raw-PII columns
- `creator_kyc.status` is NOT NULL with `kyc_status` pgEnum default `'pending'`
- pgvector status recorded (informational)
</verification>

<success_criteria>
The schema file compiles, the live Replit PG DB has all 9 tables with correct constraints, and a failing E2E test pins the KYC gate behavior that Plan 02 must deliver. Downstream plans (02, 03, 04) can `import { db, creatorsTable, creatorKycTable, ... } from "@workspace/db"` and write/read against a real, typed schema.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-repair/01-01-SUMMARY.md` when done.
</output>
