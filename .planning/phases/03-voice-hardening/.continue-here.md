---
context: phase
phase: 03-voice-hardening
plan: 6
task: 1
total_tasks: 3
status: blocked_founder
last_updated: 2026-05-29T23:03:55.309Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: GMI_TTS_CONTRACT — 03-06 is `autonomous: false` and requires founder-confirmed endpoint + model before ANY voice code is written. Do not guess the endpoint. Surface a `[BLOCKING]` checkpoint if contract is still missing.
- [ ] CONSTRAINT: @replit/object-storage not installed — DSAR worker (03-04) uses HTTP REST fallback for Object Storage. If 03-06/07 need the SDK for voice file upload, founder must approve install via the supply-chain gate first.

**Do not proceed with 03-06 until both boxes are checked.**

<current_state>
Branch: rio-de-janeiro. Phase 3 Wave 3 complete. Paused at Wave 4 entry (03-06).

Plans 03-01 through 03-05 are committed and typechecks pass. Next plan is 03-06 (voice synthesis provider + opossum circuit breaker) which is `autonomous: false` — it cannot start until the founder confirms the GMI TTS endpoint/model and provisions VOICE_URL_SIGNING_SECRET.
</current_state>

<completed_work>
- 03-01: locale normalizer (normalizeLocale), Wave 0 deps scaffold
- 03-02: DB schema (fan_name_masks, creator_deletion_log, voice_generation_jobs tables) + queue names/types/options for dsarDeletion + voiceGeneration
- 03-03: MOD-07 escalation scorer (lib/twin-runtime/src/escalation.ts, 11 tests passing, wired into routes/twin.ts + worker/text-generation.ts)
- 03-04: DSAR wizard (hermes dsar.scene.ts + setKillSwitchActive + recordDsarRequest + dsar-deletion worker 8-step sweep, commit 3fa1b87)
- 03-05: /review_masks founder queue (review-masks.scene.ts + getNextPendingMask + setMaskReviewed + isFounder gate + bot.action UUID validation, commit 47697ea)
</completed_work>

<remaining_work>
- 03-06 (Wave 3, autonomous:false): Voice provider (GmiTtsClient / GmiVoiceProvider) + opossum circuit breaker + voice-generation worker body. **BLOCKED on founder GMI TTS contract.**
- 03-07 (Wave 4, autonomous:true): Voice proxy endpoint GET /api/voice/:jobId (HMAC token), OpenAPI codegen, VoiceMessageBubble React component, wire twin.ts + text-generation.ts to enqueue voice jobs.
- 03-08 (Wave 5, autonomous:false): E2E verification of all 5 Phase 3 success criteria (SC1–SC5). Human verification required.
</remaining_work>

<decisions_made>
- DSAR Object Storage sweep uses HTTP REST (fetch to Replit storage API) — @replit/object-storage SDK not installed in worker
- Hermes locale resolution for /dsar and /review_masks: getCreatorPreferences(creator.id) → hermesLanguage → fallback 'en'
- isFounder gate reads FOUNDER_TELEGRAM_USER_ID env var (comma-separated user IDs)
- DSAR_TEST_DELAY_MS env var overrides 24h delay for 03-08 E2E testing
</decisions_made>

<blockers>
1. **[BLOCKING] GMI TTS endpoint not confirmed** — founder must test the audio API and document in `.planning/phases/03-voice-hardening/03-01-GMI-TTS-CONTRACT.md`. Without this, 03-06 cannot proceed.
2. **[BLOCKING] VOICE_URL_SIGNING_SECRET not provisioned** — generate: `openssl rand -hex 32` → Replit Secrets. Required by 03-07 voice proxy.
3. **[ADVISORY] FOUNDER_TELEGRAM_USER_ID not set** — /review_masks and /dsar scenes are live but founder cannot use them without this Replit Secret.
4. **[ADVISORY] REDIS_URL** — verify set in Replit Secrets; DSAR deletion + voice jobs both need it.
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-voice-hardening/03-06-PLAN.md` — voice provider plan; read the `user_setup` block for the founder checkpoint items
2. `.planning/phases/03-voice-hardening/03-CONTEXT.md` — autonomous-mode defaults for voice (timeout, circuit-breaker, pre-signed URL approach)
3. `.planning/phases/03-voice-hardening/03-RESEARCH.md` — §Pattern 3 (GmiTtsClient shape), §Pitfall 6 (opossum half-open state), §Threat T-03-06-*

## Infrastructure State
- Branch: `rio-de-janeiro` (all Phase 3 work to date)
- All typechecks: passing (hermes, worker, twin-runtime, api-server)
- Replit secrets needed: `GMI_TTS_ENDPOINT_PATH`, `GMI_TTS_MODEL_ID`, `VOICE_URL_SIGNING_SECRET`, `FOUNDER_TELEGRAM_USER_ID`, `REDIS_URL` (if not already set)
- @replit/object-storage: NOT installed in worker or api-server (03-01 founder checkpoint not cleared)

<context>
Phase 3 has 5 waves. Waves 0-3 (plans 03-01 through 03-05) are complete. Wave 4 (03-06 voice synthesis, 03-07 voice proxy) requires GMI TTS endpoint confirmation — this is the only remaining external dependency before Phase 3 can close. Wave 5 (03-08 E2E) requires human testing of all 5 success criteria.

The stop hook is set to "Claire live as first paying creator" which requires Phase 3 complete + her actual Hermes onboarding + eval gate pass + fan conversion. Phase 3 completion is ~2 sessions away once the GMI TTS contract is provided.
</context>

<next_action>
Founder provides GMI TTS contract (endpoint path + model ID + confirms VOICE_URL_SIGNING_SECRET provisioned) → start fresh session → implement 03-06 voice synthesis provider + circuit breaker.

Opening message for next session:
"Continue Phase 3 voice-hardening on rio-de-janeiro. 03-01 through 03-05 are done. GMI TTS contract: endpoint=[X] model=[Y]. VOICE_URL_SIGNING_SECRET is set. Implement 03-06."
</next_action>
