---
phase: 02-twin-runtime-core
plan: 05
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - artifacts/api-server/src/providers/openai/OpenAiModeratorProvider.ts
  - artifacts/api-server/src/providers/openai/MockModeratorProvider.ts
  - artifacts/api-server/src/providers/interfaces.ts
  - artifacts/api-server/src/providers/registry.ts
  - artifacts/api-server/src/lib/moderation.ts
  - artifacts/api-server/src/lib/deflections.ts
  - artifacts/api-server/src/lib/helplines.ts
  - artifacts/api-server/src/lib/notify-founder.ts
  - artifacts/api-server/src/routes/twin.ts
  - artifacts/api-server/src/__tests__/moderation-l1.test.ts
  - artifacts/api-server/src/__tests__/moderation-l3.test.ts
  - artifacts/api-server/src/__tests__/helpline-injection.test.ts
  - artifacts/web/src/components/fan/CrisisHelplineBubble.tsx
  - artifacts/web/src/components/fan/MonetizationCTA.tsx
  - artifacts/web/src/pages/fan-page.tsx
autonomous: true
requirements: [MOD-01, MOD-02, MOD-03, MOD-04, MOD-05, MOD-06, COMPLY-02, CHAT-05]
tags: [phase-2, moderation, openai, l1, l3, safety, sb243, crisis, monetization-cta]

must_haves:
  truths:
    - "Self-harm input (e.g. 'I want to hurt myself') in any locale → response is helpline + deflection (NOT LLM-generated reply); safety_audit_log row written with hashed identifiers; founder notify fired"
    - "Sexual category input → deflection from `lib/deflections.ts` (locale-keyed); LLM never invoked; safety_audit_log row written"
    - "Clean input → LLM invoked; L3 runs on output; if L3 flagged, output replaced with deflection"
    - "OpenAiModeratorProvider implements IModeratorProvider; calls https://api.openai.com/v1/moderations with model=omni-moderation-latest"
    - "getDeflection(locale, category) returns locale-keyed string from a static table (NEVER LLM-generated per Pitfall #13)"
    - "getHelpline(locale) returns hardcoded JP `0120-279-338`, TW `1925`, EN `988`, HK `2389 2222` per D-02-05"
    - "L5 high-severity flag → notifyFounderAsync(text) hits Telegram Bot API (no Telegraf import); fire-and-forget"
    - "MockModeratorProvider available for tests (always flagged=false)"
    - "CrisisHelplineBubble renders as separate React component with amber-400 left border 4px (UI-SPEC color contract)"
    - "MonetizationCTA renders as inline pill at end of AI bubble when response.monetization_pivot === true (CHAT-05)"
  artifacts:
    - path: "artifacts/api-server/src/providers/openai/OpenAiModeratorProvider.ts"
      provides: "POST /v1/moderations wrapper with ProviderError taxonomy"
    - path: "artifacts/api-server/src/providers/openai/MockModeratorProvider.ts"
      provides: "Test-only — always flagged=false"
    - path: "artifacts/api-server/src/lib/moderation.ts"
      provides: "runL1Moderation, runL3Moderation, composeFlaggedReply"
    - path: "artifacts/api-server/src/lib/deflections.ts"
      provides: "getDeflection(locale, category)"
    - path: "artifacts/api-server/src/lib/helplines.ts"
      provides: "getHelpline(locale); HELPLINES const"
    - path: "artifacts/api-server/src/lib/notify-founder.ts"
      provides: "notifyFounderAsync(text) — fire-and-forget Telegram Bot API call"
    - path: "artifacts/web/src/components/fan/CrisisHelplineBubble.tsx"
      provides: "Amber-bordered alert bubble for COMPLY-02 helpline injection"
    - path: "artifacts/web/src/components/fan/MonetizationCTA.tsx"
      provides: "Inline pill linking to creator monetization URL"
  key_links:
    - from: "routes/twin.ts"
      to: "lib/moderation.ts runL1Moderation"
      via: "import + call before persistTurn(user)"
      pattern: "runL1Moderation"
    - from: "routes/twin.ts"
      to: "lib/moderation.ts runL3Moderation"
      via: "import + call after getTextProvider().generateText"
      pattern: "runL3Moderation"
    - from: "lib/moderation.ts"
      to: "lib/safety-audit.ts writeSafetyAuditLog"
      via: "L6 audit write per flagged turn"
      pattern: "writeSafetyAuditLog"
    - from: "lib/moderation.ts"
      to: "lib/notify-founder.ts notifyFounderAsync"
      via: "L5 alert on high-severity"
      pattern: "notifyFounderAsync"
    - from: "routes/twin.ts"
      to: "OpenAI moderation API"
      via: "OpenAiModeratorProvider.moderate"
      pattern: "openai.com/v1/moderations"
---

<objective>
Wrap L1+L3+L4+L5+L6 moderation around the LLM call. L2 already lives in `system-prompt.ts` (built in 02-02). L6 (safety_audit_log) already exists from Phase 1. This plan ships: OpenAiModeratorProvider, the wrappers in `lib/moderation.ts`, the deflection + helpline string tables, the founder-notify helper, the integration into `routes/twin.ts`, and the two web UI components that render the helpline + the monetization CTA.

Purpose: SB 243 (effective 2026-01-01) is past — every twin chat MUST run input + output moderation and inject crisis helpline per locale on self-harm detection. This is a Day-1 compliance requirement with $1,000/violation private right of action.

Output: 6-layer pipeline fully wired (L1, L3, L4, L5, L6 in api-server; L2 already in system-prompt.ts from 02-02). 3 RED tests GREEN (MOD-01, MOD-03, COMPLY-02). Two new web components (CrisisHelplineBubble, MonetizationCTA).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-twin-runtime-core/02-CONTEXT.md
@.planning/phases/02-twin-runtime-core/02-RESEARCH.md
@.planning/phases/02-twin-runtime-core/02-PATTERNS.md
@.planning/phases/02-twin-runtime-core/02-UI-SPEC.md
@.planning/phases/02-twin-runtime-core/02-03-SUMMARY.md
@artifacts/api-server/src/providers/gmi/GmiTextProvider.ts
@artifacts/api-server/src/providers/interfaces.ts
@artifacts/api-server/src/providers/registry.ts
@artifacts/api-server/src/lib/safety-audit.ts
@artifacts/api-server/src/routes/twin.ts
</context>

<interfaces>
<!-- Existing patterns to extend / mirror -->

From artifacts/api-server/src/providers/interfaces.ts (current):
- `interface ITextProvider { generateText(...) }`
- `interface IVoiceProvider { ... }` (stub)
- `ProviderError`, `ProviderTransientError` classes exported

From artifacts/api-server/src/providers/gmi/GmiTextProvider.ts:
- Constructor pattern with env-var key + throw-on-missing
- `fetch(...).catch(err => throw new ProviderTransientError)` shape
- `if (!res.ok)` taxonomy: 5xx → transient, 4xx → permanent
- Optional Helicone routing via `HELICONE_API_KEY`

From artifacts/api-server/src/lib/safety-audit.ts (existing L6):
- `writeSafetyAuditLog({creatorId, fanId, sessionId, messageText, crisisLevel, crisisType?, locale, responseSent, twinPaused, confidence})` — fire-and-forget; hashes fanId and messageText before insert
- `hashFanId(value): string` — sha256 hex
- `fireSlackAlert` for crisis-level=high (existing pattern to mirror in notify-founder.ts)

From plan 02-03 routes/twin.ts:
- POST /twin/chat handler with: detectLocale, loadHistory, buildSystemPrompt, getTextProvider().generateText, persistTurn, response shape
- This plan SPLICES moderation L1 before LLM call, L3 after LLM call

OpenAI Moderation API contract (RESEARCH Pattern 3):
- POST https://api.openai.com/v1/moderations with `{model: "omni-moderation-latest", input: text}`
- Response: `{results: [{flagged: boolean, categories: {self-harm: bool, sexual: bool, ...}, category_scores: {self-harm: number, ...}}]}`
- Categories used: `self-harm`, `self-harm/intent`, `self-harm/instructions`, `sexual`, `sexual/minors`, `harassment`, `violence`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpenAiModeratorProvider + interfaces + registry + MockModeratorProvider</name>
  <files>artifacts/api-server/src/providers/openai/OpenAiModeratorProvider.ts, artifacts/api-server/src/providers/openai/MockModeratorProvider.ts, artifacts/api-server/src/providers/interfaces.ts, artifacts/api-server/src/providers/registry.ts</files>
  <read_first>
    - artifacts/api-server/src/providers/gmi/GmiTextProvider.ts (entire file — copy structure)
    - artifacts/api-server/src/providers/interfaces.ts (existing ITextProvider/IVoiceProvider declarations)
    - artifacts/api-server/src/providers/registry.ts (existing getTextProvider/getVoiceProvider pattern)
    - .planning/phases/02-twin-runtime-core/02-PATTERNS.md (A6, A7, A8 — provider/interface/registry specs)
    - .planning/phases/02-twin-runtime-core/02-RESEARCH.md (Pattern 3 — OpenAI moderator full snippet)
  </read_first>
  <behavior>
    - Constructor throws when OPENAI_API_KEY missing
    - moderate("benign hello") → `{flagged: false, categories: [], scores: {...}, primaryCategory: null}` (verified by fetch stub)
    - moderate("self-harm bait") with mocked API response `{results:[{flagged:true, categories:{self-harm:true, ...}, category_scores:{self-harm:0.9, ...}}]}` → `{flagged: true, categories: ["self-harm", ...], primaryCategory: "self-harm"}`
    - 5xx from OpenAI → throws ProviderTransientError
    - 4xx (e.g. 401 bad key) → throws ProviderError
    - Network error → throws ProviderTransientError
    - MockModeratorProvider.moderate(anyInput) always returns `{flagged: false, categories: [], scores: {}, primaryCategory: null}`
    - getModeratorProvider() returns singleton; `MODERATOR_PROVIDER=mock` returns MockModeratorProvider; `MODERATOR_PROVIDER=openai` (or unset) returns OpenAiModeratorProvider
  </behavior>
  <action>
    Extend `interfaces.ts` per PATTERNS A7: add `ModerationResult { flagged, categories[], scores, primaryCategory }` and `IModeratorProvider { modelId; moderate(text): Promise<ModerationResult> }`.

    Create `providers/openai/OpenAiModeratorProvider.ts` per PATTERNS A6 + RESEARCH Pattern 3:
    - Constructor: `{ apiKey?, baseUrl? }` defaults to env OPENAI_API_KEY + "https://api.openai.com/v1"; throw if no key
    - `modelId = "omni-moderation-latest"`
    - `moderate(text)`: POST `${baseUrl}/moderations` with `{model: modelId, input: text}` and `Authorization: Bearer ${apiKey}`
    - Error taxonomy: try/catch fetch → ProviderTransientError; 5xx → transient; non-2xx → ProviderError
    - Parse `data.results[0]` → extract flagged categories (`Object.entries(categories).filter(([_, v]) => v).map(([k]) => k)`) and primaryCategory (highest-scoring flagged category)
    - Optional Helicone routing if HELICONE_API_KEY set (mirror GmiTextProvider — `https://oai.helicone.ai/v1` baseUrl swap + Helicone-Auth header)

    Create `providers/openai/MockModeratorProvider.ts`:
    - Implements IModeratorProvider; modelId="mock"; moderate() always returns flagged:false

    Extend `registry.ts` per PATTERNS A8: add `_moderatorProvider` singleton + `getModeratorProvider()` switch on `MODERATOR_PROVIDER` env (default "openai", supports "mock"). Update `resetProviderRegistry()` to clear `_moderatorProvider`.
  </action>
  <verify>
    <automated>pnpm --filter @workspace/api-server exec tsc --noEmit && grep -c "IModeratorProvider" artifacts/api-server/src/providers/interfaces.ts | awk '{exit ($1>=1)?0:1}' && grep -c "getModeratorProvider" artifacts/api-server/src/providers/registry.ts | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>
    - OpenAiModeratorProvider + MockModeratorProvider files exist
    - interfaces.ts has IModeratorProvider + ModerationResult exports
    - registry.ts has getModeratorProvider + reset entry
    - api-server typechecks
  </done>
  <acceptance_criteria>
    - Per CLAUDE.md mandate: OpenAI used ONLY for moderation (not for LLM text generation)
    - Per PATTERNS S3: provider class shape matches GmiTextProvider (constructor, fetch, error taxonomy)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/moderation.ts + lib/deflections.ts + lib/helplines.ts + lib/notify-founder.ts + integrate into routes/twin.ts</name>
  <files>artifacts/api-server/src/lib/moderation.ts, artifacts/api-server/src/lib/deflections.ts, artifacts/api-server/src/lib/helplines.ts, artifacts/api-server/src/lib/notify-founder.ts, artifacts/api-server/src/routes/twin.ts, artifacts/api-server/src/__tests__/moderation-l1.test.ts, artifacts/api-server/src/__tests__/moderation-l3.test.ts, artifacts/api-server/src/__tests__/helpline-injection.test.ts</files>
  <read_first>
    - artifacts/api-server/src/lib/safety-audit.ts (existing L6 write + Slack alert pattern — notify-founder.ts mirrors fireSlackAlert)
    - artifacts/api-server/src/routes/twin.ts (newly written in 02-03 — insert L1 before persistTurn(user), L3 after generateText)
    - .planning/phases/02-twin-runtime-core/02-PATTERNS.md (A9 — notify-founder; S2 — fire-and-forget; S8 — disclosure)
    - .planning/phases/02-twin-runtime-core/02-RESEARCH.md (Pattern 5 — composeFlaggedReply; Pitfall #13 — helpline string table; Common Pitfalls #1, #4)
    - .planning/phases/02-twin-runtime-core/02-UI-SPEC.md (Copywriting Contract → Moderation deflection + Crisis helpline rows)
    - .planning/phases/02-twin-runtime-core/02-CONTEXT.md (D-02-04 founder notify decision; D-02-05 JP helpline number)
  </read_first>
  <behavior>
    Moderation L1 (input):
    - L1 flagged self-harm in any locale → response body = helpline + deflection (helpline FIRST per UI-SPEC); no LLM call; safety_audit_log row written with `crisis_level: 'high'`, hashed fan_id, hashed message; notifyFounderAsync invoked
    - L1 flagged sexual → response = deflection_sexual (locale-keyed); no LLM call; audit row written with `crisis_level: 'medium'`; no founder notify
    - L1 clean → LLM proceeds normally

    Moderation L3 (output):
    - LLM returns flagged content (e.g. system-prompt leak with "I am an AI") → output replaced with deflection_default; audit row with `crisis_level: 'high'`; founder notified
    - LLM returns clean content → returned as-is

    Helpline injection (COMPLY-02):
    - getHelpline("en") includes "988"
    - getHelpline("ja") includes "0120-279-338" (D-02-05)
    - getHelpline("zh-TW") includes "1925"
    - getHelpline("zh-HK") includes "2389 2222"
    - Helpline strings are HARDCODED (Pitfall #13)
    - composeFlaggedReply with primaryCategory startsWith "self-harm" → helpline prepended (separated by "\n\n") before deflection

    notify-founder:
    - notifyFounderAsync("alert text") → POST https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN_LALA}/sendMessage with `{chat_id: FOUNDER_TELEGRAM_CHAT_ID, text, parse_mode: "Markdown"}`
    - When FOUNDER_TELEGRAM_CHAT_ID unset → logs warning and returns (no throw)
    - Fire-and-forget (caller does not await)
  </behavior>
  <action>
    **lib/deflections.ts:** Export `getDeflection(locale, category): string` and `DEFLECTIONS` const. Three locales × 4 categories (default, sexual, harassment, self-harm — self-harm uses the default deflection text since helpline is prepended separately). Copy strings VERBATIM from UI-SPEC Copywriting Contract → Moderation deflection table.

    **lib/helplines.ts:** Export `HELPLINES` const with en/ja/zh-TW/zh-HK strings per D-02-05 + UI-SPEC. Export `getHelpline(locale): string` with fallback to en when locale unknown. The JP string MUST use `0120-279-338` (NOT the stale CLAUDE.md number).

    **lib/notify-founder.ts:** Per PATTERNS A9 — `notifyFounder(text): Promise<void>` (internal) calls Telegram Bot API via fetch; `notifyFounderAsync(text): void` wraps with `void (async () => { ... })()`. Reads `TELEGRAM_BOT_TOKEN_LALA` + `FOUNDER_TELEGRAM_CHAT_ID` from env. Skip-and-log when either missing.

    **lib/moderation.ts:** Export:
    - `runL1Moderation({text, locale, creatorId, fanIdHash, sessionId}): Promise<{flagged: boolean; reply?: string}>` — calls getModeratorProvider().moderate(text); if flagged: composes reply (helpline+deflection for self-harm, deflection-only for others), writes safety_audit_log with correct crisis_level (self-harm=high, sexual/violence=high, harassment=medium, other=low), fires notifyFounderAsync on high severity; returns `{flagged: true, reply}` else `{flagged: false}`
    - `runL3Moderation({text, locale, creatorId, fanIdHash, sessionId}): Promise<{flagged: boolean; reply?: string}>` — same shape but called on LLM output
    - `composeFlaggedReply(mod: ModerationResult, locale): string` — prepends helpline when categories include any `self-harm*`, then deflection text by primaryCategory
    - `severityFromCategories(categories): "low"|"medium"|"high"` — maps self-harm/sexual/violence to high, harassment to medium, default low

    **Update routes/twin.ts** (splice into the pipeline written in 02-03):
    - After `const locale = detectLocale(req)` and creator/twin resolution but BEFORE `persistTurn(user)`:
      ```
      const l1 = await runL1Moderation({text: message, locale, creatorId, fanIdHash: hashFanId(req), sessionId: conversationId});
      if (l1.flagged) {
        // Persist user message anyway (audit trail) but skip LLM
        await persistTurn({conversationId, creatorId, twinId, role: "user", content: message});
        await persistTurn({conversationId, creatorId, twinId, role: "assistant", content: l1.reply!});
        return res.json({text: l1.reply, disclosure_footer: getDisclosureFooter(locale, handle), monetization_pivot: false, conversation_id: conversationId});
      }
      ```
    - After `const llm = await getTextProvider().generateText(...)` and BEFORE persisting assistant turn:
      ```
      const l3 = await runL3Moderation({text: llm.content, locale, creatorId, fanIdHash: hashFanId(req), sessionId: conversationId});
      const safeReply = l3.flagged ? l3.reply! : llm.content;
      await persistTurn({...role: "assistant", content: safeReply});
      return res.json({text: safeReply, disclosure_footer: getDisclosureFooter(locale, handle), monetization_pivot, conversation_id: conversationId});
      ```

    Turn the 3 RED tests GREEN:
    - **moderation-l1.test.ts**: stub fetch to return flagged self-harm response; invoke runL1Moderation; assert reply contains JP helpline "0120-279-338" when locale='ja' and a JP deflection
    - **moderation-l3.test.ts**: stub fetch to return clean for input, flagged for output; invoke runL3Moderation on output; assert reply is deflection
    - **helpline-injection.test.ts**: invoke composeFlaggedReply with categories=["self-harm"] for each locale; assert helpline string is the first ~120 chars of the reply
  </action>
  <verify>
    <automated>pnpm --filter @workspace/api-server exec tsc --noEmit && pnpm --filter @workspace/api-server exec vitest run src/__tests__/moderation-l1.test.ts src/__tests__/moderation-l3.test.ts src/__tests__/helpline-injection.test.ts && grep -c "runL1Moderation\|runL3Moderation" artifacts/api-server/src/routes/twin.ts | awk '{exit ($1>=2)?0:1}' && grep -E "0120-279-338" artifacts/api-server/src/lib/helplines.ts</automated>
  </verify>
  <done>
    - 4 new lib files exist
    - routes/twin.ts invokes runL1Moderation before LLM and runL3Moderation after
    - 3 RED tests GREEN
    - helplines.ts contains `0120-279-338` (D-02-05) and `988`, `1925`, `2389 2222`
    - deflections.ts contains UI-SPEC strings verbatim
  </done>
  <acceptance_criteria>
    - Per MOD-01: L1 runs on fan input before LLM
    - Per MOD-03: L3 runs on LLM output before fan delivery
    - Per MOD-04: deflection strings are pre-canned (no LLM-generated deflections)
    - Per MOD-05: notifyFounderAsync invoked on high-severity flags
    - Per MOD-06: writeSafetyAuditLog invoked on every flagged turn
    - Per COMPLY-02: helpline injection uses hardcoded JP `0120-279-338`
    - Per D-02-04: notify-founder calls Telegram Bot API directly (no Telegraf import)
    - Per PATTERNS S2: notifyFounderAsync is fire-and-forget
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CrisisHelplineBubble + MonetizationCTA web components + integrate into fan-page.tsx</name>
  <files>artifacts/web/src/components/fan/CrisisHelplineBubble.tsx, artifacts/web/src/components/fan/MonetizationCTA.tsx, artifacts/web/src/pages/fan-page.tsx</files>
  <read_first>
    - artifacts/web/src/components/ui/alert.tsx (shadcn Alert — basis for CrisisHelplineBubble)
    - artifacts/web/src/components/ui/button.tsx (basis for MonetizationCTA pill)
    - .planning/phases/02-twin-runtime-core/02-UI-SPEC.md (Component Inventory rows for CrisisHelplineBubble + MonetizationCTA; Interaction Contract → Crisis helpline rendering + Monetization CTA placement; Color section → Crisis color contract)
    - artifacts/web/src/lib/i18n.ts (Phase 2 i18n keys added in 02-04)
    - artifacts/web/src/pages/fan-page.tsx (newly refactored in 02-04 — slot CrisisHelplineBubble + MonetizationCTA into the message rendering)
  </read_first>
  <action>
    **CrisisHelplineBubble.tsx:** Wrap shadcn Alert. Props: `{helplineText: string, locale: string}` — helplineText comes from API response text field when the response is a crisis reply (api-server has already injected helpline+deflection). For Phase 2 detection: the page can check whether the response text contains a known helpline phone number OR (cleaner) the api-server adds a `crisis: true` field to the response on COMPLY-02 turns. Per discretion: api-server already attaches `crisis_helpline_visible: boolean` and `crisis_helpline_text: string` to /api/twin/chat response — but this would require a 02-03 contract change. SIMPLEST: server returns `text` containing helpline FIRST + "\n\n" + deflection; client splits on first "\n\n", first segment renders in CrisisHelplineBubble, second in standard MessageBubble. Choose THIS approach — keeps the API contract from 02-03 stable.

    Visual: amber-400 text on neutral-900 surface, 4px amber-500 left border, no disclosure footer, `role="alert" aria-live="assertive"`. Tailwind: `border-l-4 border-amber-500 bg-neutral-900 text-amber-400 p-3 my-2 rounded-r-md`.

    **MonetizationCTA.tsx:** Wrap shadcn Button with `variant="default"` + `rounded-full` + brand-color CSS var. Props: `{platformName: string, monetizationUrl: string, locale: string}`. Renders `Want more? Find me on {platformName} →` (locale-keyed via i18n monetization_cta). Click opens monetizationUrl in new tab. Visual: 12px text, 10px y / 14px x padding, brand-color background. ARIA: `aria-label="Open {platformName} in new tab"`.

    **fan-page.tsx integration:**
    - When rendering an AI message bubble, check if text contains a helpline phone (regex `(988|0120-279-338|1925|2389 2222)`) — if yes, split text on first "\n\n", render `<CrisisHelplineBubble helplineText={firstSegment} />` followed by `<MessageBubble role="ai" text={secondSegment} ... />`. Else render single `<MessageBubble role="ai" text={...} />`.
    - When response has `monetization_pivot === true` AND `profile.monetization_url`: append `<MonetizationCTA platformName={profile.platform_name} monetizationUrl={profile.monetization_url} locale={locale} />` inside the AI bubble container (after disclosure footer).
    - Trial counter: do NOT decrement on crisis-helpline turns (per UI-SPEC State Inventory). Detection: same regex check.
  </action>
  <verify>
    <automated>cd artifacts/web && pnpm exec tsc --noEmit && test -f src/components/fan/CrisisHelplineBubble.tsx && test -f src/components/fan/MonetizationCTA.tsx && grep -c "CrisisHelplineBubble\|MonetizationCTA" src/pages/fan-page.tsx | awk '{exit ($1>=2)?0:1}'</automated>
  </verify>
  <done>
    - Both new components exist and typecheck
    - fan-page.tsx imports both and renders conditionally
    - Visual smoke (founder): send a self-harm-trigger message in dev → CrisisHelplineBubble renders above deflection bubble with amber border
    - Visual smoke: trigger monetization_pivot (every 5th AI reply) → CTA pill renders inside AI bubble
  </done>
  <acceptance_criteria>
    - Per UI-SPEC Crisis helpline rendering: two separate bubbles (helpline + deflection)
    - Per UI-SPEC color contract: amber-400 text + amber-500 left border 4px
    - Per CHAT-05: MonetizationCTA opens monetization_url in new tab
    - Per UI-SPEC Accessibility: CrisisHelplineBubble uses `role="alert" aria-live="assertive"`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fan input → OpenAI moderation API | outbound HTTPS, server-side only, fan PII never sent (only the message text per OpenAI ToS) |
| LLM output → OpenAI L3 moderation | outbound HTTPS, output text only |
| api-server → Telegram Bot API (notifyFounder) | outbound HTTPS using LALA token; only founder chat receives messages |
| audit log → DB | sha256 hashes only, no raw fan_id or message_text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Info Disclosure | OpenAI logs fan message content | accept | OpenAI moderation API per CLAUDE.md mandate; raw message text sent (required for moderation); SB 243 compliance value > privacy cost; documented in privacy policy (out of scope for this plan) |
| T-02-05-02 | Info Disclosure | helpline number wrong / hallucinated | mitigate | Pitfall #13 — HARDCODED string table in helplines.ts; never LLM-generated; D-02-05 locks JP number; test asserts substring |
| T-02-05-03 | Tampering | prompt injection bypasses L3 | mitigate | L2 meta-instruction (system-prompt.ts from 02-02) + L3 omni-moderation; multi-turn Crescendo deferred to Phase 3 MOD-07 (accepted residual) |
| T-02-05-04 | Info Disclosure | safety_audit_log leaks raw fan PII | mitigate | writeSafetyAuditLog (Phase 1) already hashes fanId and messageText via sha256; this plan reuses without modification |
| T-02-05-05 | Spoofing | crisis-level escalation faked to trigger notifyFounder spam | mitigate | severityFromCategories uses OpenAI categories (not fan-controlled); only OpenAI's flag escalates; rate-limit on notifyFounder deferred (accepted — N=1 means founder can manually handle spam) |
| T-02-05-06 | DoS | OpenAI moderation cost amplification | accept | omni-moderation is free per RESEARCH Assumption A2; verify cost on first 1000 calls and budget if surprising |
| T-02-05-07 | Info Disclosure | TELEGRAM_BOT_TOKEN_LALA in notify-founder HTTP logs | mitigate | Token in URL path is sensitive; pino-http does not log POST URLs by default (only req.url which is local); double-check by greppping logs after first deploy |
| T-02-05-SC | Tampering | npm installs | accept | No new package installs (OpenAI calls via fetch, not openai SDK per RESEARCH) |
</threat_model>

<verification>
- `pnpm --filter @workspace/api-server exec tsc --noEmit` exits 0
- `pnpm --filter @workspace/api-server exec vitest run src/__tests__/moderation-l1.test.ts src/__tests__/moderation-l3.test.ts src/__tests__/helpline-injection.test.ts` exits 0
- `pnpm --filter @workspace/api-server run test` full suite passes (no regression in twin-chat.e2e from 02-03)
- `cd artifacts/web && pnpm exec tsc --noEmit` exits 0
- `grep "0120-279-338" artifacts/api-server/src/lib/helplines.ts` returns ≥1 match
- `grep "writeSafetyAuditLog\|notifyFounderAsync" artifacts/api-server/src/lib/moderation.ts` returns ≥2 matches
- Manual integration (founder, in Replit): POST self-harm text in JP locale → response includes "0120-279-338"; safety_audit_log row exists; founder Telegram receives alert
</verification>

<success_criteria>
- 6 moderation layers verifiably wired (L1+L2+L3 in chat handler; L4 = deflections.ts; L5 = notify-founder.ts; L6 = safety-audit.ts)
- 3 RED tests GREEN
- CrisisHelplineBubble + MonetizationCTA render in fan-page
- Self-harm input in any locale → helpline + deflection, no LLM call, audit + founder notify fired
- COMPLY-02 helpline numbers match D-02-05 (JP 0120-279-338 specifically)
</success_criteria>

<output>
Create `.planning/phases/02-twin-runtime-core/02-05-SUMMARY.md` documenting moderation wiring, founder-smoke-test result for self-harm path, OpenAI cost on first batch of calls.
</output>
